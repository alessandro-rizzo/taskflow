---
id: TF-003.15
title: Run E07 namespace-private services and endpoint-routing experiment
status: In Progress
assignee:
  - '@codex-tf-003-15'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-04 17:07'
labels:
  - risk-lab
  - e07
  - namespaces
  - services
  - parallel-wave-r2
dependencies:
  - TF-003.04
  - TF-003.05
  - TF-003.13
references:
  - docs/roadmap.md#e07-namespace-private-services-and-cross-target-endpoints
  - fixtures/w3/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E07; risk R7; requirements NS-1 through NS-5.
Outcome: run two simultaneous W3 service subsets to determine whether typed, authorization-aware endpoints hide port/provider networking and mutable service state from project code.
Stable assumptions: a fake macOS consumer is allowed; E07 does not create production Service, Endpoint, or Stack packages.
Parallelism: allowed beside later core Risk Lab work once W3/lease fixtures are hardened. Own experiments/e07-namespace-services only.
Test or fault scenario: concurrent namespaces, port/data/name isolation, readiness without sleeps, unauthorized routing, caller loss, TTL cleanup, artifact reuse without mutable-data reuse, and provider-specific routing.
Observability: namespace/resource/lease IDs, routing decision and capability, health transitions, cleanup latency, collisions, and diagnostics.
Rollback or removal: all service resources are bounded to experiment namespaces with TTL and explicit teardown.
Versioned formats: endpoint/service evidence schemas are experimental.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Two W3 subsets run concurrently with no fixed project-code ports and no cross-namespace writable path, service name, volume, data, or endpoint exposure
- [ ] #2 Endpoint handles are typed and authorization-aware, and unauthorized cross-target consumers fail closed with precise diagnostics
- [ ] #3 Readiness is state-based rather than sleep-based and client loss/TTL reclaim every mutable resource within a declared bound
- [ ] #4 The decision selects typed endpoint management, explicit provider routing, Compose-style integration, or scope narrowing with measured overhead
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Phase A - freeze the result-free E07 contract before any service or listener starts

1. Create only a result-free experiment contract under experiments/e07-namespace-services: README.md, Taskfile.yml, contract.json, fixture-bindings.json, thresholds.json, decision-matrix.json, event-schema.json, frozen-artifacts.json, protocol.sha256, a dependency-free contract verifier, and verifier mutation tests. Mark every envelope experimental, forbid production/prototype imports and public packages, and make the Phase A check reject service/controller code, generated evidence, listeners, databases, selected branches, and the ADR.
2. Bind by SHA-256 the complete W3 v1 specification and examples; the T1 lease implementation, persisted-state loader, and every-stage restart test; the accepted E02 canonical W3 plan; the E05 lifecycle result, cleanup thresholds, stop-narrow decision, and limitations; the E06 worker/sandbox/session shape; and the T1 benchmark-v2 validator. Preserve every input read-only. Record that NS-1 through NS-5 are noncanonical ticket provenance and map the bounded evidence to AGENT-4, AGENT-5, EXEC-5, and the endpoint authorization portions of PLAN-5 and AGENT-6, with fake-target placement providing only limited EXEC-1 evidence.
3. Freeze the experiment boundary and workload. Two barrier-synchronised W3 subsets each request a Linux-class database/API service and consume Endpoint[API] from a fake-macOS target through project input that contains no port, route, token, writable path, provider option, or service process detail. Use only Python 3.9 standard-library subprocess, HTTP, sqlite3, Unix-socket, hashing, and unittest facilities inside experiment-owned temporary roots. No Docker, VM, simulator, external network, shared host service, production daemon, or real provider is assumed.
4. Freeze four candidate branches and mechanical precedence: stop/narrow on any hard safety failure; typed endpoint manager when one manager-owned transport-neutral routing mechanism passes every gate for both target classes without project-visible provider data; explicit provider routing when typed handles, lifecycle, and authorization pass but the fake-macOS path requires one declared provider route capability hidden behind an adapter; Compose-style integration only if an actually reachable and separately authorized Compose candidate passes the same gates after native lifecycle is rejected; otherwise stop/narrow. Command presence or a static Compose file earns no runtime credit, and no threshold may change after Phase B evidence exists.
5. Predeclare hard thresholds. Run 20 complete paired-namespace trials with zero collisions among generated service names, allocated ports, writable roots, database paths, endpoint IDs, lease IDs, route-capability IDs, or mutable objects; zero peer marker reads/writes; zero cross-namespace endpoint exposure; and zero fixed-port or provider-route values in project input. Run every wrong-type, foreign-consumer, forged-handle, missing-capability, stale-handle, and provider-mismatch denial at least 20 times with zero unauthorized successes, zero connection details or token bytes returned, denial before provider connection, and stable code plus endpoint, consumer, namespace, and policy diagnostics.
6. Freeze readiness, cleanup, reuse, and overhead gates. For 30 serial service starts, readiness is granted only after a successful health probe and a committed service.ready transition, never by a fixed sleep; p95 from process start through committed readiness must be strictly below 1 second, and unhealthy or exited services must fail closed and drain within 2 seconds. Use a 1 second lease TTL, 250 millisecond heartbeat, and 100 millisecond reaper interval for 20 abrupt caller-loss trials; expiry detection must be no more than 500 milliseconds late, and cleanup measured from expiry must have p95 at most 1 second and maximum at most 2 seconds, with zero remaining process, listener, route, lease, or mutable namespace path. Reuse one content-addressed immutable API artifact across both namespaces and a later namespace while sharing zero mutable database, token, process, or route state. Across 30 serial paired measurements, authorized endpoint resolution p95 must be below 25 milliseconds and the fake-macOS relay paired p95 overhead above direct loopback must be at most 10 milliseconds.
7. Freeze the observable state machine and evidence rules. Events follow the durable mutation they describe and carry monotonic sequence plus run, namespace, service, endpoint, consumer, lease, resource, provider, route decision, health transition, cleanup stage, collision count, latency, and diagnostic code where applicable. Route credentials are represented only by digests and never logged. Phase B must retain normalized raw JSONL per trial/fault/sample, process and listener inventories, namespace leak/collision reports, authorization matrix, readiness/cleanup records, T1 benchmark records and samples, environment/tool versions, checksums, implementation and evidence manifests, scorecard, limitations, recommendation, and exact reproduction command.
8. Verify Phase A with its contract-only Taskfile command and git diff --check, then stop and show the complete contract diff and threshold table. Obtain explicit authorization to commit the frozen contract before adding or running Phase B. Plan approval alone does not authorize service processes, listeners, Compose/container use, benchmarks, or evidence collection; any Compose or other shared runtime requires its own exact preflight and approval.

Phase B - implement and run only after the accepted contract commit

9. Implement an experiment-local narrow controller/reaper in a separate process, consistent with the E05 stop-narrow result. It owns namespace records, OS-assigned loopback ports, service child processes, typed endpoint handles, provider route adapters, heartbeats, leases, and cleanup obligations in an experimental SQLite WAL/FULL state file. Project-facing scenario code passes only typed handles and consumer identity. The controller authorizes endpoint type, namespace, consumer, policy, lease, and target-provider capability before issuing an opaque injected connection; the API validates the short-lived capability on every request.
10. Implement the two simultaneous W3 subsets with a tiny API child and separate SQLite database per namespace. Allocate ports with port zero inside the controller, derive private names/paths/IDs, synchronise the subsets at a barrier, interleave unique writes and reads, attempt peer path/name/endpoint access, and prove only the immutable service artifact digest is shared. After teardown, start a third namespace from the same artifact and prove no prior marker or mutable identity survives. Add state-based health success, slow-health, unhealthy, early-exit, and route-before-ready tests.
11. Exercise authorization and provider routing adversarially. Test the six frozen denial classes through the real controller and direct guessed loopback requests, ensure no denial path resolves or logs connection secrets, and run the authorized fake-macOS relay and direct-loopback control in alternating order. Keep provider route details inside the adapter; if target-specific options reach project input, the typed-manager branch fails rather than normalizing the leak. Record unavailable Compose as unavailable unless a separately approved real candidate is exercised.
12. Exercise caller-loss, TTL, and durable cleanup with real resources. Kill the client without cancellation while the controller remains; stop heartbeats, expire the namespace, revoke routes, terminate service processes, remove namespace mutable paths, and finalize leases. Inject a controller restart immediately before and after each of those four committed cleanup stages, reconstruct from disk in a fresh process, and prove exact-once ordered events plus idempotent completion, no port/process/path leak, and the frozen latency bounds. Preserve diagnostics outside the deleted mutable namespace root.
13. Run the frozen isolation, authorization, readiness, cleanup, reuse, and routing measurements once in their declared serial order, retaining all failed or invalidated sets. The only permitted rerun is a complete set restarted from sample one after a documented mechanical correction that does not change inputs, thresholds, or branch rules. Generate the scorecard mechanically, record limitations of same-host loopback, fake macOS, short test TTLs, local SQLite, and process-level rather than OS-network namespace isolation, then write docs/decisions/0011-e07-namespace-services.md with every roadmap ADR field, selected branch, unsupported cases, revisit triggers, and concepts that may inform Gate 1 without stabilizing any API or format.
14. Verify with mise exec -- task --dir experiments/e07-namespace-services check, mise exec -- task t1:w3:check, mise exec -- task t1:lifecycle-faults:check, repository-root mise exec -- task check, and git diff --check, using a ticket-scoped temporary cache where required. Append exact results and limitations to the ticket, then stop before checking AC or DoD, writing the Final Summary, setting Done, committing Phase B, merging, pushing, or cleanup. Present the full summary and diff and wait for the mandatory explicit approval checkpoint.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership and dependency record (2026-09-04):
- Agent: @codex-tf-003-15
- Branch: tf-003.15-e07-namespace-services
- Worktree: /private/tmp/taskflow-tf-003.15
- Base revision: 098035bf29656c3fd3b3991224a98fdded3453b7 (HEAD, main, and merge-base at planning start).
- Dependencies verified through Backlog CLI: TF-003.04, TF-003.05, and TF-003.13 are Done; no declared dependency is outstanding.
- Initially claimed paths: experiments/e07-namespace-services/**; docs/decisions/0011-e07-namespace-services.md; Backlog metadata for TF-003.15 only. The ADR path was coordinated with concurrent E06 and E08 owners, who claim 0010 and 0012 respectively.
- Read-only inputs: docs/** except the claimed E07 ADR; fixtures/w3/**; fixtures/t1-lifecycle-faults/**; experiments/e02-plan-ir/**; experiments/e05-daemon-simulation/**; experiments/e06-macos-feasibility/**; prototype/bootstrap/**. No shared fixture, production module, root task, or other ticket metadata is claimed.
- Planning-only boundary: no experiment code, contract, service/controller process, container, endpoint listener, benchmark, external network/provider action, shared resource mutation, ADR, commit, merge, push, or cleanup is authorized before explicit approval of the recorded plan.

Planning research (2026-09-04):
- The product specification defines no NS-1 through NS-5 identifiers. Retain those ticket labels as stale provenance only; map the live experiment to AGENT-4 and AGENT-5, EXEC-5, and the endpoint-authorization portions of PLAN-5 and AGENT-6, while treating per-node/fake-target routing as bounded EXEC-1 evidence rather than a real provider proof. Do not invent aliases.
- W3 v1 is a frozen specification, not a runtime. It requires two concurrent namespaces, distinct names/ports/writable roots/database paths/IDs, local authorized consumers, namespace-private routing, caller-loss reclamation, and clean reuse; its ports and event names are illustrative. E07 will bind it by digest and leave it unchanged.
- The lease fixture proves deterministic logical-time reclamation and fresh-manager reload across every before/after commit point, but not wall-clock TTL, real processes, ports, files, or atomic disk persistence. E07 must exercise those concrete resources without importing the fixture module or stabilizing its vocabulary.
- E05 selected stop-narrow: centralized ownership/durability worked, but a full shared daemon was not justified by the frozen scheduling benefit. E07 should therefore use the smallest separate experiment-local controller/reaper needed to survive client loss, not treat a broad daemon as an assumption. Its accepted cleanup bound is a useful comparable target: p95 at most 1 second and maximum at most 2 seconds after expiry.
- E06 has only an inventory/contract and explicitly leaves Linux-to-macOS routing to E07. The permitted branch-neutral consumer is therefore a fake macOS target adapter; this experiment will not start a VM or simulator or claim native-network proof.
- Python 3.9 standard-library HTTP, sqlite3, subprocess, Unix-socket, and unittest facilities are available, as are Backlog 1.44.0, Task 3.50.0, mise 2026.4.28, jq 1.7.1, Go 1.25.12, and Git 2.50.1. Docker/Compose is not assumed reachable and no external dependency or production Go module is required.

Phase A contract implementation (2026-09-04):
- Added the approved result-free E07 contract under experiments/e07-namespace-services/** only: README, Taskfile, contract, thresholds, decision matrix, event/evidence schema, 22 repository digest bindings, frozen artifact manifest and protocol digest, semantic verifier, and mutation tests. No E07 ADR exists yet.
- Froze two barrier-synchronised Linux-service/fake-macOS-consumer W3 subsets whose project-visible requests contain only source identity, Service[API], Endpoint[API], and consumer identity. Ports, routes, credentials, mutable paths, processes, and provider options remain controller-owned Phase B concerns.
- Froze the numeric gates from the approved plan: 20 paired isolation trials; six authorization denial classes with at least 20 repetitions each; 30 readiness samples with p95 below 1 second; 1 second TTL, 250 ms heartbeat, 100 ms reaper, expiry at most 500 ms late, and cleanup p95 at most 1 second/max 2 seconds; four cleanup stages with before/after restart cases; one immutable artifact digest/publication with zero mutable sharing; and 30 route samples with resolution p95 below 25 ms and relay delta at most 10 ms.
- Froze decision precedence across safety stop/narrow, typed endpoint manager, explicit provider routing, separately approved and actually exercised Compose integration, and no-credible-candidate stop/narrow. Phase A selects no branch and command presence or a static Compose file earns no credit.
- Frozen manifest digest: e2d77765a3b82cfad07996658536845563c65c3f6f0c8966bb606c97d700b3df. The verifier checks exact files, live repository bindings, requirements and scope, project-visible shape, every numeric threshold, decision rules, durable event ordering, token-redaction policy, required raw evidence paths, and the full hash envelope.
- Ten mutation tests pass, including rehashed threshold relaxation, project port leakage, premature branch selection, unapproved Compose credit, raw-token permission, missing raw trace, fixture drift, frozen-file drift, and premature controller code.

Phase A verification (2026-09-04):
- env GOCACHE=/private/tmp/taskflow-tf00315-phasea-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.15/experiments/e07-namespace-services check:phase-a: PASS; 10 tests and semantic verifier pass.
- env GOCACHE=/private/tmp/taskflow-tf00315-w3-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.15 t1:w3:check: PASS; 7 validator tests and canonical W3 validation pass.
- env GOCACHE=/private/tmp/taskflow-tf00315-lease-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.15 t1:lifecycle-faults:check: PASS; gofmt, vet, and tests pass.
- Root check passed prototype, W1, W3, benchmark, plan-conformance, lifecycle, and integrity gates, then the malicious-planner net-dial-loopback self-test could not create its throwaway 127.0.0.1:0 listener in the managed sandbox: bind: operation not permitted. It was not rerun outside the sandbox because this Phase A approval explicitly forbids starting listeners. This is an environment restriction, not an E07 contract failure.
- git diff --check passes for tracked changes. All 11 new files pass git no-index whitespace checking; the trailing-whitespace scan has no matches and no pycache was created.
- Phase A contains 11 new files and 1,099 lines plus this ticket update. No controller/reaper, listener, service, database, container, benchmark, evidence, decision branch, ADR, AC/DoD check, Final Summary, commit, merge, push, or cleanup was performed. Awaiting the mandatory contract diff review and explicit authorization before a Phase A commit or any Phase B work.

Root verification follow-up (2026-09-04):
- The root integrator reran `mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.15 check` outside the managed sandbox with approved local-loopback capability. It exited 0, including malicious-planner tests and all race gates.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Predeclared isolation, routing, readiness, and cleanup thresholds are committed first
- [ ] #2 Raw namespace event and leak/collision evidence is preserved
- [ ] #3 Task-specific fault checks and root verification pass
- [ ] #4 The E07 decision and Backlog final summary are current
<!-- DOD:END -->
