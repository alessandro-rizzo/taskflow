---
id: TF-003.07
title: Measure E01 ergonomics and record the authoring-schema decision
status: Done
assignee:
  - '@codex-tf003-07'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-03 21:45'
labels:
  - risk-lab
  - e01
  - decision
  - parallel-wave-r2
dependencies:
  - TF-003.06
references:
  - docs/roadmap.md#e01-typed-authoring-and-schema-ergonomics
  - docs/roadmap.md#21-decision-records
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E01 convergence; risks R1, R2, R10.
Outcome: select A/B, trigger the bounded TypeScript pivot, narrow the product, or stop based on measured authoring and agent behavior.
Stable assumptions: candidate implementations are disposable evidence and no SDK contract stabilizes at this decision alone.
Parallelism: runs after candidate outputs exist; may proceed beside E02 implementation. Own E01 measurements, independent-agent attempt evidence, and the E01 decision record.
Test or fault scenario: at least two independent agent attempts discover, edit, and invoke from schema alone without reading candidate implementation source.
Observability: compare W1 authoring size/clarity, compile-time misuse, schema completeness, cold/warm discovery, diagnostics, and annotation/generation/reflection burden.
Rollback or removal: rejected candidates remain documented; revisiting requires new evidence.
Versioned formats: record which candidate schema concepts, if any, are allowed to inform Gate 1.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Measurements compare every viable candidate against the same W1-W3 scope and T1 benchmark contract
- [x] #2 Warm discovery is evaluated against the 150 ms T1 budget without relaxing it merely because a candidate misses
- [x] #3 At least two independent agent attempts use schema alone and their success, failure, and source-access controls are preserved
- [x] #4 The decision chooses a roadmap branch with consequences, unsupported cases, revisit trigger, and Gate 1 inputs
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Freeze the TF-003.07 execution manifest before observing latency or agent results. Bind it to source ca87b81, the Phase A contract commit 1c88ddb, all four candidate evidence manifests, the W1-W3/effect output digests, the 56-line/10-concept control, the <=42-line/<=7-concept material threshold, the strict warm p95 <150 ms budget, the A/B 15% tie-break, exact sample counts, a deterministic candidate order, and rerun rules. Add verification that rejects missing, reordered, or post-result-adjusted measures.
2. Re-verify all A-D hard gates and independently audit the authored-region closure, concept ledgers, annotations, reflection, generator/generated lines, pinned dependencies, stale-output diagnostic, and source-mapped generator diagnostic. Preserve a candidate scorecard over the same W1-W3 plus synthetic-effect scope; do not alter candidate implementations or Phase A thresholds.
3. Build candidate-local measurement wrappers and use fixtures/t1-benchmark-harness schema v2 for every timing record. Run candidates serially with distinct temporary executable/cache roots: 30 warm W1 discovery samples with prewarm before every sample; 15 cold discovery samples; and 15 cold plus 15 warm driver build/type-check samples where applicable. Record literal preparation commands, source revision, machine/OS/toolchains, cache dimensions, all samples, median, p95, logs, and not-applicable reasons. If a warm p95 is within 10 ms of 150 ms, repeat complete sets in reverse order exactly as predeclared.
4. Create a candidate-neutral sealed trial bundle containing only the canonically shared schemas, invalid W1 input, invocation help, and a compiled fake interface—no candidate or repository source. Run two sequential, fresh, ephemeral Codex CLI sessions with the identical prompt inside a retained macOS Seatbelt profile that denies reads of the Taskflow repository while allowing only the sealed bundle for task files. Preserve the bundle digest, prompt, access-control preflight, JSONL transcripts, final answers, exit codes, elapsed times, commands, and automated assertions that each session identifies W3 output/capabilities, repairs and validates the W1 arguments, invokes the fake operation, reports typed outputs, and makes no blocked source-read attempt.
5. Apply the committed branch rules without weights or threshold changes. Record an ADR at docs/decisions/0005-e01-authoring-schema.md with the question/date, options, predeclared thresholds, raw evidence, chosen branch and why, consequences, unsupported cases, revisit trigger, and only the schema/SDK concepts permitted to inform Gate 1. Explicitly keep every experiment candidate disposable and avoid stabilizing a public SDK, schema version, plan IR, or production Go module.
6. Add experiment-level verification for benchmark-record validity, evidence completeness/digests, scorecard arithmetic, agent-trial controls/results, and ADR fields. Run the task-specific gate, mise exec -- task check from the repository root, formatting/JSON/diff/isolation audits, and self-review. Update ticket notes as evidence is produced, then stop before AC/DoD completion, Final Summary, Done, commit, merge, push, or worktree removal for the required user review checkpoint.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Ownership and dependency record (2026-09-03):
- Agent: @codex-tf003-07
- Branch: tf-003.07-e01-decision
- Worktree: /private/tmp/taskflow-tf-003.07
- Dependency TF-003.06 is Done and landed at ca87b81 after the separately committed Phase A contract 1c88ddb.
- Claimed paths: experiments/e01-authoring-schema/measurements/**; experiments/e01-authoring-schema/agent-trials/**; minimal experiment README/Taskfile/scripts changes needed to reproduce the measurements; docs/decisions/0005-e01-authoring-schema.md; and this ticket through Backlog CLI.
- No production package, root Go module, candidate implementation, fixture contract, or TF-003.08+ path is claimed.

Planning research (2026-09-03):
- Re-read docs/product-specification.md, docs/roadmap.md, TF-003.07, the committed E01 measurement contract, candidate manifests/counts/limitations, T1 benchmark v2 runner, and existing ADR 0003/0004 structure.
- Verified required local tools are available: Go/mise/Task/Bun, Codex CLI, jq/Python, and macOS sandbox-exec.
- The trial will use exactly two fresh agent sessions because the shared candidate schema is canonically identical; repeating it per candidate would manufacture a UX distinction the contract explicitly rejects.
- The external agent subprocesses will be OS-confined away from /Users/alessandro.rizzo/personal/taskflow; no collaboration agent will edit this ticket worktree.

Pre-result execution freeze (2026-09-03):
- protocol.json SHA-256: 4b77693a513a4fe7d74500f52eae5fccd339af59666ec3773a60d6946e4a02c2.
- Candidate source is bound to ca87b81869b3b22630a3772da481d5088b0816ef and Phase A to 1c88ddb.
- Deterministic primary order is C, D, B, A, derived by ascending SHA-256(source revision + colon + candidate id).
- Frozen sample sets: 30 warm discovery; 15 cold discovery; 15 cold and 15 warm driver build or semantic type-check. All run serially with candidate-specific cache paths.
- Any primary warm p95 within 10 ms of the strict 150 ms budget triggers 30-sample reverse-order warm sets; the conservative maximum p95 is scored. Otherwise there are no discretionary reruns.
- Frozen agent protocol: one shared source-free bundle, two sequential fresh ephemeral Codex sessions, identical prompt, macOS Seatbelt source denial, and two-of-two success.
- No benchmark or agent result had been collected when this digest was recorded.

Pre-measurement audit correction (2026-09-03):
- Candidate B evidence/manifest.json and evidence/count-manifest.json still declared runtime.go:E01-W1-HELPER-BEGIN..E01-W1-HELPER-END after TF-003.06 replaced that W1-specific helper with generic trace instrumentation. The markers no longer exist.
- This is stale measurement metadata, not a candidate-code or count change: project.go:E01-AUTHOR-BEGIN..E01-AUTHOR-END remains the complete author-maintained W1 region and still measures 21 lines.
- Expanded the claim only to those two Candidate B evidence JSON files for a compatibility correction, and will make the TF-003.07 audit reject any declared region whose file or markers do not exist. Phase A, candidate source, thresholds, counts, and protocol digest remain unchanged.

Permitted benchmark rerun (2026-09-03):
- The first primary C warm-discovery set ran 30 commands, but t1bench rejected the record before writing samples because managed-sandbox RAM auto-detection returned zero: hardware.ram_gib must be positive.
- Retained the failed command, validator error, exit code, and elapsed time under measurements/failures/primary-C-warm-discovery.{json,log}. No p95/median/sample record exists.
- Current hardware was read explicitly as Apple M5 Max, 18 cores, 64 GiB; sw_vers reports macOS 26.5.2 build 25F84, arm64. Unique device identifiers are not retained.
- The dated amendment permits one full restart with those fields passed explicitly. Candidate commands, preparation, caches, order, samples, thresholds, protocol digest, and decision rules are unchanged.

Agent-trial setup correction (2026-09-03):
- Both initial Codex requests failed before inference with HTTP 400 invalid_json_schema because the nested repaired_w1_arguments object was not strict. No task file was read, no tool command ran, and no agent answer was produced.
- Preserving both rejected requests, thread IDs, JSONL transcripts, exit codes, elapsed times, original bundle digest, and Seatbelt preflight under agent-trials/setup-failures/2026-09-03-invalid-response-schema/. They do not count as the required independent attempts.
- Corrected only response-schema.json by declaring additionalProperties false and the two legal W1 fields. Prompt, schemas, invalid input, fake interface, source denial, success criteria, and two-session requirement are unchanged. Two new ephemeral sessions will receive one identical corrected bundle.

Agent-trial nested-sandbox correction (2026-09-03):
- Both corrected-schema sessions reached inference but every shell command failed before execution because Codex workspace-write attempted sandbox_apply inside the enclosing Seatbelt sandbox. Both agents reported the infrastructure failure and neither inspected W3, validated arguments, or invoked the interface.
- Preserving these sessions under agent-trials/setup-failures/2026-09-03-nested-sandbox/. They do not count as usable trials.
- The rerun retains the preflighted outer Seatbelt source-denial boundary and uses Codex inner mode danger-full-access only to avoid nested sandbox installation. The enclosing profile continues to deny reads/writes to the primary checkout, every known Taskflow worktree, and Codex memory. Prompt, corrected bundle digest, tasks, and success criteria remain unchanged.

Measured E01 outcome (2026-09-03):
- Candidate audit independently recomputed authored regions and burdens, checked all common hard gates, canonical W1-W3/effect/trace equality, ten-process determinism evidence, body non-evaluation, compile/type failures, argument diagnostics, isolated dependencies, and exact TypeScript 5.9.3 lock.
- Sixteen accepted T1 benchmark v2 records preserve 300 serial samples. Warm discovery p95: A 10.814 ms; B 8.574 ms; C 6.703 ms; D 19.498 ms. All are strictly below 150 ms and none is within the 10 ms reverse-run trigger, so no reverse run occurred. Cold discovery and cold/warm build or type-check records are descriptive only.
- Two counted fresh ephemeral agents used identical sealed bundle dbc05e5d86ecf0237bbc0c8102ea46b0f91685bee9918908b65c123300746261. Both discovered W3, repaired all W1 faults, validated and invoked the fake interface, reported exact typed outputs, and made no source-read attempt. Counted times: 49.36 s and 44.26 s.
- Retained one rejected benchmark setup and two pairs of agent setup failures with dated corrections; none changed a candidate input, prompt, threshold, or success rule.
- Frozen branch result: B wins. Both A/B pass; B reduces A from 31 to 21 authored lines (32.3%, <=26 boundary) and four to three concepts (25%, <=3 boundary), while stale-output and source-mapped generator diagnostics pass. The 7 annotation lines, 171 generator LOC, 158 generated LOC, and one tag-reflection site remain explicit Gate 1 burdens.
- Proposed ADR 0005 permits B-style declaration generation to inform Gate 1 but stabilizes no contract and authorizes no production module. A remains the fallback if E02/E03 exposes plan or trust-boundary coupling.
- Reproducibility limitation recorded: the pre-result protocol digest bound inputs/order/states/samples/cache method/thresholds/reruns but not the runner script bytes; every accepted record retains literal commands, and all warm results have >130 ms margin. Future experiment protocols should hash runner bytes too.
Verification so far:
- PASS: env GOCACHE=/tmp/taskflow-tf00307-final-e01-gocache mise exec -- task --dir experiments/e01-authoring-schema check
- PASS outside managed sandbox: env GOCACHE=/tmp/taskflow-tf00307-final-root-gocache mise exec -- task check
- PASS: protocol, candidate-audit, trial, scorecard, evidence-manifest reproducibility, JSON, and diff checks.
- Ticket remains In Progress with AC/DoD unchecked, ADR proposed, and no Final Summary, commit, merge, push, or cleanup pending the required review checkpoint.

Review acceptance and landing (2026-09-03):
- User accepted the Candidate B recommendation, disclosed generator burden, retained failure evidence, compatibility correction, and full review diff.
- User explicitly authorized task completion, commit, merge to main, and push.
- AC and DoD are checked, Final Summary is recorded, and the ticket is Done pending the authorized serialized landing verification.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Measured all four E01 authoring/schema candidates under a frozen, digest-bound protocol and recorded Candidate B as the proposed Gate 1 direction.

Changes:
- Added 300 serial T1 v2 samples covering warm/cold discovery and driver build or semantic type-check behavior, plus independently verified candidate gates and burden counts.
- Added two successful source-confined schema-only agent trials and retained rejected benchmark and agent setup attempts with bounded amendments.
- Added ADR 0005 with the branch decision, consequences, unsupported cases, revisit triggers, and explicit prohibition on stabilizing production contracts before Gate 1.
- Added reproducibility checks for the protocol, candidate audit, trials, scorecard, evidence digests, and ADR; corrected Candidate B evidence pointers made stale by TF-003.06 without changing candidate code or counts.

Decision:
- Candidate B wins the frozen A/B tie-break with 21 versus 31 authored W1 lines and three versus four low-level concepts. Its generator burden remains explicit and must be resolved at Gate 1; Candidate A remains the fallback if E02/E03 exposes plan or trust-boundary coupling.

Verification:
- env GOCACHE=/tmp/taskflow-tf00307-final-e01-gocache mise exec -- task --dir experiments/e01-authoring-schema check
- env GOCACHE=/tmp/taskflow-tf00307-final-root-gocache mise exec -- task check
- JSON/JSONL validity, protocol and evidence digest reproduction, diff check, and credential scan
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 All predeclared E01 measures and agent trials have raw evidence
- [x] #2 Task-specific and root verification pass
- [x] #3 The E01 ADR follows every roadmap decision-record field
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
