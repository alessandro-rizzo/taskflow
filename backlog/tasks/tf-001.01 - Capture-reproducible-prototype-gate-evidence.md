---
id: TF-001.01
title: Capture reproducible prototype gate evidence
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:14'
updated_date: '2026-09-02 19:02'
labels:
  - milestone-t0
  - evidence
  - parallel-wave-t0
dependencies: []
references:
  - docs/roadmap.md#7-t0-repository-reset-and-prototype-baseline
  - prototype/bootstrap/docs/baseline.md
  - Taskfile.yml
parent_task_id: TF-001
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T0; gate G0; risks R1-R12 baseline.
Outcome: a timestamped, reproducible record of the isolated prototype toolchain and full formatting, race-test, test, and vet result.
Stable assumptions: the root-pinned environment delegates directly to the prototype Taskfile without nested mise.
Parallelism: safe with TF-001.02, TF-001.03, and TF-001.04. Own only the gate evidence file and any narrowly required command documentation.
Test or fault scenario: run the root prototype gate from a clean checkout and record failures verbatim.
Observability: capture OS, architecture, Go version, source revision, commands, result, and raw-output location.
Rollback or removal: documentation and raw evidence only; do not refactor the prototype.
Versioned formats: none.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Evidence records source revision, OS, architecture, Go version, exact commands, and timestamp
- [x] #2 Formatting, race tests, ordinary tests, and vet results are captured with a raw-output location
- [x] #3 The root and standalone reproduction instructions are checked for consistency
- [x] #4 Any failure is preserved as evidence and resolved or explicitly blocks G0
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. In the worktree, capture environment facts: source revision (git rev-parse HEAD), OS/arch (uname -a, sw_vers), Go version (go version), mise version, and a UTC timestamp.
2. Root reproduction: from repo root run `mise trust && mise install && mise exec -- task check` (delegates to prototype:check); capture full stdout/stderr to docs/evidence/t0/raw/root-check.log.
3. Standalone reproduction: from prototype/bootstrap run `mise trust && mise install && mise exec -- task check`; capture to docs/evidence/t0/raw/standalone-check.log. Compare against the root run and against prototype/bootstrap/docs/baseline.md's documented steps for consistency; note any drift.
4. Run the four gates individually for granular, per-gate evidence (the aggregate `check` task only wires fmt:check + test:race + lint, so plain `test` is captured separately to cover "ordinary tests"): `task fmt:check`, `task test`, `task test:race`, `task lint`. Save each raw output under docs/evidence/t0/raw/.
5. Write docs/evidence/t0/gate-evidence.md: source revision, OS, architecture, Go version, toolchain versions, timestamp, exact commands, per-gate pass/fail, links to raw-output files, the root/standalone consistency finding, and explicit measured-fact vs. limitation vs. open-question sections. Preserve any failure as evidence and mark it resolved or an explicit G0 blocker (no failures are currently expected based on the pre-check run in this session).
6. Update TF-001.01 acceptance criteria and Definition of Done checkboxes as each is satisfied; append implementation notes as work proceeds.
7. Re-run `mise exec -- task check` from repo root as final verification that adding only new docs/ files does not disturb the gate.
8. Add the Final Summary. Do not merge or push; landing requires explicit authorisation per AGENTS.md.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-001.01-prototype-gate-evidence
Worktree: /private/tmp/taskflow-tf-001.01 (created from main @ 9ddea88)
Claimed paths (new files only, no prototype edits): docs/evidence/t0/gate-evidence.md, docs/evidence/t0/raw/**

- Captured environment facts (revision 9ddea88, macOS 26.5.2/arm64, Go 1.25.12, mise 2026.4.28) to raw/environment.txt.
- Ran root-delegated `task check` and standalone `prototype/bootstrap` `task check`; both pass, and package/result sets are byte-identical after stripping timing/cache markers - root correctly delegates to standalone.
- Ran fmt:check, test (non-race, after go clean -testcache), test:race (after go clean -testcache), and lint individually for granular per-gate evidence; all pass, no formatting or vet findings, no failures to preserve.
- Wrote docs/evidence/t0/gate-evidence.md summarizing measured facts, limitations, and open questions, linking all raw logs.
- Re-ran root `task check` after adding only the new docs/evidence/ files; still green - no prototype semantics touched.
- No merge/push performed; landing awaits explicit authorisation per AGENTS.md.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added reproducible T0 gate evidence for the isolated prototype under docs/evidence/t0/, with no changes to prototype/bootstrap or any other existing file.

What was added:
- docs/evidence/t0/gate-evidence.md - the evidence record: source revision, OS/arch, Go/mise versions, timestamp, exact commands, per-gate pass/fail table, and a root-vs-standalone reproduction consistency check.
- docs/evidence/t0/raw/ - raw command output for environment capture, the root and standalone `task check` runs, and individual fmt:check/test/test:race/lint runs (test caches cleared before each test/race run so results reflect genuine execution, not Go's test cache).

Findings:
- Root (`mise exec -- task check`) and standalone (`prototype/bootstrap` `task check`) gates both pass at revision 9ddea88 on macOS/arm64, Go 1.25.12. The exercised package/result set is identical between the two runs, confirming the root command faithfully delegates to the standalone one and the documented reproduction steps (README.md, prototype/bootstrap/docs/baseline.md) are consistent with each other.
- No formatting, test, race, or vet failures were encountered, so there is no failure to preserve as a G0 blocker for this ticket's scope.
- Limitations recorded explicitly: single machine/OS/arch/toolchain snapshot; no CI-over-time claim; gate coverage is exactly what prototype/bootstrap/Taskfile.yml defines.

Tests/verification:
- mise exec -- task check (root), exit 0, before and after adding the evidence files.
- mise exec -- task check (prototype/bootstrap, standalone), exit 0.
- mise exec -- task fmt:check / test / test:race / lint (prototype/bootstrap), each exit 0.

Risks/follow-ups:
- This ticket only covers the gate-evidence acceptance criterion of T0; TF-001.02-.04 (inventory, W1 benchmarks, cache/concurrency characterisation) and TF-001.05 (G0 decision) remain to close the milestone.
- Not merged: branch tf-001.01-prototype-gate-evidence in worktree /private/tmp/taskflow-tf-001.01, awaiting explicit landing authorisation.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
