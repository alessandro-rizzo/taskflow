---
id: TF-002.04
title: Define the benchmark evidence format and runner
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:15'
updated_date: '2026-09-02 22:10'
labels:
  - milestone-t1
  - benchmark
  - harness
  - parallel-wave-t1a
dependencies:
  - TF-001
references:
  - docs/roadmap.md#8-t1-measurement-contracts-and-representative-fixtures
  - docs/roadmap.md#initial-budgets
parent_task_id: TF-002
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T1; workflows W1-W3; risks R4, R5, R6, R8; requirements PLAN-3, EXEC-4, REP-2, REP-4, DUR-1.
Outcome: a reusable experimental runner and result format that makes Risk Lab measurements comparable.
Stable assumptions: provisional budgets in roadmap section 8 are decision thresholds and may change only at G1 with evidence.
Parallelism: first-wave task, safe with TF-002.01 through TF-002.03. Own the measurement contract, runner, sample output, and raw-result layout.
Test or fault scenario: distinguish cold, warm, and cache-hit runs; reject missing metadata and invalid or partial samples.
Observability: record hardware, OS build, toolchain, source revision, experiment and fixture IDs, state, sample count, median, p95, raw results, and reservation count where applicable.
Rollback or removal: runner remains under experiment or harness ownership until Gate 1.
Versioned formats: benchmark result schema requires an explicit experimental version and validation.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Validated results require hardware, OS build, toolchain, source revision, fixture, experiment, state, and raw-result location
- [x] #2 The runner records sample count, raw samples, median, p95, and applicable reservation or lease counts
- [x] #3 Cold, warm, and cache-hit states have unambiguous preparation rules
- [x] #4 Automated tests reject incomplete metadata, malformed samples, and mislabeled run states
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Directory and module: fixtures/t1-benchmark-harness/ (own isolated go.mod, matching prototype/bootstrap's isolation pattern - not a root production module, per AGENTS.md/roadmap section 3 rule 2 and section 24 item 8). Location updated per coordinator decision (docs/roadmap.md commit 7d3c91f): fixtures/ now holds T1+ measurement fixtures/harnesses that multiple Risk Lab experiments reuse and that stay frozen once accepted (roadmap section 3 rule 3a), distinct from experiments/'s disposable spikes. Includes a README.md adapted from experiments/README.md's required contents (question, fixture, method, thresholds, limitations, recommendation, one verification command) for a harness rather than a hypothesis experiment.

2. Schema (record.go): a Go struct + JSON encoding, schema_version constant "taskflow-t1-benchmark/v1" (explicit experimental version per roadmap section 2.6 and 3a - no compatibility promise pre-G1). Required fields:
   - schema_version, experiment_id (T0/T1/E01-E08), fixture_id (references a frozen TF-002.01/.02/.03 fixture, e.g. "w1-fast-check@v1")
   - source_revision (git commit), timestamp
   - hardware {cpu, cores, ram_gib}, os {name, version, arch}, toolchain []{name, version} (array - a fixture may involve more than one toolchain, e.g. Go + Xcode for W3)
   - state: enum [cold, warm, cache-hit] - the primary cache dimension under test, satisfying roadmap section 8's literal "distinguish cold, warm, and cache-hit paths"
   - cache_dimensions: optional map[string]string (e.g. {"gocache": "warm"}) - captures secondary cache state so the exact ambiguity TF-001.03 flagged (GOCACHE warm vs TASKFLOW_DRIVER_CACHE cold both mattering) is representable, not lost, without overloading the single required `state` enum
   - samples []float64 (raw wall-clock seconds), sample_count int (redundant with len(samples), validated to match - catches silent truncation/tampering)
   - median, p95 float64 - present in the record but recomputed by the validator from raw samples and rejected if they don't match (protects against hand-edited or mislabeled results)
   - reservation_count *int - required specifically when state == "cache-hit" (directly serves the W1 budget: "zero worker reservations"), optional otherwise
   - raw_result_location: relative path string to raw sample/log files, mirroring the docs/evidence/t0/raw*/ pattern TF-001.01/.03/.04 already used ad hoc

3. Validator (validate.go): Validate(Record) error, with automated tests (validate_test.go) asserting rejection for: missing any required metadata field (one test per field, named errors); empty or non-numeric samples; sample_count mismatched with len(samples); state outside the 3-value enum; median/p95 not matching a fresh recomputation from samples; missing reservation_count when state == cache-hit. Plus a golden round-trip test for one fully valid record. This directly satisfies AC #4.

4. Runner (cmd/t1bench/main.go): a small CLI generalizing TF-001.03's ad hoc raw-w1-startup/benchmark.sh into reusable tooling - takes a command to time, a declared sample count, state, and required metadata as flags, runs the command N times (wall-clock, same method TF-001.03 used), computes median/p95, validates, and writes a JSON record plus raw sample file to a declared output directory. Cold/warm/cache-hit preparation is the CALLER'S responsibility (the runner cannot know what "cold" means for an arbitrary command); the runner requires --state to be declared explicitly and stamps it into the record, while the schema's cache_dimensions field lets the caller declare exactly which caches they cleared/warmed before invoking it - this is what makes preparation "unambiguous": the record states precisely what was and wasn't cleared, rather than trusting an implicit convention.

5. Verification: fixture-local `go test ./...` inside fixtures/t1-benchmark-harness (per repo policy, each experiment/fixture exposes its own verification command), plus root `mise exec -- task check` to confirm the new fixture does not affect the maintained prototype gate (task check only wires prototype:check today, so this should be a no-op check, but confirms no accidental interference).

6. Explicitly out of scope for this ticket: retroactively migrating TF-001.01/.03/.04's ad hoc T0 evidence into this schema (T0 evidence stays as-is; only TF-002.01/.02/.03 and later Risk Lab experiments are expected to adopt this format going forward), and evaluating any result against the roadmap section 8 budgets (no daemon/remote providers exist yet to measure most of them - this ticket only makes results comparable and validated, it does not gate anything against a budget itself).

Both the format (schema/validator, steps 2-3) and the runner (step 4) are in scope per the ticket title and AC - confirmed by coordinator, no split.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-002.04-benchmark-format
Worktree: /private/tmp/taskflow-tf-002.04 (created from main @ 71ab133)
Claimed paths (revised after research, not yet created): experiments/t1-benchmark-harness/** (own isolated go.mod, schema, validator, CLI runner, README, tests).

Research: read roadmap.md section 8 (T1 deliverables + initial budgets) and section 3 (repository layout/rules) in full. Read experiments/README.md - it mandates one-question-per-experiment dirs named by roadmap ID (eNN-slug); this ticket is a T1-deliverable harness, not a numbered Risk Lab experiment, so proposing experiments/t1-benchmark-harness/ as the directory name rather than an eNN- name. Read docs/evidence/t0/w1-startup.md (TF-001.03) as the concrete precedent this schema should formalize: it already had to track sample_count/median/p95/raw-samples/environment by hand, and its most important finding - that GOCACHE-warm vs TASKFLOW_DRIVER_CACHE-cold are two independent cache dimensions that both affect timing - is exactly the ambiguity a single cold/warm/cache-hit enum can't express on its own, which shaped the cache_dimensions field in the plan. No implementation yet; plan is written and awaiting user approval per AGENTS.md non-trivial-work review gate.

Coordinator relocated T1 harnesses to fixtures/ (docs/roadmap.md section 3 rule 3a, commit 7d3c91f) while this plan was mid-review; merged main into the branch, updated the plan to fixtures/t1-benchmark-harness/, and proceeded with full approval (both schema/validator and CLI runner in scope, no split, no T0 migration).

Implemented fixtures/t1-benchmark-harness/ (own isolated go.mod, module github.com/alessandro-rizzo/taskflow/fixtures/t1-benchmark-harness):
- record.go: Record schema, schema_version "taskflow-t1-benchmark/v1", State enum (cold/warm/cache-hit), optional CacheDimensions map.
- validate.go: Validate() + ComputeStatistics() (median/nearest-rank p95, method matches docs/evidence/t0/w1-startup.md exactly - verified against TF-001.03's actual raw cold-samples.txt as a known-answer test).
- validate_test.go: 15+ test cases covering every required-field rejection, malformed/non-finite/negative samples, sample_count mismatch, mislabeled median/p95, invalid state, and the cache-hit-requires-reservation-count rule; plus a JSON round-trip test and the known-answer statistics test.
- cmd/t1bench/main.go: CLI runner - times N samples of a shell command, auto-detects hardware/OS/toolchain (macOS + Linux), requires explicit --state/--cache-dim declarations, refuses to write anything if Validate() rejects the record.
- Taskfile.yml/mise.toml/README.md/.gitignore: fixture-local verification command and README per experiments/README.md's required contents, adapted for a harness.

Verified: gofmt clean, go vet clean, go test ./... passing (fixture-local), fixture's own `mise exec -- task check` (fmt+test+lint+build) green, and root `mise exec -- task check` unaffected (prototype gate still green, only fixtures/ added).

Smoke-tested the CLI directly: a real `sleep`-timed run produced a valid record.json+samples.txt; a state=cache-hit run without --reservation-count was correctly refused (exit 2, nothing written); the same run with --reservation-count 0 --cache-dim gocache=warm succeeded and the fields appeared correctly in the written record.

Reopened after independent Codex peer review found a High-severity functional gap plus several Medium ones:
- HIGH (AC #3 Not Met): state (cold/warm/cache-hit) is only a caller-declared label; the runner has no per-sample preparation/reset hook - it runs all N samples back-to-back after one setup step, so a nominally 'cold' run can contain 1 cold sample and N-1 warm ones and still validate successfully. This undermines the harness's core purpose.
- MEDIUM (AC #4 Partially Met): 'rejects mislabeled states' only rejects an invalid enum value, not a valid enum attached to wrongly-prepared measurements.
- MEDIUM (AC #1 Partially Met): os.version records sw_vers -productVersion (e.g. 26.5.2), not OS build, which the AC specifically asks for.
- MEDIUM (AC #2 Partially Met): no lease_count field despite W3/E05/E06/E07 being lease-oriented; reservation_count is caller-supplied (acceptable by design) but undocumented as such.
- MEDIUM: record.go's FixtureID doc comment example (w1-fast-check@v1) does not match the real fixtures/w1 identity (fixture_id: w1-fast-project-check, version: t1-experimental-v1) - a concrete cross-fixture drift the validator cannot catch since it only checks FixtureID is non-empty.
Branch: tf-002.04-benchmark-format-fixes, worktree: /private/tmp/taskflow-tf-002.04-fix, claimed paths: fixtures/t1-benchmark-harness/{record.go,validate.go,validate_test.go,cmd/t1bench/main.go,README.md} (fix-only edits, no other files). Plan: add a required --prepare command run before every sample (untimed) plus a PreparationCommand record field capturing it; add os.build (sw_vers -buildVersion / uname -v) as a new required field; add optional lease_count; fix the FixtureID example; bump CurrentSchemaVersion to v2 since new required fields are a breaking change per the package's own stated versioning policy.

Fixes applied and verified: --prepare is now a required flag run before EVERY sample (not once before the batch), captured verbatim in a new required preparation_command field; added required os.build (sw_vers -buildVersion / uname -v, distinct from os.version); added optional lease_count; fixed the FixtureID doc-comment example (was w1-fast-check@v1, now describes matching a fixture's real declared fixture_id); bumped CurrentSchemaVersion to v2 (breaking change per the package's own stated policy). Smoke-tested the CLI end-to-end: confirmed --prepare actually runs once per sample (5 samples -> 5 prepare invocations via a counter file), confirmed missing --prepare is now rejected, confirmed os.build reports a real macOS build number distinct from os.version. go build/vet/gofmt/test all pass; root mise exec -- task check green.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Follow-up to the independent Codex peer review of the T1 wave-1 batch, which found a High-severity functional gap plus several Medium ones in the benchmark harness - the most consequential of the four wave-1 tickets since every future T1/Risk Lab benchmark is expected to use it.

Fixed:
- HIGH (AC #3, was Not Met): state (cold/warm/cache-hit) was only a caller-declared label with no per-sample preparation hook - a "cold" N-sample run could contain 1 cold sample and N-1 silently-warm ones and still validate. Added a required --prepare shell command that t1bench now runs, untimed, before EVERY sample (not once before the batch), and captures its exact text in a new required preparation_command field. Smoke-tested end-to-end: 5 samples produced exactly 5 --prepare invocations (verified via a counter file); omitting --prepare is now rejected at the flags-missing stage.
- MEDIUM (AC #1): added required os.build (macOS sw_vers -buildVersion, e.g. "25F84"; Linux uname -v), distinct from the existing os.version (product version) - T1's evidence-to-capture list asks for OS build specifically.
- MEDIUM (AC #2): added optional lease_count (non-negative when present) for W3/E05/E06/E07-style lease-oriented workloads.
- MEDIUM: fixed record.go's FixtureID doc-comment example, which referenced a nonexistent "w1-fast-check@v1" not matching fixtures/w1's real declared identity (w1-fast-project-check); now documents that FixtureID should match a fixture's own declared fixture_id.
- Bumped CurrentSchemaVersion to "taskflow-t1-benchmark/v2": the new required fields (os.build, preparation_command) are a breaking change per the package's own stated pre-Gate-1 versioning policy, not a silent addition.
- validate_test.go: updated the shared valid-record fixture for the new required fields; added TestNegativeLeaseCountRejected and TestLeaseCountOptionalAcrossAllStates; the existing metadata-rejection table gained os.build and preparation_command cases. README.md updated throughout to describe the new mechanism and its residual limitation (preparation is now consistently re-applied and auditable, but its semantic correctness for an arbitrary shell command still cannot be verified).

Verified: go build/go vet/gofmt -l/go test all pass (fixture-local); a real t1bench binary was built and run end-to-end against a throwaway command, confirming --prepare's per-sample execution and os.build detection empirically, not just via unit tests; root mise exec -- task check green.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
