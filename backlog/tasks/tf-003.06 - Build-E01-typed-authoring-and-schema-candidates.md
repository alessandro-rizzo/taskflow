---
id: TF-003.06
title: Build E01 typed-authoring and schema candidates
status: In Progress
assignee:
  - '@codex-tf003-06'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-03 17:54'
labels:
  - risk-lab
  - e01
  - authoring
  - schema
  - parallel-wave-r1
dependencies:
  - TF-003.01
  - TF-003.02
references:
  - docs/roadmap.md#e01-typed-authoring-and-schema-ergonomics
  - fixtures/w1/
  - fixtures/w2/
  - fixtures/w3/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E01; risks R1, R2, R10; requirements TYPE-1 through TYPE-6 and API-1.
Outcome: disposable, directly comparable authoring candidates demonstrate whether a concise typed project API can emit complete agent-discoverable schema without privileged operation evaluation.
Competing options: generic typed values with explicit registration; code generation from Go declarations; reflection-heavy registration; and a minimal TypeScript comparison emitting the same provisional schema.
Stable assumptions: T1 fixture identities and harness outputs are experimental; execution may be fake; no candidate becomes a production SDK or root module.
Parallelism: first Risk Lab implementation wave after conformance/root-gate hardening. Own experiments/e01-authoring-schema only.
Test or fault scenario: W1, W2 build/artifact half, W3 public surface, compile-time incompatible value misuse, invalid arguments, and schema discovery without invoking operation bodies.
Observability: record authored lines, visible concepts, annotations/reflection/generated code, build/discovery latency, and diagnostics per candidate.
Rollback or removal: retain candidates as evidence until the E01 decision; no production consumers.
Versioned formats: candidate schema envelopes and generators declare experimental versions.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 All four roadmap candidate approaches emit comparable provisional schema for the scoped W1-W3 surfaces or record a reproducible infeasibility result
- [ ] #2 Typed Go candidates include compile-fail fixtures for incompatible artifact or endpoint composition
- [ ] #3 Schema exposes arguments, defaults, enums, outputs, effects, and capabilities without running privileged operation bodies
- [ ] #4 Each candidate is isolated, imports neither prototype/bootstrap nor another experiment as a production dependency, and has one verification command
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Phase A - freeze the decision contract before candidate code

1. Create only the experiment-level measurement contract and neutral comparison fixtures under experiments/e01-authoring-schema/: a scope manifest for W1, the W2 build/artifact half, and the landed W3 v1 public surface; an experiment-local low-level W1 control with the same observable surface; the exact counting rules; candidate isolation rules; benchmark preparation commands; diagnostic cases; and continue/pivot/stop thresholds. Freeze a candidate-neutral W1 logical authoring shape comprising one typed source, format/test/lint child work, the required test report from test, aggregate check dependencies, and optional aggregate diagnostics. Bind W3 to the final v1 schema golden plus the v1 specification and canonical namespace examples. The contract will invoke the conformance harness externally; it will not define production Go types, import the prototype, or provide a shared candidate library.
2. Add one narrow, explicitly synthetic effect probe to the contract because all three frozen W1-W3 schema goldens correctly declare empty required_effects, while AC #3 requires positive proof that effect metadata is discoverable. The probe must expose a required enum argument, an Effect[PublishedRelease] output, a non-empty effect request, and capability metadata; it remains E01-only and cannot become a production contract.
3. Make the experiment-local low-level W1 control syntactically parseable and gofmt-stable while keeping its placeholder types deliberately undefined so it cannot become a fifth candidate. Count only the marked author region after formatting, and freeze the resulting LOC and concept ceilings in the verified manifest.
4. Stop after the Phase A files are reviewed. Present their full diff and threshold table, obtain explicit approval, and commit that contract separately. Do not create any candidate implementation, generated file, or candidate output until the Phase A commit exists. This separate mid-ticket checkpoint is required by DoD #1.

Predeclared thresholds and protocol to encode in Phase A

5. Apply hard comparability gates to every viable candidate: emit deterministic W1, W2, and W3 schema documents with zero T1 conformance violations or diffs; emit the same synthetic effect probe; author the exact W1 logical composition with typed fake handles/nodes and emit a candidate-local normalized trace matching the Phase A oracle; produce byte-identical repeated output across 10 fresh processes; never read a golden as generation input; and expose one candidate-local verification command. A schema-only descriptor plus a panic sentinel cannot satisfy the W1 gate. Run candidate generation from its standalone subtree and compare outputs only after generation.
6. Apply safety and typing gates equally to A, B, C, and D: discovery and argument validation must not invoke operation bodies, proved by panic/file-write sentinels across 10 fresh processes; every candidate must type-check positive controls and fail semantic type checking for both incompatible Artifact[T] and incompatible Endpoint[T] composition; and every candidate must reject unknown arguments, wrong scalar types, invalid enum members, and a missing required synthetic-effect argument with stable operation/argument/expected/actual diagnostics. Go candidates use the pinned Go compiler. TypeScript D uses a candidate-local pinned semantic checker; Bun transpilation alone is not accepted, and unavailable reproducible installation is recorded as infeasibility.
7. Predeclare the E01 decision measures for TF-003.07. Warm W1 discovery uses the T1 benchmark v2 harness with 30 samples, a prewarmed executable/runtime before every sample, explicit cache dimensions, and an unchanged p95 threshold below 150 ms. Cold and warm driver build/discovery use 15 samples each and are recorded without inventing a new pass budget. Ergonomics use mechanically counted non-comment, non-blank user-authored LOC and a frozen vocabulary of author-visible concepts; the headline count must cover every author-maintained line needed for the complete W1 logical shape, including source acquisition, format/test/lint children, typed dependency flow, aggregation, and outputs. A non-reflection Go candidate must use at least 25% fewer user-authored W1 LOC and at least 30% fewer low-level concepts than the Phase A control. Record annotations/directives/tags, reflection sites, generator LOC, generated LOC, and duplicated metadata separately rather than hiding them in the headline count. No candidate may satisfy the threshold by embedding a hand-written schema document or excluding its authored fake composition.
8. Predeclare two blinded agent trials for TF-003.07 against a candidate-neutral sealed bundle containing emitted schema and invocation help but no implementation source. Both independent attempts must discover the requested W3 operation, repair invalid W1 arguments, and invoke the fake operation interface without source reads; preserve prompts, outputs, access controls, and failures. If the outputs are canonically identical, the trials test the shared schema once rather than pretending identical documents are independent UX variants.
9. Encode branch rules without relaxing a missed threshold: continue only if A or B passes every hard gate plus the ergonomic and warm-discovery thresholds; A wins on explicit registration evidence, B wins only if generation burden and stale-output diagnostics remain acceptable; if only reflection-heavy C works, stop and redesign; if Go candidates fail but the shared schema and D succeed, hand TF-003.07 the bounded TypeScript-pivot branch; if typed authoring shows no material gain over the control, narrow or stop.

Phase B - implement disposable candidates only after the Phase A commit

10. Build four standalone candidates under disjoint subdirectories with no imports from prototype/bootstrap, fixtures, or another candidate and no shared implementation framework: A uses generic typed values plus explicit typed operation descriptors; B uses generic values plus a candidate-local standard-library Go AST/type generator from declarations, comments, and tags; C registers typed function values and derives schema primarily through reflection plus minimal metadata; D is a minimal TypeScript descriptor comparison that emits the same provisional documents and carries the same positive/negative type-composition fixtures. Each candidate must express the frozen W1 source-to-format/test/lint-to-check composition through its own typed fake handles/nodes; fake execution and normalized evidence do not establish E02 plan semantics. Candidate D must pin its TypeScript checker locally; Bun transpilation alone is not accepted as type checking, and unavailable dependency installation becomes a reproducible infeasibility result rather than a silent downgrade.
11. Give each candidate its own module/package metadata, source definitions for the identical four-document scope, isolated tests, positive and compile/type-fail harnesses, W1 normalized logical-trace test, output directory, limitations note, exact counted authored path/range closure, and task check. A top-level experiment Taskfile may orchestrate independent commands and cross-compare already-emitted JSON, but it must contain no reusable authoring/schema implementation.
12. Retain Phase B smoke evidence under the experiment: emitted schema documents, conformance and compile/type-fail logs, body-sentinel results, W1 logical traces, exact commands, tool versions, and candidate limitations. Provide measurement commands and manifests for TF-003.07, but leave comparative benchmark execution, blinded agent attempts, scoring, and the E01 branch decision to TF-003.07.
13. Verify each candidate command, the experiment-level check, mise exec -- task check from the repository root, and git diff --check. Update implementation notes, then stop before AC/DoD completion, Final Summary, Done, or commit for the required review checkpoint.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Ownership and dependency record (2026-09-03):
- Agent: @codex-tf003-06
- Branch: tf-003.06-e01-candidates
- Worktree: /private/tmp/taskflow-tf-003.06
- Claimed paths: experiments/e01-authoring-schema/** plus this ticket through Backlog CLI.
- TF-003.01 and TF-003.02 are Done. The worktree was rebased onto landed main bdd9655 after TF-003.04 and TF-003.05 completed.
- No candidate implementation may begin until the Phase A contract is separately reviewed, approved, and committed.

Planning and scope:
- W1 is the full schema and ergonomics workload; W2 is limited to its build/artifact public surface; W3 is limited to its v1 public operation plus typed endpoint composition. Plan IR and execution remain outside E01.
- Added an E01-only synthetic effect probe because all W1-W3 schema goldens correctly contain empty effect arrays and cannot positively demonstrate existing AC #3 alone. The probe is not a fourth roadmap workflow or production contract.
- Candidate A is explicit typed Go registration; B is candidate-local Go AST/type generation; C is reflection-heavy Go registration; D is a minimal TypeScript comparator. Candidates must be standalone and may not import/read another candidate, the prototype, or fixtures as implementation dependencies.
- Available tools: Go/gofmt 1.25.12, Node 25.9.0, npm 11.12.1, Bun 1.3.13, jq 1.7.1, Python 3.9.6, mise 2026.4.28, and Task 3.50.0. TypeScript D must add one candidate-local pinned semantic checker; Bun transpilation alone cannot pass.

Phase A review state:
- Added seven files only under experiments/e01-authoring-schema: README, measurement contract, Taskfile, hash-bound targets manifest, synthetic effect schema, low-level W1 control, and Phase A verifier.
- The control is syntactically parseable and gofmt-stable but retains undefined placeholder types so it is not runnable candidate code. Its marked author region is 56 non-comment/non-blank lines; the 25% reduction ceiling is 42. Its ten named low-level concepts produce a seven-concept ceiling.
- Hard gates cover exact W1-W3 conformance, the positive effect probe, canonical cross-candidate equality, ten-process byte determinism, ten-process body sentinels, four argument diagnostic classes, isolation, and derived-not-copied schema.
- Typed composition applies equally to A/B/C/D: positive controls must type-check, while incompatible Artifact and Endpoint cases must fail with fixture/type context. TypeScript uses its pinned checker; unavailable locked installation is preserved as reproducible infeasibility.
- TF-003.07 measurement rules are frozen at 30 warm discovery samples against the unchanged p95 <150 ms budget, 15 descriptive cold/warm samples, two blinded schema-only agent attempts, and explicit A/B/C/TypeScript/stop branches. A is preferred when B is within 15%; B wins at floor(A * 0.85) on either headline metric plus stale-generation and source-diagnostic gates.
- Review choices left deliberately visible before the Phase A commit: ergonomic threshold strictness, A/B tie-break, positive-effect interpretation, and one shared-schema versus per-candidate agent trials.
- Landed W3 v1 is fully aligned. The manifest verifies v1 plus these final bindings:
  - W1 schema golden: 9b08663477b30b981a68ed3f07ea76da9e3e8ae84ff05088ac1156394e4c3878
  - W2 schema golden: 877bfcfabfcb7ca9875c55ee913adcb6602c39fe14da5691292a010a6945b9c3
  - W3 schema golden: ff1d00afc5bf4d284a78db336e8d8d74c2e80ee95614a792321904af2be5f761
  - W3 spec: 3604c22837c2c377309c4d08b1eeeef862fc150b195b545bed981745fd1768a4
  - W3 namespace A: b86378913dcc9f7e80c8a47cf387f68a5a6130d88592fa02be72229e85a3583e
  - W3 namespace B: 22db508434c6997cc8ec4bd9b0dee4bc0a01181380ca196e0abb875c865ab682
- The verifier checks the landed W3 schema envelope, v1 version in spec and both namespace records, and every bound digest. This ticket did not modify frozen fixtures.

Verification:
- PASS: env GOCACHE=/tmp/taskflow-tf00306-phasea-gocache mise exec -- task --dir experiments/e01-authoring-schema check
- Synthetic effect probe conformance digest: c43e18381886824a526f6015c80c458f119f7395dedb2d2efef818663479dc30
- PASS outside the managed sandbox: env GOCACHE=/tmp/taskflow-tf00306-root-gocache mise exec -- task check
- PASS: jq empty for both Phase A JSON files; gofmt -d is empty for the control; trailing-whitespace audit; candidate/module/dependency absence audit; git diff --check.
- No A/B/C/D source, modules, generated candidate outputs, dependency locks, or shared candidate framework exist.
- Stopped uncommitted and In Progress before AC/DoD checks, Final Summary, candidate implementation, Done, merge, push, or cleanup.

W1 comparability correction (2026-09-03):
- Frozen a candidate-neutral W1 fake authoring oracle in targets.json: typed source -> format/test/lint children -> aggregate check, with a required Report[GoTests] from test, optional Report[Diagnostics] from check, and six typed-handle relations.
- Phase B now requires A/B/C/D to author that exact shape with their own typed fake handles/nodes and prove it using a candidate-local normalized trace. Execution remains fake; this does not define E02 plan IR.
- Headline authored LOC must close over every project-maintained line for source acquisition, child work, typed dependencies, aggregation, and exposed outputs. A schema-only descriptor plus an uncounted/absent body and panic sentinel cannot pass.
- The Phase A verifier freezes the exact manifest oracle, rejects logical shapes on W2/W3, checks W1 exposed outputs against the landed schema golden, and verifies that the low-level control contains the source, three child consumers, test-report production, aggregate dependencies, and aggregate outputs.
- PASS: env GOCACHE=/tmp/taskflow-tf00306-phasea-gocache mise exec -- task --dir experiments/e01-authoring-schema check.
- PASS: env GOCACHE=/tmp/taskflow-tf00306-root-gocache mise exec -- task check.
- PASS: JSON parse, gofmt stability, trailing-whitespace absence, candidate/module/dependency absence, and git diff --check.
- Phase A remains uncommitted and In Progress with no candidate source, generated candidate output, dependency lock, or candidate framework.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Predeclared E01 thresholds and measurement method are committed before candidate implementation
- [ ] #2 Task-specific and root verification pass
- [ ] #3 Raw outputs and candidate limitations are preserved under the experiment
- [ ] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
