---
id: TF-002.02
title: Freeze the W2 cross-target artifact fixture
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:15'
updated_date: '2026-09-02 22:10'
labels:
  - milestone-t1
  - w2
  - fixture
  - parallel-wave-t1a
dependencies:
  - TF-001
references:
  - docs/roadmap.md#w2-cross-target-artifact-pipeline
  - docs/roadmap.md#8-t1-measurement-contracts-and-representative-fixtures
parent_task_id: TF-002
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T1; workflow W2; risks R4, R5, R9, R12; requirements EXEC-1, EXEC-4, REP-1, REP-3, REP-6, DUR-2, DUR-3.
Outcome: a frozen cross-target pipeline fixture covering Linux build, verified artifact transfer, tests, local inspection, failure, cancellation, and resume.
Stable assumptions: fixture placement and artefacts express required semantics without selecting a provider protocol.
Parallelism: first-wave task, safe with TF-002.01, TF-002.03, and TF-002.04. Own only the W2 fixture subtree and its specification.
Test or fault scenario: corrupt artifact, downstream failure, worker loss, provider outage, cancellation, and resume on a compatible worker.
Observability: expected profile identity, manifest digest, lifecycle events, repeated-work count, and durable state are declared.
Rollback or removal: fixture is independently removable and provider-neutral.
Versioned formats: fixture manifest and golden expectations declare experimental versions.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The fixture represents build, test-report, artefact transfer, and local inspection edges
- [x] #2 Expected profile and artifact identities are known before placement and digest verified
- [x] #3 Failure, cancellation, provider outage, worker loss, and compatible resume cases are specified
- [x] #4 Assertions detect repeated successful work and unexplained durable-state loss
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Propose fixtures/w2/ at repo root (parallel to experiments/ and prototype/) as the frozen-fixture location - flagged to the user as a cross-ticket naming convention that should also govern TF-002.01 (W1) and TF-002.03 (W3), since AGENTS.md forbids independently inventing shared conventions from parallel tickets. Do not finalize the path without confirming it covers all three.
2. Write fixtures/w2/README.md: restates the roadmap W2 shape (source -> Linux build -> Artifact[BackendBinary] -> Linux tests -> Report[GoTests] -> local package/inspection) and required properties, then maps each required property to how this fixture exercises it (profile identity known before placement; artifact manifest digest verified across targets; failed downstream node resumes on a compatible worker; successful work not repeated; provider outage/cancellation produce durable, explainable state).
3. Write fixtures/w2/graph.json (or .yaml): the frozen, provider-neutral node/edge graph - abstract target roles (e.g. "linux-build", "linux-test", "local-inspect"), declared expected typed outputs (Artifact[BackendBinary], Report[GoTests]), and an explicit experimental schema version field (this ticket's own contract; does not preempt or duplicate TF-002.04's benchmark-result format).
4. Write fixtures/w2/golden/ - one file per required fault/behavior scenario named in the AC (corrupt artifact, downstream failure + resume on compatible worker, worker loss, provider outage, cancellation), each specifying the expected lifecycle-event sequence, durable-state outcome, and repeated-work assertion in a structured, testable form. These are specifications for TF-002.06/TF-002.07 (lifecycle and integrity fault-injection fixtures, second wave) to execute against - this ticket does not itself build fault-injection machinery or touch prototype code, to avoid duplicating those tickets' ownership.
5. Explicitly document, in the README, what is buildable now (graph + golden specification) versus deferred (actual fault-injection execution, which needs TF-002.06/.07's injectors and, later, a real implementation to run the fixture against - none of which exist yet).
6. Verify mise exec -- task check remains green (fixture is pure new files under fixtures/w2/, no prototype/production code touched).
7. Update AC/DoD, append notes, final summary; do not merge without landing authorisation.

STOP after step 1-5 are drafted and this plan is written - do not begin implementation until the user approves, per AGENTS.md's non-trivial-work approval gate. In particular, the fixtures/ directory naming (step 1) should be confirmed against whatever TF-002.01 and TF-002.03 land on.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-002.02-w2-fixture
Worktree: /private/tmp/taskflow-tf-002.02 (created from main @ 71ab133)
Claimed paths: fixtures/w2/** (proposed location - not yet an established repo convention, see report to user)

Research: roadmap.md W2 shape/required-properties (section 4) and T1 deliverables/budgets (section 8) reviewed. prototype/bootstrap/engine/runtime_integration_test.go:103 TestRuntimeTransfersDependencyArtifactsAcrossTargets shows the prototype's own cross-target pattern (two target/ssh.Provider instances + flow.Builder Needs/On/Outputs) - useful as a reference shape, not to be reused directly, since TF-001.02's inventory (docs/evidence/t0/package-inventory.md) classifies target/ssh as a deliberately crude, non-production spike. The frozen W2 fixture must describe targets/edges abstractly (no ssh/local selection) per this ticket's stable-assumption field.

Coordinator confirmed fixtures/ as the shared top-level location (docs/roadmap.md section 3 rule 3a, pushed at 7d3c91f) - matches this ticket's proposal. Merged main into the branch, reran mise exec -- task check (green). Implemented the plan as drafted: fixtures/w2/README.md, graph.json (schema t1-w2-experimental-v1), golden/{corrupt-artifact,downstream-failure-resume,worker-loss,provider-outage,cancellation}.md. Verified graph.json is valid JSON and mise exec -- task check remains green (fixtures/ is new files only, no prototype/production code touched).

Reopened after independent Codex peer review. AC #2 rated Partially Met: graph.json declares capability requirements and says profile identity must be known before placement, but supplies no concrete profile ID/digest/version or attestation expectation, and the artifact manifest schema requires a digest field without ever showing a concrete example digest to check against - an aspiration, not a verifiable golden identity. Also: graph.json's fixture_schema_version key should be renamed to version to match the fixtures/w1 and fixtures/w3 convention.
Branch: tf-002.02-w2-fixture-fixes, worktree: /private/tmp/taskflow-tf-002.02-fix, claimed paths: fixtures/w2/graph.json (fix-only edits, no other files).

Fixes applied and verified: graph.json's fixture_schema_version renamed to version (matches w1/w3 convention); added example_profile (illustrative id/digest/attributes) to each target and a concrete example artifact digest on the build node's manifest, clearly labeled as hand-authored example data. README.md updated to match. JSON validity confirmed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Follow-up to the independent Codex peer review of the T1 wave-1 batch. AC #2 was rated Partially Met: the fixture declared that profile/artifact identity must be known before placement, but supplied no concrete example to check against - an aspiration, not a verifiable golden identity.

Fixed:
- graph.json: renamed fixture_schema_version -> version to match fixtures/w1 and fixtures/w3's convention (a cross-fixture consistency gap the review also flagged).
- graph.json: added an example_profile block (illustrative id/digest/attributes) to each target, and a concrete example artifact digest/size/produced_by fields on the build node's manifest - explicitly labeled as hand-authored example data (no real provider exists yet to attest one), showing the shape a golden check can compare against once E04/E08/T3/T6 supply a real provider.
- README.md updated to reference the renamed version key.

Verified: JSON validity confirmed (python3 json.load); root mise exec -- task check green (no Go code touched).
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
