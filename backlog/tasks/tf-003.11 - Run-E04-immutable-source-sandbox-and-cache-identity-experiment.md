---
id: TF-003.11
title: Run E04 immutable-source sandbox and cache-identity experiment
status: Done
assignee:
  - '@tf003-11-agent'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-04 14:37'
labels:
  - risk-lab
  - e04
  - reproducibility
  - cache
  - parallel-wave-r1
dependencies:
  - TF-003.01
  - TF-003.03
references:
  - docs/roadmap.md#e04-immutable-source-lightweight-sandbox-and-cache-identity
  - fixtures/integrity-faults/
  - fixtures/w1/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E04; risks R4, R5, R9; requirements SRC-1 through SRC-5, EXEC-1 through EXEC-4, CACHE-1 through CACHE-6.
Outcome: determine whether immutable source, disposable local workspaces, declared profiles, and cache identity before provisioning can meet isolation and local-latency requirements.
Approaches: Merkle/CAS source plus copy-on-write workspace; Linux namespace/overlay; macOS APFS clone/sandbox controls; pooled container or microVM fallback.
Stable assumptions: result cache, tool cache, and warm worker state are distinct; attained reproducibility is stated explicitly.
Parallelism: first Risk Lab wave independent of E01. Own experiments/e04-source-sandbox-cache only.
Test or fault scenario: post-creation source mutation, concurrent W1 outputs, undeclared paths/env, complete identity inputs, ready cache hit, mismatched profile attestation, and cache-state separation.
Observability: source/definition/profile/policy/dependency digests, reservation count before and after lookup, sandbox latency, file-access denials, and attestation mismatch.
Rollback or removal: all mechanisms remain experiment-local; preserve failures and remove privileged resources after bounded cleanup.
Versioned formats: source manifest, profile, and cache evidence formats are experimental.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Execution consumes immutable source bytes unaffected by later worktree mutation and concurrent W1 runs cannot observe each other’s writable outputs
- [x] #2 Undeclared paths and environment values are denied or detected at the claimed reproducibility level
- [x] #3 Cache identity covers source, inputs, process, profile, policy, and dependency manifests before any worker reservation
- [x] #4 A ready hit performs zero reservations, mismatched attestation fails closed, cache classes remain distinct, and local warm sandbox p95 is evaluated against 250 ms
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create and freeze a Phase A experiment contract under experiments/e04-source-sandbox-cache before mechanism code. Add an experiment README, machine-readable contract, contract verifier, and Taskfile entry that declare E04/R4/R5/R9 and map the seven demonstrations to the canonical current requirements REP-1 through REP-6 plus the directly exercised worker/sandbox and profile-attestation requirements EXEC-3 and EXEC-4. Record the ticket description's SRC-1..SRC-5 and CACHE-1..CACHE-6 labels as stale, undefined compatibility metadata; do not create aliases or freeze them into a contract. Bind the frozen W1 and integrity-fixture versions/digests; define requested versus attained reproducibility labels; and forbid imports from prototype/bootstrap or fixtures. Predeclare 30 serial samples per decision metric, warm local sandbox creation p95 strictly below 250 ms, ready W1 cache-hit-after-planning p95 strictly below 300 ms with exactly zero reservations, and the seven roadmap probes below.
2. Predeclare the decision branches in that contract: (a) native APFS clone plus enforceable/detectable controls passes all seven probes and 250 ms, so it can be the isolated local default; (b) native cloning is fast but path/capability control is incomplete, so report only the attained level and reserve pooled container/VM modes for stronger work; (c) only an exercised pooled container meets isolation/latency, so select it for reproducible Linux nodes; (d) profile identity or a stable key cannot exist before acquisition, so stop and redesign profiles/attestation rather than inherit probe-before-cache ordering; (e) no local miss path meets hook latency, so retain local planning/hits and dispatch misses to a warm pool; (f) immutable consumed bytes or cross-run writable isolation fails, so stop/narrow. Docker/container evidence is conditional on an actually reachable daemon and is recorded as unavailable, never inferred, otherwise.
3. At the Phase A checkpoint, run the contract verifier and show its diff. Only after user acceptance and explicit authorization, commit that contract first as required by the ticket DoD; do not add source/sandbox/cache implementation before that commit. Bind later evidence to that contract commit plus a digest manifest of the measured implementation tree.
4. Implement an experiment-local immutable source path: capture a selected temporary W1 tree into content-addressed blobs plus a sorted Merkle manifest, reject symlinks/path escapes, and materialize only verified bytes from that manifest. Probe demonstration 1 by creating the run snapshot, mutating the live source copy, then executing from the materialized snapshot and proving the observed file/result matches the original digest and never the post-creation marker. Keep this independent of the T1 toy Snapshot type and do not claim atomic live capture beyond the demonstrated boundary.
5. Implement disposable local sandbox candidates over the immutable materialization. Prefer APFS clonefile via the available cp -c mechanism, retain an ordinary-copy control/fallback, give every run unique writable HOME/TMPDIR/output/tool-cache paths, and destroy only experiment-owned temp roots. Run the real W1 format/test/vet leaves in two barrier-synchronised sandboxes while each writes a unique declared marker; prove neither can see or modify the peer output and the shared immutable source/CAS still verifies. For demonstration 3, inherit an undeclared environment canary and place read/write canaries outside the sandbox: sanitize the child environment, exercise sandbox-exec where supported, and require each undeclared access to be denied or captured as a detected violation. Evidence records names/reasons only, never canary bytes; detection without enforcement caps the attained claim below hermetic.
6. Implement a versioned, experiment-only cache-identity input with mandatory source, typed input manifests, resolved process/arguments, execution-profile, sandbox-policy, and dependency-manifest components. Reject incomplete inputs, canonicalize maps/lists deterministically, and prove independently that changing each semantic component changes the digest. Model the event order explicitly as resolve profile -> compute key -> lookup -> optional reserve -> attest -> create sandbox -> execute/publish. A ready hit must return on lookup with reservation/acquisition/sandbox counts all zero; an attestation mismatch after a miss must fail closed with no execution/publication and no key recomputation.
7. Keep result cache, mutable tool cache, and warm-worker state as separate experiment types/directories and event namespaces. Probe that warming or poisoning tool/provider state cannot manufacture a result hit, that a valid result hit needs no warm worker, and that only verified result manifests authorize reuse. Retain raw, machine-readable evidence for each roadmap demonstration: source-mutation, concurrent-output isolation, ambient path/environment control, complete pre-reservation identity, zero-reservation hit, mismatched attestation, and cache-class separation, including event sequences, input/output digests, requested/attained level, and limitations.
8. Build one experiment CLI before timing and use fixtures/t1-benchmark-harness/cmd/t1bench without importing the harness. Record 30 serial warm samples for APFS clone sandbox creation (and the copy control where supported) with CAS/source and worker state warm but result cache absent; time creation only, with cleanup/preparation outside the measured command. Record 30 serial ready-cache-hit samples with preparation recreating a verified entry, explicit cache dimensions, and reservation_count=0. Preserve v2 record.json and samples.txt, environment/tool versions, exact commands, failures/unavailable mechanisms, and a manifest of small raw evidence; do not commit generated sandboxes, CAS blobs, build caches, or large artifacts.
9. Add focused unit/integration/fault tests and an evidence verifier that cross-checks the frozen contract, all seven evidence files, trace ordering/counters, identity sensitivity, attestation rejection, cache-class separation, benchmark statistics, thresholds, fixture bindings, and absence of leaked canary content. Provide one task-specific command: mise exec -- task --dir experiments/e04-source-sandbox-cache check. Then run focused/race tests, that task-specific gate, git diff --check, and repository-root mise exec -- task check (rerunning only the malicious-planner loopback portion outside the managed sandbox if the known bind restriction is encountered).
10. Summarize the measured branch, recommendation, explicit limitations, and Gate 1 implications in the experiment README and TF-003.11 notes/final summary. Stop with implementation and verification complete but before checking AC/DoD, marking Done, committing the implementation, merging, pushing, or cleaning the worktree; show the user the full summary and diff for the required approval checkpoint.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership (2026-09-03):
- Agent: @tf003-11-agent
- Branch: tf-003.11-e04-source-sandbox-cache
- Worktree: /private/tmp/taskflow-tf-003.11
- Base: main@834ee0a95eef05a2a4e434bf9095826ee93a8cc5
- Initially claimed paths: experiments/e04-source-sandbox-cache/**; Backlog metadata for TF-003.11 via CLI only (backlog/tasks/tf-003.11 - Run-E04-immutable-source-sandbox-and-cache-identity-experiment.md).
- Boundary: experiment-local code and evidence only; no production module, fixture mutation, prototype import, or shared helper. Planning phase only until user approval.

Planning research (2026-09-03):
- Dependencies TF-003.01 and TF-003.03 are Done. The frozen W1 passing fixture and integrity-fault harness both pass from this worktree; mise emits only the known non-fatal tracked-config symlink warning.
- The integrity fixture freezes identity metadata and deliberately reserves before lookup; E04 must instead materialize and execute immutable bytes, and prove lookup-before-reservation. Its reject-all symlink policy is useful fixture evidence but is not a production source/CAS decision.
- Experiment boundary: consume fixtures/w1 and fixtures/integrity-faults as frozen evidence inputs without importing or modifying them. Use an experiment-local identity/profile/event model only; E02 owns canonical plan IR and Gate 1 owns stable contracts.
- Available native mechanisms: APFS-backed filesystem, BSD cp -c clonefile support, sandbox-exec, Go/gofmt 1.25.12, Python 3.9.6, Task 3.50.0, mise 2026.4.28, git, rg, and the T1 benchmark harness v2. Docker CLI exists but the daemon socket is inaccessible in the managed session; clonefile and Tart CLIs are absent.
- Intended claim ceiling: isolated unless every hermetic input is actually controlled. Sanitized environment plus sandbox denial/detection will be reported separately; network, time, randomness, and host-tool closure remain limitations unless explicitly demonstrated.
- No additional paths are claimed beyond experiments/e04-source-sandbox-cache/** and TF-003.11 metadata.

Compatibility correction after plan review (2026-09-03):
- docs/product-specification.md currently defines reproducibility/cache requirements as REP-1 through REP-6; it defines no SRC-* or CACHE-* requirement IDs. TF-003.11's description therefore contains stale labels, not canonical requirements.
- Phase A will map E04 directly to REP-1 immutable source, REP-2 requested/attained level, REP-3 semantic identity, REP-4 cache-before-reservation, REP-5 distinct cache classes, and REP-6 verified provenance. It will not invent compatibility aliases for SRC-* or CACHE-*.
- Of the execution requirements, E04 directly exercises EXEC-3 worker/sandbox lifecycle separation and EXEC-4 pre-provision profile identity plus execution attestation. EXEC-1 per-node placement and EXEC-2 saturation/global-slot behavior are not established by this local single-node experiment; EXEC-5 durable leases belongs to later daemon/service work.
- This correction changes only the TF-003.11 plan and notes via Backlog CLI; the stale ticket description remains visible as provenance and as an explicit limitation to resolve at the next ticket-definition review.

Phase A contract implemented for review (2026-09-03):
- Added only experiments/e04-source-sandbox-cache/README.md, Taskfile.yml, protocol.json, protocol.sha256, fixture-bindings.json, and scripts/verify_contract.py. No Go module, CAS/sandbox/cache/provider mechanism, probe result, benchmark record, or recommendation was added.
- protocol.json predeclares the six decision branches, all seven E04 demonstrations and raw-evidence paths, canonical REP-1..REP-6 plus EXEC-3/EXEC-4 mappings, the SRC/CACHE compatibility correction, maximum isolated claim, six mandatory cache-identity components, exact event ordering, 30-sample serial measurement rules, warm sandbox p95 < 250 ms, ready-hit p95 < 300 ms, and per-sample zero reservation/acquisition/sandbox counters.
- fixture-bindings.json pins the W1 passing repository/manifest, the complete integrity-fault fixture, and the benchmark harness v2 implementation/tests by SHA-256 at base 834ee0a. protocol.json pins that manifest at 23e578bf7499c3faae47d27d6037af7444987d44a984f460f45b79cb7f67b14e and is frozen at 43f024a1ceb74ec0a8b0d8341d74270116e0a268bc02bee2482c50c3ffe6f200.
- The verifier checks protocol and binding digests, product/roadmap anchors, fixture versions and every bound file, canonical requirement usage, identity ordering, metrics/counters, seven probe mappings, branches, reproducibility ceiling, and the Phase A-only file allowlist.
- Verification passed: mise exec -- task --dir experiments/e04-source-sandbox-cache check; mise exec -- task --dir experiments/e04-source-sandbox-cache check:contract; git diff --check. Mise emitted only the known non-fatal tracked-config symlink warning.
- Stopped at the required pre-commit checkpoint. AC/DoD remain unchecked; no final summary, commit, merge, push, cleanup, or Phase B work has occurred.

Phase B implementation and evidence checkpoint (2026-09-03):
- Implemented the experiment-local immutable source/CAS, verified materialization, APFS-clone and ordinary-copy sandbox candidates, private writable roots, targeted native sandbox controls, complete pre-reservation cache identity, zero-reservation ready hits, fail-closed attestation, and separate result/tool/warm-worker cache classes under the claimed experiment path only.
- Preserved the frozen Phase A protocol, hash, fixture bindings, and verifier byte-for-byte. The live Taskfile default check now runs the contract, Phase B tests, and evidence verifier; check:phase-a remains available as the historical exact-boundary check and intentionally rejects a Phase B tree.
- Retained machine-readable raw evidence for all seven probes, implementation/evidence manifests, environment and execution records, plus 30-sample APFS clone, ordinary-copy control, and ready-hit benchmarks. Measured p95: APFS 43.43 ms (<250 ms); copy control 40.16 ms; ready hit 44.46 ms (<300 ms), with zero reservations/acquisitions/sandboxes/executions/publications for all timed hits.
- Selected predeclared branch native-fast-incomplete. APFS cloning and cache/attestation ordering are credible, but the targeted allow-by-default sandbox-exec policy proves only named denials, so attained reproducibility remains observed rather than isolated. Docker daemon was inaccessible and Tart absent; neither was credited.
- Verification passed: mise exec -- task --dir experiments/e04-source-sandbox-cache check (10 unit/integration tests, frozen contract, evidence verifier); repository-root mise exec -- task check; git diff --check. Evidence generation required running outside the managed sandbox because nested sandbox-exec is prohibited there; the identical bounded command then completed against macOS sandbox-exec.
- Phase A contract commit: fe5cb0aa25deb4c10f72dc56e800cfeaac9e363c. Phase B remains uncommitted for the required review checkpoint. AC/DoD/final summary/status are intentionally unchanged.

Phase B final self-review correction (2026-09-03):
- Corrected the ready-hit benchmark boundary so an untimed prepare-cache-hit command recreates a verified file-backed result entry before every sample; the timed benchmark-cache-hit command now performs lookup, digest/provenance verification, and artifact-handle return. The timed upper bound still includes CLI startup and trace serialization.
- Regenerated all bound evidence and manifests. Final 30-sample results: APFS clone median 38.66 ms / p95 41.69 ms; ordinary copy median 36.22 ms / p95 39.35 ms; ready hit median 42.01 ms / p95 44.44 ms with reservation_count=0.
- Final verification passed: task-specific frozen-contract + 11 tests + evidence gate; repository-root mise exec -- task check; git diff --check. Explicit check:phase-a was also exercised and rejected the Phase B files as intended, preserving the historical exact-boundary diagnostic.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Implemented and measured the disposable E04 immutable-source, sandbox, and cache-identity experiment.

Changes:
- Added content-addressed source capture and verified materialization, APFS-clone and copy sandbox candidates, private writable roots, and concurrent real-W1 isolation probes.
- Added complete pre-reservation cache identity, verified zero-reservation ready hits, fail-closed profile attestation, and separate result/tool/warm-worker cache classes.
- Retained all seven required demonstrations, per-sample benchmark traces, implementation/evidence manifests, environment records, and limitations.

Results:
- Selected native-fast-incomplete: APFS sandbox p95 41.69 ms, copy p95 39.35 ms, and verified ready-hit p95 44.44 ms.
- Every timed ready hit recorded zero reservations, acquisitions, sandboxes, executions, and publications.
- Immutable consumed bytes, cross-run writable isolation, identity sensitivity, and attestation failure all passed.

Verification:
- mise exec -- task --dir experiments/e04-source-sandbox-cache check
- Eleven unit/integration tests and evidence verification
- mise exec -- task check from repository root
- git diff --check

Risks and follow-ups:
- The targeted allow-by-default sandbox profile proves named denials but not complete host isolation, so attained reproducibility is observed rather than isolated. Gate 1 should carry the APFS and cache-ordering findings forward while requiring a deny-by-default native profile or stronger container/VM mode before claiming isolation.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Predeclared E04 branches, latency budgets, and isolation probes are committed first
- [x] #2 All seven roadmap demonstrations retain raw evidence
- [x] #3 Task-specific fault/isolation checks and root verification pass
- [x] #4 The E04 decision and Backlog final summary are current
<!-- DOD:END -->
