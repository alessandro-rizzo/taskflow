---
id: TF-001.04
title: Characterise cache ordering and local interference
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:14'
updated_date: '2026-09-02 19:11'
labels:
  - milestone-t0
  - cache
  - concurrency
  - parallel-wave-t0
dependencies: []
references:
  - docs/roadmap.md#7-t0-repository-reset-and-prototype-baseline
  - prototype/bootstrap/cache
  - prototype/bootstrap/engine
parent_task_id: TF-001
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T0; gate G0; workflows W1 and W2; risks R4, R5, R9; requirements EXEC-2, EXEC-4, REP-3, REP-4.
Outcome: an executable baseline showing cache-hit and cache-miss ordering plus local concurrency and filesystem interference behaviour.
Stable assumptions: the prototype currently acquires and probes an environment before cache lookup can finish and shares the checkout.
Parallelism: safe with TF-001.01, TF-001.02, and TF-001.03. Own only dedicated characterisation tests and evidence paths.
Test or fault scenario: exercise hit, miss, simultaneous local runs, cancellation where relevant, and conflicting filesystem access.
Observability: capture ordered lifecycle events, reservation or acquisition evidence, timing, and interference symptoms.
Rollback or removal: characterisation instrumentation must be isolated and removable.
Versioned formats: none unless a temporary evidence schema is explicitly labelled non-production.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Cache-hit and cache-miss path ordering is demonstrated by an automated or reproducible scenario
- [x] #2 Provider reservation or environment acquisition before cache resolution is explicitly evidenced
- [x] #3 Concurrent local runs exercise shared-checkout and filesystem interference behaviour
- [x] #4 Results include limitations and the concrete questions handed to E04
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Cite the fixed ordering in prototype/bootstrap/engine/runtime.go's runtimeExecution.Run: TryAdmit/TryReserve (admission, engine/runtime.go:81) -> reservation.Acquire (environment acquisition, runtime.go:118) -> Materializer.Materialize/Upload (runtime.go:142) -> environment.Identity toolchain probe (runtime.go:158) -> Cache.ComputeIdentity (runtime.go:185, requires the probed environment identity) -> Cache.Restore (runtime.go:198, the actual cache lookup). This structurally proves acquisition-and-probe precedes cache resolution.
2. Add prototype/bootstrap/engine/t0_characterization_test.go (clearly commented as removable T0 evidence instrumentation):
   a. A test that wraps the real target.Provider/Reservation/Environment and cache.Store with thin logging decorators (delegating all real behaviour) to produce a timestamped ordered event log across a cache-miss run followed by a cache-hit run of the same pipeline. Assert acquire-before-cache-open ordering and record hit vs miss event sequences.
   b. A test with two independent local.Provider instances constructed over the SAME root directory (modeling two independent CLI processes sharing one checkout, since admission state lives in-process per Provider) running concurrent ExclusiveWorkspace-declared steps that write overlapping files. Assert both get admitted simultaneously (no cross-instance/cross-process serialization) and inspect resulting file content for interference. Run this under `go test -race` and record whether the detector flags anything, noting explicitly that filesystem races across separate child processes are outside what Go's race detector observes.
3. Run `go test -v -race -run T0Characterization ./engine/...` (and `./target/local/...` if a local-package test is added) from the worktree's prototype/bootstrap, capturing raw output to docs/evidence/t0/raw-cache/.
4. Write docs/evidence/t0/cache-characterisation.md: ordering evidence with file:line citations plus captured test output, acquisition-before-cache-resolution evidence, concurrent/shared-checkout interference evidence, explicit limitations (single-process simulation of multi-process concurrency, filesystem races vs Go race detector scope, local target only), and concrete open questions handed to E04 (e.g. cache identity depends on a probed/acquired environment identity, contradicting cache-before-provision; admission exclusivity is per-process-instance so a real shared checkout has no enforced cross-process isolation; workspace materialization/upload happens even when the run turns out to be a cache hit).
5. Check off Acceptance Criteria and Definition of Done via backlog task edit; append Implementation Notes as work proceeds.
6. Re-run `mise exec -- task check` from repo root in the worktree; the new test file must pass fmt/vet/race cleanly.
7. Add Final Summary calling out exactly which prototype/bootstrap file(s) were added, set status Done.
8. git add only the claimed evidence/test paths, commit to the branch only; no merge/push/main changes.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-001.04-cache-characterisation
Worktree: /private/tmp/taskflow-tf-001.04 (created from main @ 9ddea88)
Claimed paths: docs/evidence/t0/cache-characterisation.md, docs/evidence/t0/raw-cache/**, and (if needed) one new clearly-labelled removable test file under prototype/bootstrap/cache/ or prototype/bootstrap/engine/ named t0_characterization_test.go. No refactor of prototype runtime semantics.

- Read engine/runtime.go's runtimeExecution.Run and confirmed the fixed ordering: Acquire (118) -> Materialize (142, if configured) -> Identity probe (158) -> ComputeIdentity (185) -> Cache.Restore/Open (198). Cache identity is computed FROM the probed environment identity, so cache resolution is data-dependent on acquisition, not just ordered after it.
- Added prototype/bootstrap/engine/t0_characterization_test.go (only new prototype file) with two tests: TestT0CacheOrderingAcrossMissThenHit (instrumented wrappers around the real Provider/Reservation/Environment/Store, asserts acquire/identity-probe precede cache resolution for both miss and hit runs) and TestT0ConcurrentLocalProvidersDoNotCoordinateAcrossInstances (two independent local.Provider instances over one shared root both admit ExclusiveWorkspace work simultaneously; 100 concurrent small appends land without byte corruption but with no enforced serialization).
- Ran both under -race: no data race reported (expected - the interference is filesystem-level across separate os/exec children, outside Go's race detector scope; documented this explicitly as a limitation).
- Wrote docs/evidence/t0/cache-characterisation.md with ordering/interference evidence, limitations, and four concrete open questions for E04 (cache identity not resolvable before acquisition; TryReserve alone doesn't prevent Acquire on a guaranteed hit; admission exclusivity is per-Provider-instance not per-filesystem-root; corruption modes beyond atomic small appends untested).
- Full mise exec -- task check (fmt/vet/race) passes with the new test file in place.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added reproducible T0 characterisation of the isolated prototype's cache ordering and local-concurrency behaviour under docs/evidence/t0/, plus one new, clearly-labelled, removable test file in the prototype module.

Prototype file added (the only prototype change in this ticket):
- prototype/bootstrap/engine/t0_characterization_test.go - two tests that exercise the real Scheduler/RuntimeExecutor/local.Provider/cache.Store stack through thin, delegating instrumentation (no reimplementation of logic under test, no production behaviour change).

Evidence added:
- docs/evidence/t0/cache-characterisation.md
- docs/evidence/t0/raw-cache/ (environment facts, verbose and -race test logs, full gate log with the new test present)

Findings:
- Structural + dynamic evidence that cache-key computation and cache lookup happen strictly after provider/environment acquisition and toolchain identity probing (engine/runtime.go:118-198). Cache identity is computed FROM the probed environment identity, so this is a data dependency, not just an ordering coincidence - sharper than the baseline doc's existing prose claim.
- Two independent local.Provider instances sharing one root both admit "exclusive" work simultaneously (admission state is per-Provider-instance, not per-filesystem-root). 100 concurrent small appends landed without byte corruption (OS-level atomic appends), but nothing in Taskflow enforced serialization; go test -race reported no race, as expected since the interference is filesystem-level across separate child processes, outside the Go race detector's scope.
- Four concrete open questions handed to E04, most importantly: cache identity is not resolvable independently of environment acquisition today (disproves E04 required demonstration #4), and a guaranteed cache hit still performs a full Acquire (disproves E04 required demonstration #5).

Tests/verification:
- go test -v -run TestT0 ./engine/... and go test -race -v -run TestT0 ./engine/..., both exit 0.
- mise exec -- task check (fmt:check + test:race + lint) from repo root inside the worktree, exit 0, with the new test file present.

Risks/follow-ups:
- Concurrency evidence uses two Provider instances in one test process rather than two OS processes; this models the actual in-process admission-state bug faithfully but doesn't exercise process-level isolation. Documented as a limitation.
- Not merged: branch tf-001.04-cache-characterisation in worktree /private/tmp/taskflow-tf-001.04, awaiting explicit landing authorisation.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
