---
id: TF-003.01
title: Wire frozen T1 fixtures into the root verification gate
status: Done
assignee:
  - '@codex-tf003-01'
created_date: '2026-09-03 13:53'
updated_date: '2026-09-03 14:37'
labels:
  - risk-lab-prerequisite
  - t1-hardening
  - verification
  - parallel-wave-r0
dependencies:
  - TF-002
references:
  - Taskfile.yml
  - docs/roadmap.md#3-repository-and-code-lifecycle-policy
  - docs/decisions/0004-t1-exit.md
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: Risk Lab prerequisite; T1 verification hardening; risks R1-R12.
Outcome: the documented root gate fails when any maintained frozen T1 fixture or harness regresses, rather than checking only the preserved prototype.
Stable assumptions: fixture-local Taskfiles remain authoritative and the root delegates with the root-pinned toolchain; do not nest mise exec in delegated tasks.
Parallelism: independent hardening task. Own Taskfile.yml, root verification documentation, and the narrow command-claim correction in docs/decisions/0004-t1-exit.md only.
Test or fault scenario: introduce or select a failing fixture check and demonstrate that the root gate propagates its failure, then restore the fixture.
Observability: root output names each delegated fixture gate.
Rollback or removal: delegation can be removed only if an equivalent maintained-fixture gate replaces it.
Versioned formats: no format change.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The root check runs the prototype gate plus every maintained executable T1 fixture/harness gate
- [x] #2 Delegation uses the root-pinned environment without nested mise execution or trusting each fixture config
- [x] #3 Any delegated fixture failure makes the root check fail and identifies the failing surface
- [x] #4 Root documentation states exactly which specification-only and executable fixtures are covered
- [x] #5 The T1 exit ADR and raw evidence distinguish fixture-local checks from any separately executed race-detector run, and every claimed command is reproducible
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Extend the root Taskfile with explicitly named T1 wrapper tasks. Keep each fixture-local Taskfile check authoritative, run it from its fixture directory inside the already root-pinned mise environment, and do not invoke nested mise. Add root-owned checks for the W1 passing repository and W3 validator; document W2 as specification-only.
2. Add a distinct root T1 race group for the six passing Go modules (W1 plus the five executable Go harnesses), with one named task per surface, and include both the fixture-local gates and these separate race runs in the top-level check so failures identify their source.
3. Update the root README with an exact coverage matrix: prototype, W1 passing fixture, W2 specification-only exclusion, W3 specification validator, five executable harnesses, local-gate behavior, and the separate race layer.
4. Correct ADR 0004 narrowly so its evidence table and criterion text no longer call race runs part of each fixture-local Taskfile gate. Record the exact fixture-local and separate race commands, then refresh root-check.log and add t1-fixtures-race.log so every claim has reproducible raw evidence.
5. Demonstrate top-level failure propagation without modifying a frozen fixture: temporarily point the root-owned W1 wrapper at the existing repo-test-failure negative fixture, run mise exec -- task check and capture the expected non-zero, named failure in root-check-failure-probe.log, then restore the passing W1 path.
6. Verify the restored tree with mise exec -- task t1:check, mise exec -- task check, and git diff --check. Review the diff and ticket evidence, but stop before checking acceptance criteria, marking Done, committing, merging, or pushing until the user gives the required completion approval.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership (2026-09-03):
- Agent: @codex-tf003-01
- Branch: tf-003.01-root-fixture-gate
- Worktree: /private/tmp/taskflow-tf-003.01
- Initially claimed paths: Taskfile.yml; README.md; docs/decisions/0004-t1-exit.md; docs/evidence/t1/raw/root-check.log; docs/evidence/t1/raw/t1-fixtures-race.log (new only if needed to make the race evidence reproducible).
- Planning phase only: no implementation or documentation edits before user approval.

Planning research (2026-09-03):
- Root Taskfile currently delegates only prototype/bootstrap task check.
- Executable T1 surfaces are five fixture-local Taskfile check gates (benchmark, plan conformance, lifecycle, integrity, malicious planner), the W1 passing repository format/vet/test commands, and the W3 dependency-free validator. W2 is specification-only; the three W1 failure repositories are negative fixtures, not normal green gates.
- Fixture-local Go tests are plain go test ./..., while the accepted T1 ADR currently describes the retained logs as fixture-local go test -race evidence. The correction must name local gates and separately executed race commands precisely.
- Available tools: mise 2026.4.28, Task 3.50.0, Go/gofmt 1.25.12, Python 3.9.6, git, and rg.
- Additional claimed path: docs/evidence/t1/raw/root-check-failure-probe.log (new negative-propagation evidence).
- Risk: the malicious-planner network attempt can be denied by the managed sandbox itself; if that prevents the complete gate, rerun the same root command with the established narrow approval rather than weakening the fixture.

Implementation completed for review (2026-09-03):
- Root Taskfile runs the prototype, W1 passing fixture, W3 validator, all five fixture-local Go harness gates, and a separately named six-module race layer without nested mise.
- README records exact executable coverage, W2 and negative-fixture exclusions, and the root-pinned toolchain boundary. ADR 0004 distinguishes fixture-local checks from separately executed race runs.
- Raw root, race-only, and expected-failure evidence records implementation base 9e265f1. The negative probe temporarily selected repo-test-failure, exited non-zero at t1:w1:check, and restored the passing path.
- Verification passed: mise exec -- task t1:check; mise exec -- task t1:race; mise exec -- task check; task --list; git diff --check. Malicious-planner requires the established outside-sandbox run because it deliberately binds loopback.
- Limitations: W2 has no executable gate; W3 checks fixture consistency rather than real native-mobile infrastructure.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Extended the root verification gate so regressions in frozen executable T1 fixtures and harnesses fail alongside the preserved prototype.

Changes:
- Added named root wrappers for W1, W3, and all five Go harness local gates without nested mise.
- Added a separate race-detector layer for W1 and the five passing harness modules.
- Documented exact coverage and corrected ADR 0004 command claims.
- Added passing root/race evidence plus an expected-failure propagation transcript.

Verification:
- mise exec -- task t1:check
- mise exec -- task t1:race
- mise exec -- task check
- git diff --check

Limitations:
- W2 remains specification-only; W3 validates fixture consistency only; malicious-planner loopback tests require execution outside the managed sandbox.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and the exact command is recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes the corrected guarantee from remaining limitations
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
