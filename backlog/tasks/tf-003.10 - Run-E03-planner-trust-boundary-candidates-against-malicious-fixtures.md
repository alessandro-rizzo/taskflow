---
id: TF-003.10
title: Run E03 planner trust-boundary candidates against malicious fixtures
status: In Progress
assignee:
  - '@codex-tf-003-10'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-04 15:11'
labels:
  - risk-lab
  - e03
  - security
  - parallel-wave-r3
dependencies:
  - TF-003.02
  - TF-003.06
  - TF-003.08
references:
  - docs/roadmap.md#e03-project-planner-trust-boundary
  - fixtures/malicious-planner/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E03; risks R3 and R11; requirements SEC-1 through SEC-6.
Outcome: determine whether compiled project code can emit a plan without inheriting daemon filesystem, credential, network, process, resource, or authorization power.
Approaches: restricted native subprocess; pooled minimal sandbox/container; microVM fallback; reduced-execution declarative or generated discovery.
Stable assumptions: the T1 malicious catalogue is a starting set, not proof of safety; the daemon must validate and authorize emitted plans independently.
Parallelism: execution begins after initial E01/E02 drivers exist. Own experiments/e03-planner-trust only; do not modify the frozen catalogue except through a separate follow-up.
Test or fault scenario: external file/env reads, network/socket access, persistent descendants, CPU/memory/fd/output/time exhaustion, secret/unsafe plan fields, parser ambiguity, and privileged capability/effect requests.
Observability: per-attempt blocked/bounded/trusted-local verdict, latency, resource ceiling, sandbox identity, denial reason, and leak scan.
Rollback or removal: sandbox candidates remain disposable; no daemon/provider credential is used.
Versioned formats: policy and result envelopes are experimental and fail closed on unknown versions.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 The planner receives only an immutable selected source view and declared inputs, with no daemon/provider/secret credential
- [ ] #2 Every malicious case is blocked, bounded, or explicitly classified as a trusted-local limitation with evidence
- [ ] #3 An independent validator rejects unauthorized targets, networks, secrets, effects, unsafe paths, and ambiguous versions
- [ ] #4 Warm planning is measured against the 250 ms T1 W1 plan budget and the decision selects native, pooled sandbox, helper VM, static descriptors, or stop
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Phase A - freeze the E03 security and measurement contract before any attack execution

1. Create only the result-free contract surface under experiments/e03-planner-trust: README.md, Taskfile.yml, threat-model.md, protocol.json, protocol.sha256, scope-hashes.json, attacks.json, policy and resource-limit templates, contract tests, a strict contract verifier, and frozen attack/benchmark orchestration wrappers. Bind by SHA-256 the T1 malicious catalogue metadata and result types, the T1 plan-conformance CLI and validation implementation, the T1 benchmark-v2 runner, the accepted E01 Candidate B discovery closure and outputs, and the E02 W1 planner closure and retained plan. Treat every bound input as read-only evidence and forbid imports from prototype/bootstrap or creation of a production module/public package.
2. Record the canonical requirement mapping before implementation. docs/product-specification.md defines only SEC-1, SEC-2, and SEC-3, so retain the ticket reference to SEC-1 through SEC-6 as provenance but test SEC-1 through SEC-3 together with PLAN-2, PLAN-5, and AGENT-6; do not invent SEC-4 through SEC-6 aliases. Freeze experimental policy, attempt-result, benchmark, and evidence-envelope versions and fail closed on unknown versions.
3. Freeze the threat model and trusted computing base. Treat compiled project code, its stdout/stderr, and its emitted JSON as hostile; trust only the outer supervisor, kernel or exercised isolation mechanism, immutable bundle builder, and independent validator. Give a planner one digest-verified selected source view plus one declared-input document, a minimal fixed environment, an invocation-private writable temporary directory, and no daemon, provider, Docker, Lima, SSH-agent, keychain, cloud, signing, or secret-store handle. Seed only obviously synthetic daemon/provider/secret canaries in the trusted parent. Every filesystem and socket target is created inside an experiment-owned temporary root, and no real secret or external endpoint is ever used.
4. Freeze a 1:1 E03 attack matrix without editing fixtures/malicious-planner. Preserve its six stable IDs and metadata while replacing the synthetic Run behavior in an experiment-local attack executable: traversal, absolute-path and symlink reads/writes outside the selected view; reads of synthetic ambient credential variables; TCP loopback and Unix-socket access only to listeners owned by the harness; a descendant that tries to persist after planner exit; separately bounded CPU, address-space, file-descriptor, stdout/stderr, and wall-time exhaustion probes; and exact, hex, and base64 synthetic-marker or unsafe-path plan output. Add experiment-local parser/version cases for duplicate keys, trailing documents/bytes, malformed or unsupported versions, unknown fields, excessive document depth/size/counts, and policy cases requesting unauthorized target profiles, network routes, secret capabilities, effects, unsafe paths, or excessive resources.
5. Freeze hard safety thresholds before results: each executable attempt has a 2 second wall deadline and the complete candidate suite a 30 second deadline; CPU hard limit 1 second, address-space hard limit 256 MiB where the mechanism supports it, file-descriptor hard limit 64, combined captured stdout/stderr limit 1 MiB, accepted W1 plan limit 1 MiB, and descendant cleanup verification within 1 second. The selected-source digest and every outside canary must remain unchanged; zero daemon/provider/secret canary bytes or their hex/base64 encodings may appear in planner-visible state or retained evidence; no listener may accept an unauthorized connection; no descendant or post-exit marker may survive; and every case must end as blocked, bounded, or a specifically justified trusted_local_limitation with mechanism identity, applied ceilings, latency, denial reason, and leak-scan outcome. A trusted_local_limitation can document an explicit trusted mode but cannot qualify a candidate for untrusted agent planning.
6. Freeze candidate definitions and comparable boundaries. Native means a fresh process group behind a trusted limit-setting launcher, explicit minimal environment, capped pipes, deadline/kill-group supervision, and a digest-recorded deny-first sandbox-exec profile granting only the selected source, declared inputs, executable/system runtime reads, and invocation temp writes. Pooled container means a prestarted locally built minimal scratch image, no network, read-only root, all capabilities dropped, no host/runtime sockets, 256 MiB/one CPU/pid and fd ceilings, read-only selected-source mount, tmpfs scratch, and one fresh exec per sample. Helper VM is eligible only through an already available and separately authorized dedicated endpoint with equivalent read-only input, network denial, quotas, and cleanup; E03 will not silently create or start host-wide VM state. Reduced execution means a hash-bound E01 schema plus E02 canonical W1 descriptor selected from declared inputs and accepted without running project code; it must disclose that arbitrary dynamic graph construction is unavailable. Installed commands, inaccessible daemons, or unstarted VMs are recorded as unavailable and receive no security or latency credit.
7. Freeze an independent Python 3.9 standard-library validator that shares no planner or E02 Go implementation. It must reject duplicate JSON members, trailing data, malformed/unknown versions, unknown fields, over-limit structure/resources/output, absolute or parent-traversing path-bearing values, targets outside the untrusted W1 allowlist, any unapproved service/network route, any secret request when the policy secret allowlist is empty, and any effect when the policy effect allowlist is empty. It must also require referentially valid effect targets and exact policy identity, accept the known-good W1 plan, and retain stable semantic-path denial diagnostics. T1 conformance remains an additional structural comparison, never an authorization decision.
8. Freeze measurement and evidence rules. Measure each actually exercised candidate from immutable input readiness through independently validated accepted W1 plan using 30 serial warm, prebuilt, fresh-invocation samples and the T1 v2 statistics; pooled mechanisms may exclude one-time pool creation but must include per-invocation admission/exec, output capture, and validation. Pass requires p95 strictly below 0.250 seconds and every sample passing the safety boundary. Bind the wrapper bytes before sampling, retain complete failed/unavailable sets, permit at most one full rerun after a documented mechanical correction, never replace individual samples, and never relax thresholds after observing results.
9. Freeze branch priority. Select native for untrusted/local agent planning only if all hard attack and validator gates pass with no security-relevant trusted-local limitation and warm p95 is below 0.250 seconds. Otherwise select an actually exercised pooled container that passes the same gates and budget; otherwise an actually exercised helper VM that passes them. If executable candidates fail, are unavailable, or miss the budget while the descriptor path passes validation and latency, select static/generated descriptors for untrusted runs and permit only an explicit trusted-local executable mode with its limitations. Stop or narrow the product if the independent validator leaks authority, no approach safely bounds every case, the static form cannot represent the required W1 plan, or every safe approach is incompatible with the planning budget. Correctness and secret leakage failures take precedence over latency and availability.
10. Verify the contract-only boundary with the task-specific check and git diff --check, then stop. Show the complete Phase A diff and threshold table and request explicit authorization to commit the contract and begin Phase B. No attack, candidate benchmark, evidence result, AC/DoD completion, or implementation commit occurs before that checkpoint. Any later contract correction must itself be reviewed and committed before affected attacks restart from the first sample.

Phase B - implement, attack, measure, and decide only after the committed-contract checkpoint

11. Add an experiment-local Go module containing the hostile planner/attack executable, limit-setting launcher, process-group supervisor, immutable selected-view builder, candidate adapters, and synthetic-canary/leak scanner. Build the current E01/E02 drivers into experiment-owned output paths from their bound read-only sources; do not write into those experiments. Assert catalogue ID/category/outcome/assertion parity at runtime so fixture drift fails before attacks. Keep mechanism-specific code isolated and avoid a shared production abstraction.
12. Implement and exercise the native candidate under the frozen profile, then exercise the minimal pooled-container candidate only if Docker access is explicitly approved and the daemon is genuinely reachable. Exercise the helper-VM adapter only if an approved dedicated endpoint exists. Implement the reduced-execution descriptor path regardless, because it is the fail-closed comparison. Managed-environment denial, unavailable images/endpoints, or unsupported resource controls become sanitized availability/trusted-local evidence rather than inferred success. Do not contact public networks, pull images, create a Lima instance, or use host credentials implicitly.
13. Implement the independent validator and its negative corpus separately from the Go planner. Prove the known-good W1 plan is accepted; T1-invalid plans still fail structural conformance; and each authorization, unsafe-path, version, ambiguity, size/depth/count, and resource mutation is rejected with the exact predeclared semantic path before any plan can be treated as accepted. Verify that a malicious planner cannot self-authorize by editing policy identity or emitted approval fields.
14. Run the frozen attack wrapper once per available candidate, preserve one sanitized per-attempt JSON record plus candidate/sandbox identity and aggregate scorecard, and hash all raw evidence. For process/resource cases record enforced and observed ceilings, timeout/termination sequence, post-exit liveness checks, and capped output counts. Scan raw and summarized evidence for every synthetic marker representation before persistence; store only marker digests and redaction placeholders. Bind the exact contract commit and measured implementation tree in implementation and evidence manifests.
15. Run the frozen 30-sample warm W1 benchmark sets serially in the predeclared candidate order after correctness gates pass. Retain T1 v2 records and raw samples, sandbox/pool cache dimensions, exact preparation and timed commands, OS/tool versions, policy/image/VM identity, and unavailable or invalidated sets. Apply the frozen branch rules mechanically and write a limitations-first recommendation; do not claim sandbox strength from command presence or generalize one macOS host result to Linux or production.
16. Verify with mise exec -- task --dir experiments/e03-planner-trust check, focused Go race and vet checks in the experiment module, Python validator/contract/evidence tests, fixture-local conformance checks where invoked, repository-root mise exec -- task check, and git diff --check. Append exact commands and outcomes to the ticket. Then stop before checking AC/DoD, writing Final Summary, setting Done, committing Phase B, merging, pushing, or cleanup; present the full result summary and diff for explicit user approval.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Ownership and dependency record (2026-09-04):
- Agent: @codex-tf-003-10
- Branch: tf-003.10-e03-trust
- Worktree: /private/tmp/taskflow-tf-003.10
- Base revision: 21a55f3ea9eac0016d55b7827e80c01c237c9020
- Initially claimed paths: experiments/e03-planner-trust/**; backlog/tasks/tf-003.10 - Run-E03-planner-trust-boundary-candidates-against-malicious-fixtures.md
- Dependencies: TF-003.02, TF-003.06, and TF-003.08 are Done; no unmet declared dependencies.
- Scope boundary: fixtures/malicious-planner/**, fixtures/t1-plan-conformance/**, experiments/e01-authoring-schema/**, experiments/e02-plan-ir/**, product docs, prototype/bootstrap, and production modules are read-only inputs unless the task claim is revised and coordinated first.
- Planning-only checkpoint: no implementation, attack execution, evidence generation, commits, merges, pushes, or cleanup before explicit user approval of the recorded plan.

Planning research (2026-09-04):
- Canonical requirements correction: docs/product-specification.md defines SEC-1 through SEC-3 only; SEC-4 through SEC-6 do not exist. The contract will retain the ticket text as provenance and map the tested boundary to SEC-1, SEC-2, SEC-3, PLAN-2, PLAN-5, and AGENT-6 without inventing compatibility aliases.
- E01 input: proposed ADR 0005 selects generated Go candidate B as Gate 1 evidence. Its schema discovery parses embedded declarations and does not run operation bodies, but it is not a security boundary and E03 may revisit it.
- E02 input: the current disposable e02plan driver emits a validated canonical W1 plan from Candidate B and records a 10.366 ms warm p95 before sandboxing. Its Python reader and T1 harness are read-only comparison inputs, not policy authorization.
- T1 malicious-planner input: six stable catalogue IDs cover filesystem, environment, network, process, resource, and output abuse, but their current Run functions are synthetic self-tests. E03 must implement real experiment-local attacks with the same catalogue identities and add parser ambiguity plus privileged target/network/secret/effect cases without changing the frozen fixture.
- T1 conformance limitation: it validates known outer plan shapes but intentionally leaves nested condition/resource/profile/cache content opaque and does not authorize cross-array domain references. E03 therefore needs a separate fail-closed policy validator.
- Available local tools: Go 1.25.12, Python 3.9.6, jq 1.7.1, Task 3.50.0, mise 2026.4.28, sandbox-exec, Docker CLI, and Lima CLI. Current managed-environment probes found sandbox-exec denied with Operation not permitted, Docker API access denied, and no Lima instance. Presence alone will not count as candidate evidence; host-level execution needs explicit approval/escalation after the contract is committed.
- Existing E02/E04/E05 convention: commit a result-free hashed Phase A contract and fixture bindings before candidate execution; bind the measurement wrapper before sampling; retain failed/unavailable sets; then bind exact implementation and evidence bytes. No experiment or attack has been executed in this planning pass.

Phase A contract implementation (2026-09-04):
- Added the result-free E03 contract under experiments/e03-planner-trust only: narrative threat model, machine protocol, six-entry attack catalogue mapping plus 19 extended parser/policy cases, native/container/untrusted-plan/resource policies, frozen non-executing wrappers, strict verifier, and contract tests.
- Bound 26 read-only inputs spanning the T1 malicious catalogue/result contract, T1 conformance implementation, T1 benchmark-v2 implementation, accepted E01 Candidate B discovery closure and W1 outputs, and current E02 W1 planner closure/evidence.
- Frozen protocol digest: 2d801910b647d65e9aac0fa6a797011c04921d58c3cac8f2740cd15d93a82cbb. scope-hashes.json binds every Phase A file except its own manifest plus every external input.
- Frozen thresholds: 2 s per attempt; 30 s per candidate suite; 1 s CPU; 256 MiB address space where supported; 64 file descriptors; 1 MiB combined output; 1 MiB accepted W1 plan; descendant cleanup within 1 s; 30 serial warm samples with p95 strictly below 250 ms.
- Frozen branch priority: correctness stop; native; pooled container; helper VM; static descriptor; latency stop. trusted_local_limitation cannot qualify untrusted agent planning.
- The attack and benchmark wrappers expose only --describe during Phase A validation. Execution requires a full contract commit and Phase B runner/binaries that are absent. No attack, benchmark, sandbox, Docker, or VM capability was invoked.

Phase A verification (2026-09-04):
- mise trust /private/tmp/taskflow-tf-003.10/mise.toml: trusted the isolated worktree config.
- mise exec -- task --dir experiments/e03-planner-trust check:contract: PASS; 26 bindings, six catalogue entries, 19 extended cases, no Phase B files; 6/6 contract tests passed.
- git diff --check: PASS for tracked changes.
- git diff --no-index --check /dev/null FILE: PASS for all 15 new Phase A files.
- The contract check emitted only the known non-fatal managed-sandbox warning because mise could not create a tracked-config symlink under the user state directory.
- No AC/DoD item, Final Summary, commit, merge, push, cleanup, attack result, benchmark sample, or Phase B implementation was created. Stopped for the required Phase A diff review and separate authorization to commit the contract and begin Phase B.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Threat assumptions, thresholds, and safe execution boundaries are committed before attacks run
- [ ] #2 Every attack has reproducible sanitized evidence and no real secret is used
- [ ] #3 Task-specific security gate and root check pass
- [ ] #4 The E03 decision and Backlog final summary are current
<!-- DOD:END -->
