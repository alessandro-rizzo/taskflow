---
id: TF-002.01
title: Freeze the W1 fast project check fixture
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:15'
updated_date: '2026-09-02 22:10'
labels:
  - milestone-t1
  - w1
  - fixture
  - parallel-wave-t1a
dependencies:
  - TF-001
references:
  - docs/roadmap.md#w1-fast-project-check
  - docs/roadmap.md#8-t1-measurement-contracts-and-representative-fixtures
parent_task_id: TF-002
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T1; workflow W1; risks R1, R2, R5, R12; requirements TYPE-1, TYPE-2, TYPE-6, PLAN-1, PLAN-3, REP-4.
Outcome: a minimal frozen repository or fixture tree that represents a fast project check without embedding an SDK solution.
Stable assumptions: G0 evidence is accepted; fixture semantics come from roadmap W1.
Parallelism: first-wave task, safe with TF-002.02, TF-002.03, and TF-002.04. Own only the W1 fixture subtree and its specification.
Test or fault scenario: format failure, unit-test failure, static-analysis failure, changed-path condition, cold plan, warm plan, and ready cache hit.
Observability: fixture declares expected nodes, edges, typed outcomes, event assertions, and worker-reservation count.
Rollback or removal: fixture is independently removable and must not become production code.
Versioned formats: fixture manifest or expected output must declare an experimental version.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The frozen fixture captures format, unit-test, static-analysis, and aggregate Check semantics
- [x] #2 Expected dependency edges, conditions, outputs, and human or machine diagnostics are explicit
- [x] #3 Cold, warm, and ready-cache-hit paths can be selected without changing fixture content
- [x] #4 The ready cache-hit expectation requires zero worker reservations
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
OPEN QUESTION for user before implementing (see report): where does a T1 cross-experiment fixture tree live? Proposal: new top-level fixtures/ directory (fixtures/w1/), since it doesn't fit docs/ (not a decision), experiments/eNN-*/ (not one question with a deadline, and experiments/README.md bans even experiments from importing prototype/bootstrap - inapplicable here anyway since this fixture doesn't need to), or prototype/bootstrap/ (frozen, not to be extended). This does extend roadmap section 3's repository layout, so flagging for explicit confirmation rather than deciding unilaterally.

1. fixtures/w1/repo/: a minimal standalone Go module (own go.mod) - not wired through prototype/bootstrap's flow/engine and not a Taskflow-authored pipeline (no SDK exists pre-Gate-1). This is the target repo that a future authoring experiment (E01) points a pipeline at, matching roadmap section 8 deliverable #1 ("minimal repositories... for W1"). Contains: a small package with normal source + one unit test, deliberately simple.
2. fixtures/w1/repo-format-failure/, repo-test-failure/, repo-lint-failure/: minimal variant copies that each fail exactly one of the three W1 checks (gofmt-dirty, failing test, vet-triggering issue), for the "format failure / unit-test failure / static-analysis failure" fault scenarios (AC #1). Each variant is otherwise identical to the base repo so only one dimension differs.
3. fixtures/w1/manifest.yaml: machine-readable declaration - fixture_id, roadmap_workflow: W1, version: t1-experimental-v1 (satisfies "versioned formats"), expected nodes (format, test, lint, aggregate Check), expected edges (format/test/lint -> Check), planning conditions (e.g. test node re-plans only when *_test.go or its package changed - satisfies "planning conditions based on changed paths"), expected outputs (test report shape), expected diagnostics per fault variant (AC #2).
4. fixtures/w1/README.md (fixture specification, human-readable twin of manifest.yaml): explains the three variants, and defines cold/warm/ready-cache-hit as external PRECONDITIONS on cache/driver state (not fixture content) so state can be selected without editing the fixture (AC #3) - modeled on TF-001.03's TASKFLOW_DRIVER_CACHE pattern (external cache dir empty vs warm vs pre-seeded with a known result).
5. Explicitly declare the "ready cache-hit requires zero worker reservations" property (AC #4) as a TARGET/golden expectation this fixture asserts for any future implementation to be measured against - explicitly NOT a claim that the current prototype meets it (TF-001.04 proved the opposite: acquisition always precedes cache resolution today). Word this carefully so the fixture reads as a measurement contract, not a passing test of prototype/bootstrap.
6. Do NOT invent a benchmark-result schema here (TF-002.04 owns that) - just note in manifest.yaml/README.md where this fixture's expected-output declarations are meant to plug into that format once it exists.
7. Verification: gofmt/go vet/go test manually run against each variant to confirm each fixture actually fails exactly the intended single check and the base repo passes all three cleanly. Record commands and results as evidence (measured facts vs. limitations vs. open questions, matching T0's evidence style) in fixtures/w1/README.md or a small evidence subsection.
8. Root `mise exec -- task check` must remain green throughout (fixtures/ is inert to it - no root Go module exists yet).

STOP after this plan is written - do not create fixture files yet. Awaiting user approval, in particular on the fixtures/ location question.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-002.01-w1-fixture
Worktree: /private/tmp/taskflow-tf-002.01 (created from main @ 71ab133, includes merged T0 evidence)
Claimed paths: fixtures/w1/** (new top-level dir, see plan for open question about this location)

Research: roadmap.md section 3 (repo layout: docs/, experiments/eNN-*/, prototype/bootstrap/, cmd|internal|pkg future) has no slot for T1's cross-experiment shared fixtures - experiments/README.md's format (one question, one deadline, hypothesis/pivot/stop thresholds) doesn't fit a long-lived shared measurement contract used by all of E01-E08, and experiments/README.md explicitly forbids even experiments from importing prototype/bootstrap's Go module (stricter than roadmap section 3 rule 1, which only bans PRODUCTION packages). Re-reading roadmap section 8 deliverable #1 ("Minimal repositories or frozen fixture trees for W1, W2, and W3") together with section 4's W1 shape: the fixture should be a minimal standalone target Go repo (its own go.mod, source+test files) that a FUTURE authoring experiment (E01) points a pipeline at - not a Taskflow-authored pipeline itself (no SDK exists pre-Gate-1) and not wired through prototype/bootstrap's flow/engine (which would violate the no-import rule). TF-001.03's w1-startup.md explicitly flags that its .taskflow/main.go measurement is not this fixture and that TF-002.01 should freeze a purpose-built one.

Coordinator confirmed fixtures/ as the T1 fixture location (docs/roadmap.md section 3 rule 3a, pushed to main at 7d3c91f) - matches this ticket's proposal, no change needed. Merged main into the branch, reran mise exec -- task check (green), then implemented per the plan:

- fixtures/w1/repo/: base Go module (its own go.mod), one function + one test, passes gofmt/vet/test cleanly.
- fixtures/w1/repo-{format,test,lint}-failure/: each differs from repo/ in exactly one file, verified empirically to fail exactly its named dimension and nothing else.
- Lint variant needed a second attempt: a Printf-verb-mismatch first draft also failed go test (go test runs a default high-confidence vet subset that includes printf), collapsing the lint/test distinction. Switched to an "unreachable code" fault, empirically confirmed caught by go vet but NOT by go test's default subset - documented in README.md.
- fixtures/w1/manifest.yaml: machine-readable declaration (fixture_id, version t1-experimental-v1, expected_graph, planning_conditions, expected_diagnostics, execution_states, ready_cache_hit_target explicitly marked as a target the current prototype does not meet per TF-001.04).
- fixtures/w1/README.md: human-readable twin, evidence table with raw command output, limitations, open questions.
- fixtures/w1/raw/: environment.txt + one log per repo variant (gofmt/vet/test output).

Verified: root mise exec -- task check still green (fixtures/ is outside the prototype Go module, inert to it).

Reopened after independent Codex peer review of the T1 wave-1 batch. All 4 ACs were rated Met, but two Low-severity provenance/doc issues were found: (1) manifest.yaml's expected_diagnostics.test-failure references a nonexistent evidence.md instead of the real raw/ logs + README.md; (2) raw/environment.txt's recorded source revision (7d3c91f) necessarily predates this fixture's own commit, so it doesn't by itself identify the measured content - needs a clarifying note, not a structural fix.
Branch: tf-002.01-w1-fixture-fixes, worktree: /private/tmp/taskflow-tf-002.01-fix, claimed paths: fixtures/w1/manifest.yaml, fixtures/w1/raw/environment.txt (fix-only edits, no other files).

Fixes applied and verified: manifest.yaml's dangling evidence.md reference now points to raw/repo-test-failure.log + README.md's real section; raw/environment.txt annotated explaining the inherent revision/commit chicken-and-egg. go build/vet/gofmt/test all pass on fixtures/w1/repo; root mise exec -- task check green.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Follow-up to the independent Codex peer review of the T1 wave-1 batch. All 4 ACs were already rated Met; this addressed two Low-severity documentation/provenance nits the review found:

- fixtures/w1/manifest.yaml's expected_diagnostics.test-failure referenced a nonexistent evidence.md file; now points to the real raw/repo-test-failure.log and README.md's "Evidence: verification commands and results" section.
- fixtures/w1/raw/environment.txt recorded a source revision that necessarily predates this fixture's own commit (a fixture cannot record the hash of a commit that doesn't exist yet); added a note explaining this and pointing reviewers at README.md's reproduction steps instead of trusting the revision string alone.

No acceptance-criteria or structural changes. Verified: go build/go vet/gofmt -l/go test all pass on fixtures/w1/repo (and the three fault variants, unaffected by this change); root mise exec -- task check green.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
