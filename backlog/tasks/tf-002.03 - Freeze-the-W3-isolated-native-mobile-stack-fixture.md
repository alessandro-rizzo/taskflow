---
id: TF-002.03
title: Freeze the W3 isolated native-mobile stack fixture
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:15'
updated_date: '2026-09-02 19:40'
labels:
  - milestone-t1
  - w3
  - fixture
  - parallel-wave-t1a
dependencies:
  - TF-001
references:
  - docs/roadmap.md#w3-isolated-native-mobile-stack
  - docs/roadmap.md#8-t1-measurement-contracts-and-representative-fixtures
parent_task_id: TF-002
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T1; workflow W3; risks R4, R7, R8, R9; requirements EXEC-3, EXEC-4, EXEC-5, AGENT-4, AGENT-5, DUR-3.
Outcome: a frozen native-mobile stack fixture spanning a Linux API endpoint, macOS Xcode build, simulator execution, concurrent namespaces, and cleanup.
Stable assumptions: the fixture describes capabilities and identities without claiming that macOS feasibility has passed E06.
Parallelism: first-wave task, safe with TF-002.01, TF-002.02, and TF-002.04. Own only the W3 fixture subtree and its specification.
Test or fault scenario: concurrent namespaces, port collision attempt, unauthorized routing, cancellation, caller loss, dirty warm infrastructure, and simulator mismatch.
Observability: expected endpoint authorization, namespace paths, profile attestation, leases, cleanup events, and orphan state are declared.
Rollback or removal: fixture is independently removable and contains no production provider.
Versioned formats: fixture manifest and golden expectations declare experimental versions.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 The fixture represents Linux API, macOS build, explicit endpoint, simulator, and MobileE2E report edges
- [x] #2 Two concurrent namespace instances have distinct writable paths, services, ports, and leases
- [x] #3 Cancellation, caller-loss cleanup, profile mismatch, and unauthorized endpoint cases are specified
- [x] #4 The fixture clearly marks unimplemented infrastructure and does not imply E06 success
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Confirm scope: since no W3 infrastructure (macOS/Xcode/simulator/namespace/service code) exists anywhere in the repo, "frozen fixture" here means a versioned specification document plus concrete golden example records (plain JSON/YAML, not Go types - no schema/plan IR may stabilize before Gate 1 per roadmap section 3/24) describing the expected shape E06/E07 will eventually build against. It is not runnable today; DoD's "task-specific verification" is a spec-validity check (JSON parses, required fields present), not an execution test.
2. Location: fixtures/w3/ (roadmap section 3 rule 3a, decided during this ticket's work and merged to main at 7d3c91f - fixtures/ holds T1+ frozen, reusable, versioned fixtures, distinct from disposable experiments/ and from docs/evidence/).
3. Write fixtures/w3/spec.md covering:
   - The five W3 edges (Linux API build -> Endpoint[API]; macOS Xcode build -> Artifact[IOSApp]; Endpoint + IOSApp + simulator -> Report[MobileE2E]) with explicit node/edge names (AC #1).
   - Two concurrent namespace instances with distinct example writable paths, service names, ports, and lease records, shown as parallel golden JSON examples with no overlapping values (AC #2).
   - Named fault/test scenarios with expected observable events: port collision attempt (rejected), unauthorized endpoint routing (rejected/denied event), cancellation (bounded cleanup event sequence), caller loss (orphan detection + reclaim event sequence), dirty warm infrastructure (reset-before-reuse expectation), simulator/profile mismatch (rejected with attestation-failure event) (AC #3).
   - A prominent "status: specification only, not implemented" banner plus a per-section marker distinguishing "modeled/aspirational" fields from anything already demonstrated by the prototype (none) or by E06 (not yet run) (AC #4).
   - An explicit experimental fixture-format version string (e.g. `t1-w3-fixture-v0-experimental`), satisfying roadmap rule 3a's version requirement.
4. Add fixtures/w3/examples/*.json - the concrete golden records referenced by spec.md (two-namespace example, each fault scenario's expected event sequence).
5. Add a minimal, dependency-free validation script (documented shell/python commands, or a tiny script under fixtures/w3/) that checks the example JSON files are well-formed and contain the fields spec.md declares required - this is the "task-specific verification" command for DoD #1, kept out of prototype/bootstrap and out of any new Go module.
6. Update AC/DoD, append notes, re-run root `mise exec -- task check` to confirm the docs-only addition doesn't disturb the gate, write the final summary, mark Done.
7. Do not implement any macOS/namespace/service runtime code - explicitly out of scope (that is T5/T7's job, gated behind E06/E07/Gate 1). Commit to branch only; do not merge/push.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-002.03-w3-fixture
Worktree: /private/tmp/taskflow-tf-002.03 (created from main @ 71ab133)
Claimed paths: docs/evidence/t1/w3-fixture/** (spec + golden example records; reuses the docs/evidence/<tranche>/ convention established at T0 rather than inventing a new shared top-level fixtures/ directory unilaterally, since that would need coordination with the parallel TF-002.01/.02/.04 tickets - flagged as an open convention question, see plan/notes below)

Research confirmed: grep across prototype/bootstrap for xcode|simulator|namespace|endpoint[|service[ finds no real hits (one incidental match in state/file/file_test.go, unrelated). No macOS/Xcode/simulator/namespace/service code exists anywhere in this repository. No fixtures/ or testdata/ directory exists at root. This fixture therefore has nothing to run against yet - it must be a specification/golden-expectation artifact, not runnable code, consistent with the ticket's own stable assumption ('the fixture describes capabilities and identities without claiming that macOS feasibility has passed E06') and AC #4.

- Coordinator relayed mid-task: fixtures/ is now the decided top-level location for T1+ fixtures (roadmap section 3 rule 3a, merged to main at 7d3c91f). Merged main into this branch, updated the plan from docs/evidence/t1/w3-fixture/ to fixtures/w3/.
- Implemented fixtures/w3/spec.md, fixtures/w3/examples/{namespace-a,namespace-b,scenario-port-collision,scenario-unauthorized-routing,scenario-cancellation,scenario-caller-loss,scenario-dirty-warm-infrastructure,scenario-simulator-profile-mismatch}.json, and fixtures/w3/validate.sh.
- validate.sh (dependency-free, python3 stdlib json only) passes: all 8 example files well-formed with required fields.
- Root mise exec -- task check verified green both before and after (docs/fixture-only addition, no Go code touched, no prototype changes).
- All 4 AC and 4 DoD checked.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Froze the W3 (isolated native-mobile stack) fixture at fixtures/w3/, per the coordinator's mid-task relocation decision (roadmap section 3 rule 3a: fixtures/ holds T1+ frozen, reusable, versioned measurement fixtures, distinct from disposable experiments/ and from docs/evidence/).

Since no W3 infrastructure (macOS/Xcode/simulator/namespace/service code) exists anywhere in this repository - verified by grep across prototype/bootstrap - this fixture is a specification/golden-example artifact, not runnable code. It is the target E06 (macOS/Xcode/simulator feasibility) and E07 (namespace-private services/endpoints) will eventually be checked against, written to be neutral across E06's still-open branches.

What was added:
- fixtures/w3/spec.md: the five W3 edges (Linux API -> Endpoint[API]; macOS Xcode build -> Artifact[IOSApp]; Endpoint+IOSApp+simulator -> Report[MobileE2E]), a two-namespace concurrency model with no colliding identifiers, six named fault/test scenarios (port collision, unauthorized routing, cancellation, caller loss, dirty warm infrastructure, simulator/profile mismatch) each mapped to a roadmap requirement and an expected outcome, a prominent "specification only" status section, and the experimental fixture-version string t1-w3-fixture-v0-experimental (satisfies roadmap rule 3a's version requirement).
- fixtures/w3/examples/*.json: 2 namespace records + 6 scenario records backing spec.md, all plain JSON (deliberately not Go types, since no schema/plan IR may stabilize before Gate 1).
- fixtures/w3/validate.sh: dependency-free (python3 stdlib json only) structural validator; all 8 examples pass.

Explicitly out of scope: any macOS VM, Xcode, or simulator implementation; any choice among E06's branches; any Go type/wire schema for these concepts; W1/W2 (see sibling TF-002.01/TF-002.02).

Verification: fixtures/w3/validate.sh (PASS, all 8 examples). mise exec -- task check from repo root, exit 0, before and after (fixture-only addition, no Go/prototype code touched).

Open question handed to TF-002.09 (T1 exit convergence): whether fixtures/w1, fixtures/w2, and fixtures/w3 should share a common event-vocabulary/record-format convention - this fixture intentionally does not presume one to avoid inventing a shared contract outside its own ownership.

Not merged: branch tf-002.03-w3-fixture in worktree /private/tmp/taskflow-tf-002.03, awaiting the coordinator's serialized landing alongside the other T1 wave-1 tickets.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
