---
id: TF-001.02
title: 'Inventory prototype packages, interfaces, and review findings'
status: Done
assignee:
  - '@alessandro-rizzo'
created_date: '2026-09-02 17:14'
updated_date: '2026-09-02 19:09'
labels:
  - milestone-t0
  - evidence
  - parallel-wave-t0
dependencies: []
references:
  - docs/roadmap.md#7-t0-repository-reset-and-prototype-baseline
  - prototype/bootstrap/docs/reviews/2026-07-27-fable-5.md
  - prototype/bootstrap/docs/baseline.md
parent_task_id: TF-001
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap tranche: T0; gate G0; risks R1-R12 baseline.
Outcome: an evidence inventory mapping prototype packages and public interfaces to proven, unproven, and disproven concepts.
Stable assumptions: the Fable review and prototype source are evidence, not compatibility requirements.
Parallelism: safe with TF-001.01, TF-001.03, and TF-001.04. Own the inventory artefact; avoid shared benchmark outputs.
Test or fault scenario: cross-check the inventory against package discovery, exported API inspection, tests, and known correctness findings.
Observability: record source paths and executable verification commands for every material claim.
Rollback or removal: documentation only.
Versioned formats: none.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Every prototype package has a purpose and public-interface inventory
- [x] #2 Known Fable correctness findings are mapped to current tests or explicit gaps
- [x] #3 Concepts are classified as proven, unproven, or disproven without treating APIs as stable
- [x] #4 Inventory entries cite source or test evidence
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Enumerate packages with `go list ./...` (20 packages confirmed) and capture `go doc -all <pkg>` for each into docs/evidence/t0/raw-inventory/godoc/ as raw exported-API evidence.
2. List each package's Go source and test files (`ls`) to ground purpose descriptions and test-coverage citations in real paths.
3. Read prototype/bootstrap/docs/reviews/2026-07-27-fable-5.md in full and, for each Critical/High/Medium finding, grep the test tree for a plausible regression test; record the specific test file/function if found, or an explicit gap if not.
4. Read prototype/bootstrap/docs/baseline.md's "Demonstrated concepts" and "Important limitations" sections as a starting classification hint, then independently verify each concept against source/tests before classifying it proven/unproven/disproven - never restate baseline.md without a citation of my own.
5. Write docs/evidence/t0/package-inventory.md: one section per package (purpose, public/exported interface summary, source path, test files/executable verification command), a Fable-finding-to-test mapping table, and a proven/unproven/disproven concept table, each entry citing a source or test path. Explicitly disclaim API stability.
6. Check off TF-001.02 AC/DoD as satisfied; append implementation notes as work proceeds.
7. Re-run `mise exec -- task check` from repo root in the worktree to confirm the gate stays green after adding only docs files.
8. Add Final Summary, set status Done, commit to the branch only (no merge/push).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Branch: tf-001.02-package-inventory
Worktree: /private/tmp/taskflow-tf-001.02 (created from main @ 9ddea88)
Claimed paths (new files only, no prototype edits): docs/evidence/t0/package-inventory.md, docs/evidence/t0/raw-inventory/**

- Enumerated 20 packages via `go list ./...`; captured full exported surface per package with `go doc -all` under raw-inventory/godoc/, plus file listing and test-function listing.
- Read prototype/bootstrap/docs/reviews/2026-07-27-fable-5.md in full and mapped all 18 Critical/High/Medium findings to specific tests by file:line; found two explicit gaps not carried over from the review's own gap list: (1) no test asserts retry-attempt/output-identity reset specifically on resume (Medium #2), (2) neither runner/taskfile nor runner/just has a test exercising the runner-version cache-identity probe, and runner/just has no test file at all (Medium #8).
- Classified concepts as proven/unproven/disproven independently against source+tests, cross-checked against baseline.md's demonstrated-concepts/limitations lists rather than restating them.
- 9 of 20 packages have no package-local test file: cmd/taskflow, event, examples/basic, process, runner, runner/command, runner/just, target, terminal (several are thin type/interface packages exercised indirectly by consumers).
- Wrote docs/evidence/t0/package-inventory.md with source/test citations and verification commands for every entry.
- Re-ran root `task check` after adding only docs/evidence/ files; still green (exit 0).
- No merge/push performed; landing awaits explicit authorisation.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added a package, public-interface, and Fable-review inventory for the isolated prototype under docs/evidence/t0/, with no changes to prototype/bootstrap or any other existing file.

What was added:
- docs/evidence/t0/package-inventory.md - inventory of all 20 prototype packages (purpose, exported surface highlights, test files, verification command per package); a table mapping all 18 Fable 5 review findings (5 Critical, 4 High, 9 Medium) to specific current tests; and a proven/unproven/disproven concept classification cross-checked independently against baseline.md.
- docs/evidence/t0/raw-inventory/ - raw `go doc -all` output per package (godoc/), a source-file listing, and a full test-function listing backing the inventory's citations.

Findings:
- 20 packages confirmed via `go list ./...`. 9 have no package-local test file (cmd/taskflow, event, examples/basic, process, runner, runner/command, runner/just, target, terminal); several are thin type/interface definitions exercised indirectly by consumers, but runner/command, runner/just, and terminal have no direct or attributable indirect coverage.
- All 16 of 18 Fable findings map to an identifiable current test. Two explicit gaps found in this pass (not from the review's own gap list): resume's reset of retry-attempt counters/stale output identity (Medium #2) has no direct test, and neither runner/taskfile nor runner/just tests the runner-version cache-identity probe the review says was added (Medium #8) - runner/just has zero test coverage at all.
- Concept classification: proven items (structural digests, scheduling, cache hit/miss/corruption, durable journal, transitive resume invalidation, deterministic symlink-safe archives) are each backed by a specific test; unproven items (adapter version-probe identity, resume retry-attempt reset, any performance property) and disproven-by-baseline items (Environment as reproducible sandbox, single immutable source snapshot, current flow.Step surface as the product API) are listed with their own citations rather than restating baseline.md.

Tests/verification:
- mise exec -- task check (root), exit 0, before and after adding the evidence files.
- Per-package `go doc -all` and `go list` captured as raw evidence (see raw-inventory/godoc/, file-listing.txt, test-functions.txt).

Risks/follow-ups:
- The two explicit test gaps are handed to TF-001.05 as an open question: whether G0 needs a regression test for them first, or accepts them as residual prototype risk (neither affects Risk Lab E01-E08 semantic-model risks).
- This ticket only covers the inventory acceptance criterion of T0; TF-001.03 (W1 benchmarks), TF-001.04 (cache/concurrency characterisation), and TF-001.05 (G0 decision) remain.
- Not merged: branch tf-001.02-package-inventory in worktree /private/tmp/taskflow-tf-001.02, awaiting explicit landing authorisation.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Task-specific verification passes and commands are recorded
- [x] #2 mise exec -- task check remains green
- [x] #3 Evidence distinguishes measured facts, limitations, and open questions
- [x] #4 Backlog acceptance criteria, notes, and final summary are current
<!-- DOD:END -->
