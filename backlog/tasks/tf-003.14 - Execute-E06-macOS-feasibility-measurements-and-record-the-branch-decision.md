---
id: TF-003.14
title: Execute E06 macOS feasibility measurements and record the branch decision
status: In Progress
assignee:
  - '@codex-tf-003-14'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-04 17:11'
labels:
  - risk-lab
  - e06
  - macos
  - mobile
  - decision
  - parallel-wave-r2
dependencies:
  - TF-003.13
references:
  - docs/roadmap.md#e06-macos-vm-xcode-and-simulator-feasibility
  - fixtures/w3/
  - fixtures/t1-benchmark-harness/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E06 execution/convergence; risks R4, R8, R9.
Outcome: run the approved W3 feasibility matrix on representative macOS infrastructure and select warm VM, per-namespace VM, trusted native host, coarse external runner, serialized capacity, or stop.
Stable assumptions: infrastructure and safety procedures come from the accepted inventory ticket; no candidate is a production provider.
Parallelism: follows inventory/reservation and may run beside E02-E05. Own E06 raw measurements and decision record; use separate mutable VM/simulator state.
Test or fault scenario: cold boot, warm acquire, workspace clone, simulator ready, build/install/test/reset/cleanup, cross-namespace contamination, safe concurrency, and VM/simulator loss recovery.
Observability: base image digest, OS/Xcode/SDK/runtime/runner identity plus latency distributions, contamination verdicts, capacity, recovery, and image distribution cost.
Rollback or removal: reset/cleanup after every sample and preserve failed state only as sanitized evidence.
Versioned formats: profile/image/session evidence remains experimental until Gate 1.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Every viable inventory candidate is measured or rejected with a reproducible reason before execution
- [ ] #2 Warm workspace creation is evaluated against 3 s p95 and simulator ready-to-install against 15 s p95 without post-hoc relaxation
- [ ] #3 Two-namespace contamination and maximum safe concurrency are tested, with any serialization requirement explicit
- [ ] #4 Failure recovery and update cost are recorded and the E06 decision selects one roadmap branch with a credible native-mobile path or recommends narrowing/stopping
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Phase A anchor and repository-only preparation

1. Preserve commit 098035bf29656c3fd3b3991224a98fdded3453b7 as the accepted E06 Phase-A anchor. Do not edit any file listed by experiments/e06-macos-feasibility/frozen-artifacts.json or its protocol digest. Add TF-003.14 work only as separate Phase-B files under the claimed experiment subtree and docs/decisions/0010-e06-macos-feasibility.md. The Phase-B verifier will materialize the exact Phase-A files from 098035b in a temporary directory, run the original Phase-A verifier there, and byte-compare every frozen live file before accepting any Phase-B result; it will not weaken the original exact-fileset check.

2. Add an additive, result-free Phase-B execution contract before any candidate command runs: a Phase-B README and Taskfile, implementation/evidence manifests, candidate rejection and decision matrices, a manifest-resolution record, the eventual exact execution manifest, deterministic native iOS smoke fixture, guarded lifecycle/measurement scripts, verifier, and unit/mutation tests. Bind by SHA-256 the accepted Phase-A commit, W3 fixture files, T1 benchmark-v2 implementation, every new runner/guard/fixture byte, and the proposed E06 ADR skeleton. Keep every format experimental, forbid production/prototype imports, and make the repository-only checks exercise fake temporary resources only; no xcodebuild, simctl, VM/provider, network, install/download, signal, or cleanup operation is allowed during this phase.

3. Resolve the frozen execution-manifest schema as far as current evidence permits and fail closed on the rest. Freeze manifest id taskflow-e06-native-a; candidate trusted-native-host; operator @codex-tf-003-14; host inventory digest 9e021a326cba6e3b3b92c6cfa9f274c531d5f9cf13b95a4f314f2afc95d80630; base_image_digest null; mutable root /private/tmp/taskflow-e06-native-a; namespace-a and namespace-b workspace, HOME, TMPDIR, DerivedData, results, and custom CoreSimulator roots below it; evidence root experiments/e06-macos-feasibility/evidence/taskflow-e06-native-a; concurrency levels [1,2,3,4] with five repetitions per level; minimum free RAM 16 GiB; minimum free disk 200 GiB; thermal stop at serious; and a 900-second per-command ceiling. Compute the expected profile, runner, sandbox-policy, and reset-policy digests only after their repository bytes exist. Leave approved_by, approved_at, the exclusive window, and exact mutation scope unresolved until the user supplies them. A manifest is executable only when every field validates, all digests attest, the reservation window is current, and the exact command ledger has separate approval.

4. Implement the repository-only fixture and guard so later measurements are meaningful and bounded. The minimal deterministic iOS app will build without signing or network access, accept synthetic namespace markers, exercise app data/preferences/keychain-canary names without secret values, and emit a structured smoke-test result after explicit install and launch. The guard must resolve every path beneath the manifest root, require every simulator command to include the custom device set, require experiment-prefixed simulator names, require explicit DerivedData on xcodebuild, refuse the default simulator set and pre-existing resources, track only experiment child PIDs, and stop with an exact orphan record instead of broadening cleanup. The manifest command ledger must enumerate underlying mkdir/copy/build and xcrun simctl create, clone, boot, bootstatus, install, launch, shutdown, erase, and delete actions plus exact-path removal; killall, default-set commands, broad globs, immutable-base deletion, unscoped process signals, provider login, downloads, and network access remain forbidden.

5. Evaluate candidates in the accepted matrix order before mutation. Record warm-immutable-vm-restore, warm-vm-apfs-workspaces, and vm-per-namespace rejected because no pinned controller, immutable image, or reserved capacity exists; record coarse-external-runner rejected because no endpoint, credential mediator, profile, quota, reset, cleanup, or orphan API exists. Reconfirm those facts without installing, authenticating, downloading, or probing a provider. Admit trusted-native-host only after an exclusive window and out-of-sandbox custom CoreSimulator access are approved. Freeze simulator execution order as fresh-create-boot, erase-reset, then clone-from-golden; reject an individual mechanism before timing if pinned Xcode 26.6 syntax or custom-set support cannot pass a read-only/dry-run preflight. Command presence is never viability evidence.

6. Verify the result-free repository preparation with the immutable Phase-A check, Phase-B unit/mutation tests, exact schema validation or explicit unresolved-field diagnostics, a full command-ledger dry run, the W3 validator, T1 benchmark harness checks, git diff --check, and the repository root mise exec -- task check. Then stop and present the complete Phase-B contract/runner/fixture/ADR-skeleton diff and summary. Plan approval authorizes only this repository work; it does not authorize a commit or any external/destructive measurement. Obtain explicit approval before committing the frozen execution implementation, because its commit and digests must be immutable evidence inputs.

Separate external and destructive execution approval

7. After the repository-only implementation is accepted and committed, regenerate the final schema-valid manifest from that commit and present it verbatim with its complete command/mutation/cleanup ledger. Request a new explicit approval covering the named host, exclusive start/end window, use of only /private/tmp/taskflow-e06-native-a, creation and deletion of only experiment-owned custom-set simulators and files, xcodebuild with explicit DerivedData, signals only to recorded experiment descendants, simulator-loss/cancellation/caller-loss injections, and exact cleanup. CoreSimulator access outside the managed sandbox is an expected approval need. Approval must not cover the default device set, the currently booted simulator, user workspaces or DerivedData, VM/image installation, provider access, network use, immutable resources, or any broader cleanup. If the window, host/profile digest, command ledger, target set, or implementation commit changes, stop and obtain a fresh approval.

Approved Phase-B execution and evidence

8. Under the approved manifest, attest the host profile before every build/install/test set and first execute one deliberate profile-mismatch case that must reject before mutation. Run candidates serially except for the named concurrency probe. For trusted-native-host collect 30 warm-workspace-ready samples with p95 strictly below 3 seconds. For each admitted simulator mechanism collect 30 simulator-ready-to-install samples with p95 strictly below 15 seconds and 15 samples each for build, install, mobile smoke test, reset, and cleanup using taskflow-t1-benchmark/v2 records and raw traces. Mark cold VM boot, VM image import/update, and network image distribution not applicable to the native candidate with reproducible reasons; do not synthesize samples. Retain every failure and permit a rerun only from sample one after recording a contract-compatible operational correction. Never relax thresholds after results.

9. Exercise the frozen correctness and recovery probes with synthetic markers only: twenty deterministic alternating namespace handoffs; ten two-namespace repetitions; the [1,2,3,4] concurrency ramp with five repetitions per level and immediate stop on any identity/isolation violation, cleanup failure/orphan, RAM or disk floor, serious thermal pressure, or user safety stop; five simulator-loss cases against experiment devices only; five cancellation cases; and five caller-loss cases against the experiment client only. Verify zero workspace, HOME, TMPDIR, DerivedData, app-data, preferences, keychain-name, simulator, device-set, reservation, namespace, and lease contamination; zero unauthorized cleanup; and cleanup within the W3 30-second deadline or a precise retained orphan. VM-loss and immutable-base integrity are not applicable to the selected native mechanism and must be recorded as such. Record native Xcode/SDK/runtime update and rollback cost as an explicit unmeasured operational limitation unless the user separately supplies and approves an alternate toolchain; absence is not credited as a pass.

10. Preserve sanitized raw command traces, T1 records and samples, profile/lease/resource observations, rejection evidence, contamination matrices, concurrency and safety-stop results, recovery/cleanup timelines, update-cost ledger, failure sets, orphan records, checksums, environment, implementation manifest, evidence manifest, and limitations under the manifest evidence root. Never retain device UDIDs, host serial/UUID, user identity/path, secret values, credentials, or unredacted provider data. Reproduce all deterministic summaries into a temporary directory and compare them byte-for-byte; preserve nondeterministic raw measurements by digest and validate their internal relationships.

11. Apply the predeclared decision mechanically with correctness before latency: select stop-or-narrow if no candidate provides a bounded credible path or any zero-tolerance isolation/reset/cleanup gate fails; select serialized-macos-capacity if native execution is credible at one namespace but no clean level reaches two; otherwise select trusted-native-host only if its reset/recovery gates and both strict p95 budgets pass at concurrency two or higher. The unexecuted VM and external-runner branches cannot be selected. Record requested versus attained reproducibility honestly, then complete docs/decisions/0010-e06-macos-feasibility.md with the question/date, all options, unchanged thresholds, evidence/raw locations, selected branch and precedence, consequences, deliberately unsupported cases, revisit triggers, and Gate-1 inputs. Do not claim E07 routing, a production provider, hermetic native execution, or a universal host capacity limit.

12. Run the experiment Phase-B check, focused fixture/guard tests, immutable Phase-A verification, W3 validation, T1 record validation, repository-root mise exec -- task check, and git diff --check. Append exact outcomes and limitations through the Backlog CLI, but do not check AC/DoD, write the Final Summary, mark Done, commit, merge, push, or clean up. End at the mandatory user checkpoint with the full evidence/decision summary and complete diff; only explicit approval after that checkpoint may authorize ticket completion and any completion commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership (2026-09-04):
- Agent: @codex-tf-003-14
- Branch: tf-003.14-e06-measurements
- Worktree: /private/tmp/taskflow-tf-003.14
- Base revision: 098035bf29656c3fd3b3991224a98fdded3453b7 (current main and origin/main at planning start)
- Initially claimed paths: experiments/e06-macos-feasibility/**; docs/decisions/0010-e06-macos-feasibility.md; Backlog metadata for TF-003.14 only.
- Dependency evidence: TF-003.13 is Done. Its accepted Phase-A inventory/contract is present at base revision 098035b.
- Ownership refinement: accepted Phase-A files and protocol hashes are immutable read-only anchors; TF-003.14 additions must use separate Phase-B contract, tooling, evidence, decision, and verification files unless a separately reviewed contract correction is required.
- Coordination: docs/decisions/0010-e06-macos-feasibility.md is reserved for E06; TF-003.15 was notified and is considering 0011 for E07.
- Planning-only boundary: no implementation, measurement, simulator/VM lifecycle command, external provider action, installation/download, shared/default simulator access, host reservation, destructive cleanup, commit, merge, push, or worktree cleanup is authorized in this pass.

Planning research (2026-09-04):
- Product/roadmap boundary: E06 tests risks R4, R8, and R9 plus EXEC-3/4/5, REP-2, and Phase-B AGENT-4/5 evidence. Gate 1 needs one credible native-mobile branch; no production module, provider contract, or stabilized public package is allowed.
- Accepted dependency contract: TF-003.13 freezes warm workspace p95 strictly below 3 seconds, simulator ready-to-install p95 strictly below 15 seconds, exact sample counts/boundaries, zero-tolerance contamination/identity/cleanup gates, five repetitions per loss case, and six mutually named branches. Thresholds cannot be relaxed after observing results.
- Phase-A immutability issue resolved in the plan: its verifier requires an exact fileset and rejects Phase-B artifacts in the live tree. TF-003.14 will keep all hashed Phase-A bytes unchanged and verify them from accepted commit 098035b before validating additive Phase-B files.
- Available tooling for repository preparation: Backlog 1.44.0, Python 3.9.6, Task 3.50.0, mise 2026.4.28, Git, xcodebuild, and xcrun. Tool presence does not prove CoreSimulator access or candidate viability. No lifecycle/help probe, build, simulator command, provider action, or benchmark was run.
- Current accepted inventory: Mac17,7 Apple M5 Max arm64, 18 cores, 64 GiB, macOS 26.5.2 build 25F84, Xcode 26.6 build 17F113, iOS/iOS Simulator SDK 26.5 build 23F81a, and installed arm64 simulator runtime 26.5 build 23F77. SDK and runtime builds remain distinct profile fields.
- Current blockers: the native host is shared and unreserved; the default device set contains a recently booted device and is forbidden; managed CoreSimulator access is denied; no Tart/Orchard/macOS VM controller or digest-pinned image exists; and no external runner endpoint, credentials, profile, quota, or reservation exists.
- Next safe path: repository-only contract/fixture/guard implementation first, followed by a diff and commit approval; then a separately reviewed fully resolved manifest and exclusive-window approval for only the trusted-native-host/custom-device-set path. VM or external-runner execution would require a different fresh manifest and additional provisioning authority.
- ADR coordination: docs/decisions/0010-e06-macos-feasibility.md is claimed for this ticket; TF-003.15 was notified and its proposed 0011 E07 path does not overlap.
- No implementation or external/destructive execution occurred during this planning pass.

Repository-only Phase-B preparation (2026-09-04):
- Added 20 additive files under experiments/e06-macos-feasibility/phase-b and proposed docs/decisions/0010-e06-macos-feasibility.md. No accepted Phase-A file changed.
- The verifier reconstructs all 23 accepted Phase-A files plus their bound docs/fixtures from commit 098035b, runs the original verifier, and byte-compares the live Phase-A anchor before accepting Phase B.
- Recorded four reproducible pre-execution worker rejections. trusted-native-host remains blocked awaiting a separately approved exclusive window, current attestation, complete schema-valid external manifest, and CoreSimulator access limited to the custom device set.
- Resolved the repository-known manifest fields: taskflow-e06-native-a paths, [1,2,3,4] concurrency levels, 16 GiB RAM and 200 GiB disk floors, serious thermal stop, 900-second command limit, runner digest 512690fb200d2dfae5eaaa68e8ee8df7c9be130182e1bf32412cfd1a3c73fada, and expected profile digest ecca75a43cf785793fe187316998d55f31653815a8562b1d6c05ddf138d4b570.
- Added a deterministic unsigned iOS smoke fixture, shared scheme, synthetic file/defaults/keychain-name contamination markers, 30-command two-namespace/golden-device proposal, dry-run path/device-set/DerivedData/cleanup guards, a describe-only runner with no execution primitive, frozen scope/artifact hashes, and seven mutation tests.
- Phase-B protocol manifest digest: 12170d8effa3ce838c49c3180bd662ae879ea2a66ffe990ef5d793119af6f1bd.
- No xcodebuild, simctl, VM/provider, network, process-signal, lifecycle, cleanup, benchmark measurement, external manifest execution, or branch decision occurred.

Verification:
- task --dir experiments/e06-macos-feasibility/phase-b check: PASS; seven tests, immutable Phase-A reconstruction, contract verifier, 30-command guard dry-run, and describe-only runner.
- fixtures/w3/validate.sh: PASS; seven tests and canonical W3 examples.
- env GOCACHE=/private/tmp/taskflow-tf00314-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.14/fixtures/t1-benchmark-harness check: PASS. The first run using the default user Go cache was sandbox-denied; no test failed after using the ticket-scoped cache.
- env GOCACHE=/private/tmp/taskflow-tf00314-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.14 check: PASS outside the managed sandbox. The sandboxed attempt reached the existing malicious-planner loopback self-test and was denied at bind; the approved rerun passed the full root and race gates.
- git diff --check and the new-file trailing-whitespace audit: PASS before this note; rerun at final checkpoint.

Awaiting the mandatory diff/summary approval. No AC/DoD item, Final Summary, Done transition, commit, merge, push, external execution, or cleanup is authorized.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Explicit approval covers the exact external infrastructure mutations performed
- [ ] #2 Raw benchmark, contamination, capacity, cleanup, and recovery evidence is preserved
- [ ] #3 Task-specific checks and root verification pass
- [ ] #4 The E06 ADR and Backlog final summary are current
<!-- DOD:END -->
