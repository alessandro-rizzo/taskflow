---
id: TF-003.14
title: Execute E06 macOS feasibility measurements and record the branch decision
status: In Progress
assignee:
  - '@codex-tf-003-14'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-04 17:56'
labels:
  - risk-lab
  - e06
  - macos
  - mobile
  - decision
  - parallel-wave-r2
dependencies:
  - TF-003.13
references:
  - docs/roadmap.md#e06-macos-vm-xcode-and-simulator-feasibility
  - fixtures/w3/
  - fixtures/t1-benchmark-harness/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E06 execution/convergence; risks R4, R8, R9.
Outcome: run the approved W3 feasibility matrix on representative macOS infrastructure and select warm VM, per-namespace VM, trusted native host, coarse external runner, serialized capacity, or stop.
Stable assumptions: infrastructure and safety procedures come from the accepted inventory ticket; no candidate is a production provider.
Parallelism: follows inventory/reservation and may run beside E02-E05. Own E06 raw measurements and decision record; use separate mutable VM/simulator state.
Test or fault scenario: cold boot, warm acquire, workspace clone, simulator ready, build/install/test/reset/cleanup, cross-namespace contamination, safe concurrency, and VM/simulator loss recovery.
Observability: base image digest, OS/Xcode/SDK/runtime/runner identity plus latency distributions, contamination verdicts, capacity, recovery, and image distribution cost.
Rollback or removal: reset/cleanup after every sample and preserve failed state only as sanitized evidence.
Versioned formats: profile/image/session evidence remains experimental until Gate 1.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Every viable inventory candidate is measured or rejected with a reproducible reason before execution
- [ ] #2 Warm workspace creation is evaluated against 3 s p95 and simulator ready-to-install against 15 s p95 without post-hoc relaxation
- [ ] #3 Two-namespace contamination and maximum safe concurrency are tested, with any serialization requirement explicit
- [ ] #4 Failure recovery and update cost are recorded and the E06 decision selects one roadmap branch with a credible native-mobile path or recommends narrowing/stopping
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Phase A anchor and repository-only preparation

1. Preserve commit 098035bf29656c3fd3b3991224a98fdded3453b7 as the accepted E06 Phase-A anchor. Do not edit any file listed by experiments/e06-macos-feasibility/frozen-artifacts.json or its protocol digest. Keep TF-003.14 work under the claimed E06 experiment subtree and docs/decisions/0010-e06-macos-feasibility.md. Every later verifier must materialize the exact Phase-A files from 098035b, run the original Phase-A verifier, and byte-compare every frozen live Phase-A file before accepting Phase-B evidence.

2. Retain the result-free Phase-B contract frozen at commit 6decbbd1323fd9a69137129db234028d80b1151d, tree e80c5fb834523430890acbb91b2462fa082fae32, and protocol digest 12170d8effa3ce838c49c3180bd662ae879ea2a66ffe990ef5d793119af6f1bd. Its fixture, policies, candidate/decision matrices, rejected-candidate evidence, 30-command lifecycle proposal, dry-run guard, description-only runner, tests, and proposed ADR are immutable inputs rather than an executable measurement implementation. Never rewrite or claim that commit as execution-capable.

3. Preserve the predeclared manifest values and thresholds without relaxation: manifest taskflow-e06-native-a; candidate trusted-native-host; operator @codex-tf-003-14; accepted inventory digest 9e021a326cba6e3b3b92c6cfa9f274c531d5f9cf13b95a4f314f2afc95d80630; base image digest null; mutable root /private/tmp/taskflow-e06-native-a; namespace-private workspace, HOME, TMPDIR, DerivedData, results, and custom CoreSimulator roots; evidence root experiments/e06-macos-feasibility/evidence/taskflow-e06-native-a; concurrency [1,2,3,4] with five repetitions per level; RAM floor 16 GiB; disk floor 200 GiB; thermal stop at serious; 900-second per-command ceiling; warm workspace p95 strictly below 3 seconds; simulator ready-to-install p95 strictly below 15 seconds; and every Phase-A sample count, zero-tolerance correctness gate, stop condition, and decision branch.

4. Preserve the deterministic unsigned E06SmokeApp fixture and the safety contract: every path must resolve beneath the exact manifest root; every simulator command must use the custom device set and an experiment-prefixed name; every xcodebuild command must use explicit namespace DerivedData and signing disabled; the default device set, pre-existing resources, user paths, immutable resources, provider/network access, downloads, broad globs, killall, and unrecorded process signals are forbidden. Cleanup must retain a precise orphan and stop rather than widen scope.

5. Keep the accepted pre-execution candidate result: warm-immutable-vm-restore, warm-vm-apfs-workspaces, and vm-per-namespace are rejected because there is no pinned controller, immutable image, or reserved capacity; coarse-external-runner is rejected because there is no endpoint, credential mediator, profile, quota, reset, cleanup, or orphan API. trusted-native-host remains the only conditional candidate. The simulator order remains fresh-create-boot, erase-reset, then clone-from-golden. Tool presence is not viability evidence.

6. Preserve the completed repository-preparation verification record: immutable Phase-A and frozen Phase-B checks, Phase-B unit/mutation tests, dry-run ledger validation, W3 validation, T1 benchmark-harness checks, root mise exec -- task check, and git diff --check passed before 6decbbd. This record does not authorize execution.

Retained blocked audit and repository-only execution-runner correction

7. After explicit user approval of the current diff, commit the seven files under experiments/e06-macos-feasibility/phase-b/approval together with only the associated TF-003.14 Backlog metadata as a retained blocked-audit commit. Preserve their status as non-executing and rejected for execution: they bind 6decbbd, the 30-command lifecycle ledger, one exact-root cleanup proposal, and safe read-only host facts, while recording that approval identity/time/window, CoreSimulator attestation, and a complete execution runner/schedule are absent. Do not edit, squash away, or reinterpret those retained audit bytes; do not run any native or external command while committing them.

8. Only after that retained-audit commit, add a new execution implementation under experiments/e06-macos-feasibility/phase-b/execution without changing 6decbbd or the retained phase-b/approval subtree. Implement an execution-capable runner that defaults to non-executing describe/validate modes and can enter its execution path only with an exact schema-valid manifest, matching implementation/runner/fixture/policy/ledger digests, a current approved window, matching host and custom-device-set attestation, an explicit execution flag, and a fresh approval binding. Separate command construction from execution behind an injected backend so repository checks use only a recording fake. The runner must enforce exact-root and no-symlink ownership, custom device-set isolation, deterministic experiment device names, explicit DerivedData, network denial, bounded timeouts, recorded-child PID lineage before any signal, exact orphan reporting, and fail-closed cleanup.

9. Add a machine-expanded schedule and ledger under phase-b/execution that enumerates every command/effect before results: 30 warm-workspace-ready samples; for fresh-create-boot, erase-reset, and clone-from-golden in that order, 30 simulator-ready samples each and 15 build, install, mobile-smoke, reset, and cleanup samples each; twenty alternating namespace handoffs; ten two-namespace repetitions; concurrency levels 1, 2, 3, and 4 with five repetitions each; five simulator-loss, five cancellation, and five caller-loss cases; one deliberate profile-mismatch rejection; and the required before/after attestations, residue probes, evidence finalization, and exact cleanup. Give every operation a deterministic ID, namespace, repetition, argv or typed internal effect, expected mutation targets, timeout, prerequisites, evidence path, and cleanup action. Runtime-only device IDs and child PIDs may be consumed only from the immediately preceding owned operation, must be bound into the evidence ledger before use, and must never authorize another target. Mark VM-only boot/loss/image/update and network-distribution observations not applicable or explicitly unmeasured for the native candidate exactly as Phase A requires.

10. Add focused unit and mutation tests for the new execution subtree. Prove rejection of path traversal, symlinks, root mismatch, default or mismatched device sets, unprefixed/pre-existing devices, missing DerivedData, unrecorded/non-descendant PIDs, stale or changed approval windows, missing approval identity/time, implementation/runner/fixture/policy/ledger/profile digest drift, sample-count/order changes, threshold relaxation, unsafe cleanup, network/provider/download commands, and incomplete evidence. Patch subprocess creation, process signals, filesystem mutation, and native adapters to fail the test if repository verification reaches them; assert the recording fake receives the fully expanded schedule and that all repository checks leave execution_count zero and create no evidence or mutable-root state.

11. Run only repository-safe checks for the execution implementation: retained-audit verifier/tests; immutable Phase-A reconstruction; frozen 6decbbd verification; new execution schedule/ledger/schema/guard tests; a full fake-backend dry run; W3 validation; T1 benchmark-harness checks; repository-root mise exec -- task check; git diff --check; claimed-path/trailing-whitespace audit; and an explicit process/evidence/mutable-root absence check. Append exact results through Backlog CLI, then stop with the complete diff and summary. Do not run xcodebuild, xcrun, simctl, CoreSimulator, VM/provider/network, signals, lifecycle, cleanup, or measurements. Obtain explicit user approval before committing the new execution implementation.

Separate exact native-host execution approval

12. Only after the execution implementation is approved and committed, bind a replacement approval packet to that new immutable commit/tree and to exact hashes for the runner, fixture, policies, expanded schedule/ledger, schema, cleanup ledger, and evidence contract. Retain the 6decbbd contract and the earlier blocked packet as provenance. Collect only separately permitted read-only host facts, then require the user to supply the approval identity, approval timestamp, and exclusive start/end window. Resolve the current profile, capacity/thermal, mutable-root-absence, and custom CoreSimulator runtime/device-type/access attestations at the approved boundary. Materialize and validate the final manifest against the accepted Phase-A schema, present its exact bytes/hash plus the complete expanded command/mutation/signal/fault/cleanup ledger, and request fresh explicit native-host execution approval. Any changed commit, digest, command, target, host, profile, window, or attestation invalidates the packet and requires another approval. Approval never covers the default device set, an existing/currently booted simulator, user paths, VM/image/provider lifecycle, network, downloads, immutable resources, broad signals, broad cleanup, or an unresolved runtime handle.

Approved Phase-B execution and evidence

13. Under that approved manifest only, attest the host profile before every build/install/test set and first execute the deliberate profile-mismatch case, which must reject before mutation. Run candidates serially except for the named concurrency probe. Collect the predeclared samples: 30 warm-workspace-ready observations with p95 strictly below 3 seconds; for each admitted simulator mechanism, 30 simulator-ready-to-install observations with p95 strictly below 15 seconds and 15 observations each for build, install, mobile smoke test, reset, and cleanup. Use taskflow-t1-benchmark/v2 records and raw traces. Retain every failure; rerun only from sample one after a recorded contract-compatible operational correction. Never relax a threshold after results.

14. Exercise the frozen correctness and recovery probes with synthetic markers only: twenty alternating namespace handoffs; ten two-namespace repetitions; the [1,2,3,4] concurrency ramp with five repetitions per level and immediate stop on any identity/isolation violation, cleanup failure/orphan, RAM or disk floor, serious thermal pressure, or user safety stop; five simulator-loss cases against owned experiment devices; five cancellation cases; and five caller-loss cases against the recorded experiment client. Require zero workspace, HOME, TMPDIR, DerivedData, app-data, preferences, keychain-name, simulator, device-set, reservation, namespace, and lease contamination; zero unauthorized cleanup; and cleanup within 30 seconds or a precise retained orphan. Record VM-only probes as not applicable and native Xcode/SDK/runtime update/rollback cost as an explicit unmeasured limitation unless a separately supplied and approved alternate toolchain exists.

15. Preserve sanitized raw command traces, T1 records and samples, profile/lease/resource observations, rejection evidence, contamination matrices, concurrency and safety-stop results, recovery/cleanup timelines, update-cost ledger, failure sets, orphan records, checksums, environment, implementation manifest, evidence manifest, and limitations under the manifest evidence root. Never retain device UDIDs, host serial/UUID, user identity/path, secrets, credentials, or unredacted provider data. Reproduce deterministic summaries in a temporary directory and compare them byte-for-byte; preserve nondeterministic measurements by digest and validate their relationships.

16. Apply the predeclared decision mechanically with correctness before latency: select stop-or-narrow if no candidate provides a bounded credible path or any zero-tolerance isolation/reset/cleanup gate fails; select serialized-macos-capacity if native execution is credible at one namespace but no clean level reaches two; otherwise select trusted-native-host only if reset/recovery gates and both strict p95 budgets pass at concurrency two or higher. Unexecuted VM/external branches cannot be selected. Record requested versus attained reproducibility honestly and complete docs/decisions/0010-e06-macos-feasibility.md with the unchanged question, options, thresholds, evidence locations, selected branch and precedence, consequences, unsupported cases, revisit triggers, and Gate-1 inputs. Do not claim E07 routing, a production provider, hermetic native execution, or a universal host capacity limit.

17. Run the experiment execution/evidence checks, focused fixture/guard/runner tests, immutable Phase-A and 6decbbd verification, retained-audit verification, W3 validation, T1 record validation, repository-root mise exec -- task check, and git diff --check. Append exact outcomes and limitations through Backlog CLI, but do not check AC/DoD, write the Final Summary, mark Done, commit, merge, push, or clean up. End at the mandatory user checkpoint with the full evidence/decision summary and complete diff; only explicit approval after that checkpoint may authorize ticket completion and any completion commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership (2026-09-04):
- Agent: @codex-tf-003-14
- Branch: tf-003.14-e06-measurements
- Worktree: /private/tmp/taskflow-tf-003.14
- Base revision: 098035bf29656c3fd3b3991224a98fdded3453b7 (current main and origin/main at planning start)
- Initially claimed paths: experiments/e06-macos-feasibility/**; docs/decisions/0010-e06-macos-feasibility.md; Backlog metadata for TF-003.14 only.
- Dependency evidence: TF-003.13 is Done. Its accepted Phase-A inventory/contract is present at base revision 098035b.
- Ownership refinement: accepted Phase-A files and protocol hashes are immutable read-only anchors; TF-003.14 additions must use separate Phase-B contract, tooling, evidence, decision, and verification files unless a separately reviewed contract correction is required.
- Coordination: docs/decisions/0010-e06-macos-feasibility.md is reserved for E06; TF-003.15 was notified and is considering 0011 for E07.
- Planning-only boundary: no implementation, measurement, simulator/VM lifecycle command, external provider action, installation/download, shared/default simulator access, host reservation, destructive cleanup, commit, merge, push, or worktree cleanup is authorized in this pass.

Planning research (2026-09-04):
- Product/roadmap boundary: E06 tests risks R4, R8, and R9 plus EXEC-3/4/5, REP-2, and Phase-B AGENT-4/5 evidence. Gate 1 needs one credible native-mobile branch; no production module, provider contract, or stabilized public package is allowed.
- Accepted dependency contract: TF-003.13 freezes warm workspace p95 strictly below 3 seconds, simulator ready-to-install p95 strictly below 15 seconds, exact sample counts/boundaries, zero-tolerance contamination/identity/cleanup gates, five repetitions per loss case, and six mutually named branches. Thresholds cannot be relaxed after observing results.
- Phase-A immutability issue resolved in the plan: its verifier requires an exact fileset and rejects Phase-B artifacts in the live tree. TF-003.14 will keep all hashed Phase-A bytes unchanged and verify them from accepted commit 098035b before validating additive Phase-B files.
- Available tooling for repository preparation: Backlog 1.44.0, Python 3.9.6, Task 3.50.0, mise 2026.4.28, Git, xcodebuild, and xcrun. Tool presence does not prove CoreSimulator access or candidate viability. No lifecycle/help probe, build, simulator command, provider action, or benchmark was run.
- Current accepted inventory: Mac17,7 Apple M5 Max arm64, 18 cores, 64 GiB, macOS 26.5.2 build 25F84, Xcode 26.6 build 17F113, iOS/iOS Simulator SDK 26.5 build 23F81a, and installed arm64 simulator runtime 26.5 build 23F77. SDK and runtime builds remain distinct profile fields.
- Current blockers: the native host is shared and unreserved; the default device set contains a recently booted device and is forbidden; managed CoreSimulator access is denied; no Tart/Orchard/macOS VM controller or digest-pinned image exists; and no external runner endpoint, credentials, profile, quota, or reservation exists.
- Next safe path: repository-only contract/fixture/guard implementation first, followed by a diff and commit approval; then a separately reviewed fully resolved manifest and exclusive-window approval for only the trusted-native-host/custom-device-set path. VM or external-runner execution would require a different fresh manifest and additional provisioning authority.
- ADR coordination: docs/decisions/0010-e06-macos-feasibility.md is claimed for this ticket; TF-003.15 was notified and its proposed 0011 E07 path does not overlap.
- No implementation or external/destructive execution occurred during this planning pass.

Repository-only Phase-B preparation (2026-09-04):
- Added 20 additive files under experiments/e06-macos-feasibility/phase-b and proposed docs/decisions/0010-e06-macos-feasibility.md. No accepted Phase-A file changed.
- The verifier reconstructs all 23 accepted Phase-A files plus their bound docs/fixtures from commit 098035b, runs the original verifier, and byte-compares the live Phase-A anchor before accepting Phase B.
- Recorded four reproducible pre-execution worker rejections. trusted-native-host remains blocked awaiting a separately approved exclusive window, current attestation, complete schema-valid external manifest, and CoreSimulator access limited to the custom device set.
- Resolved the repository-known manifest fields: taskflow-e06-native-a paths, [1,2,3,4] concurrency levels, 16 GiB RAM and 200 GiB disk floors, serious thermal stop, 900-second command limit, runner digest 512690fb200d2dfae5eaaa68e8ee8df7c9be130182e1bf32412cfd1a3c73fada, and expected profile digest ecca75a43cf785793fe187316998d55f31653815a8562b1d6c05ddf138d4b570.
- Added a deterministic unsigned iOS smoke fixture, shared scheme, synthetic file/defaults/keychain-name contamination markers, 30-command two-namespace/golden-device proposal, dry-run path/device-set/DerivedData/cleanup guards, a describe-only runner with no execution primitive, frozen scope/artifact hashes, and seven mutation tests.
- Phase-B protocol manifest digest: 12170d8effa3ce838c49c3180bd662ae879ea2a66ffe990ef5d793119af6f1bd.
- No xcodebuild, simctl, VM/provider, network, process-signal, lifecycle, cleanup, benchmark measurement, external manifest execution, or branch decision occurred.

Verification:
- task --dir experiments/e06-macos-feasibility/phase-b check: PASS; seven tests, immutable Phase-A reconstruction, contract verifier, 30-command guard dry-run, and describe-only runner.
- fixtures/w3/validate.sh: PASS; seven tests and canonical W3 examples.
- env GOCACHE=/private/tmp/taskflow-tf00314-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.14/fixtures/t1-benchmark-harness check: PASS. The first run using the default user Go cache was sandbox-denied; no test failed after using the ticket-scoped cache.
- env GOCACHE=/private/tmp/taskflow-tf00314-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.14 check: PASS outside the managed sandbox. The sandboxed attempt reached the existing malicious-planner loopback self-test and was denied at bind; the approved rerun passed the full root and race gates.
- git diff --check and the new-file trailing-whitespace audit: PASS before this note; rerun at final checkpoint.

Awaiting the mandatory diff/summary approval. No AC/DoD item, Final Summary, Done transition, commit, merge, push, external execution, or cleanup is authorized.

- Approved frozen-contract commit created locally: `6decbbd1323fd9a69137129db234028d80b1151d` (`feat(e06): freeze Phase B execution contract`), tree `e80c5fb834523430890acbb91b2462fa082fae32`. No push, merge, AC/DoD updates, or native execution.
- Post-commit work is limited to an uncommitted manifest/approval proposal bound to that immutable commit; exact execution still requires a new user approval with operator identity, approval timestamp, exclusive window, and CoreSimulator attestation.

Post-commit manifest finalization proposal (uncommitted):
- Bound immutable implementation commit 6decbbd1323fd9a69137129db234028d80b1151d, tree e80c5fb834523430890acbb91b2462fa082fae32, Phase-B protocol 12170d8effa3ce838c49c3180bd662ae879ea2a66ffe990ef5d793119af6f1bd, execution schema 396ad8f5ebad541c2144f0db1e5578e11a4c2872f37e72766231911d1c801ea2, frozen 30-command ledger ce8592f5be1075361f51ae3bc60b55cdd0ad7254deb65fbb2a4a9124fa082db0, and proposed one-command cleanup ledger db43ac71bb1088c8e4e122bd2e365e8687c715499031322a3345ad8bd7a649c1.
- Refreshed only sw_vers product/build and uname architecture with safe read-only commands; all match the accepted profile. xcodebuild, xcrun, simctl, CoreSimulator, VM/provider/network, signals, lifecycle, cleanup, and measurement commands were not run.
- Approval identity/time/window, execution-window capacity/thermal facts, mutable-root absence, and CoreSimulator runtime/device-type/custom-set access remain unresolved.
- Additional fail-closed blocker found: the committed runner is description-only and the 31 proposed exact commands cover one fixture lifecycle, not the complete predeclared measurement/fault schedule. An execution-capable runner and expanded exact ledger would change the immutable commit/digests and therefore require another reviewed commit plus a replacement fresh approval packet before execution.
- Safe proposal verifier: PASS. Five mutation/unit tests: PASS. git diff --check: PASS before this note; rerun at handoff. No external/native execution or second commit occurred.

Planning correction after blocked approval-packet audit (2026-09-04):
- The earlier step 7 was invalid because commit 6decbbd deliberately freezes a description-only runner, while the 30-command ledger plus one cleanup command covers only a bounded fixture lifecycle rather than the full predeclared measurement and fault schedule. The blocked seven-file packet is retained as negative audit evidence and must never be used as execution authority.
- Corrected sequence: obtain approval to commit the seven-file blocked audit; add a separately frozen execution-capable runner and fully expanded schedule/ledger; prove safety and zero repository-check execution with mutation tests; stop for a new diff/commit approval; then bind a replacement schema-valid manifest to that new commit and request fresh exact native-host approval.
- Proposed additional claimed paths: experiments/e06-macos-feasibility/phase-b/approval/** for the current retained audit only; experiments/e06-macos-feasibility/phase-b/execution/** for the runner, expanded schedule/ledger, guards, evidence contract, and repository-only tests; experiments/e06-macos-feasibility/phase-b/execution-approval/** for the post-commit replacement manifest/approval packet; experiments/e06-macos-feasibility/evidence/taskflow-e06-native-a/** only after exact execution approval; docs/decisions/0010-e06-macos-feasibility.md for the eventual decision; and TF-003.14 metadata only.
- Immutable/read-only anchors remain experiments/e06-macos-feasibility Phase A at 098035b, the frozen Phase-B contract at 6decbbd, and—after approval—the seven-file rejected packet commit. No overlapping shared contract, another ticket metadata file, default simulator state, or user mutable path is claimed.
- This correction is planning-only. No source/fixture/runner implementation changed, no commit was created, and no Xcode/CoreSimulator, VM/provider/network, signal, lifecycle, cleanup, or measurement action ran.
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 Explicit approval covers the exact external infrastructure mutations performed
- [ ] #2 Raw benchmark, contamination, capacity, cleanup, and recovery evidence is preserved
- [ ] #3 Task-specific checks and root verification pass
- [ ] #4 The E06 ADR and Backlog final summary are current
<!-- DOD:END -->
