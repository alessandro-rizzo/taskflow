---
id: TF-003.16
title: Run E08 minimal remote-worker protocol experiment
status: Done
assignee:
  - '@codex-tf-003-16'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-05 18:46'
labels:
  - risk-lab
  - e08
  - worker-protocol
  - parallel-wave-r2
dependencies:
  - TF-003.03
  - TF-003.13
references:
  - docs/roadmap.md#e08-minimal-remote-worker-protocol
  - fixtures/w2/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E08; risks R5 and R9; requirements REM-1 through REM-5 and EXEC-4.
Outcome: find the smallest provider-neutral worker protocol that survives an in-process fake, SSH-backed Linux worker, and a macOS adapter stub shaped by E06 inventory.
Scope: capability/profile advertisement, non-blocking reservation, attestation, sandbox/session creation, CAS materialization, command/log/cancel/exit, output publication, bounded cleanup, reconnect, and orphan query.
Stable assumptions: no provider-specific universal option map; transport choice follows state-machine semantics.
Parallelism: begins after E06 establishes an initial macOS shape; may finish shortly after Gate 1 if it does not affect the first local slice. Own experiments/e08-worker-protocol only.
Test or fault scenario: ready cache hit before reservation, profile mismatch, transfer corruption, disconnect/reconnect, worker loss, cancellation, output publication failure, cleanup timeout, and orphan reconciliation.
Observability: protocol transitions, reservation/lease identity, attestation, transfer digests, logs/cursors, cancellation, cleanup, and reconnect state.
Rollback or removal: worker processes and SSH resources are experimental and cleaned within a declared bound.
Versioned formats: state-machine and wire envelopes are explicitly experimental; transport is not frozen.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 One typed core protocol drives an in-process fake and SSH-backed Linux worker, with a macOS stub reflecting E06’s initial shape
- [x] #2 Reservation is non-blocking, profile attestation precedes execution, and cache hits require no worker wake or acquisition
- [x] #3 CAS materialization, execution/logging, cancellation, output publication, bounded cleanup, reconnect, and orphan query have success and failure tests
- [x] #4 The decision chooses one typed core, separated worker/sandbox/session protocols, or state-machine-first transport deferral without provider option leakage
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Freeze a contract-only Phase A under experiments/e08-worker-protocol before writing any protocol implementation. Bind by SHA-256 the complete W2 v1 graph and golden scenarios, the accepted integrity-fault fixture, the E04 cache/profile evidence and selected limitations, and the accepted E06 contract, profile identity, lifecycle shape, candidate matrix, and infrastructure blocker. Record the canonical EXEC-1..5, REP-1, REP-3..6, AGENT-3, AGENT-5, DUR-1, and DUR-3 mapping; preserve REM-1..5 only as stale ticket provenance. Declare the experiment and every state or wire envelope experimental, forbid production imports and provider-specific option bags, and expose a dependency-free Phase A verifier plus mutation tests.

2. Freeze the semantic state machine before implementation in machine-readable protocol/state-transition artifacts. Keep controller node state separate from capacity and resource lifecycles: ready -> cache_lookup -> cache_hit terminal, or cache_miss -> reservation_requested -> reserved -> worker_acquired -> attested -> sandbox_created -> inputs_materialized -> executing -> publishing -> succeeded; cancellation/failure may enter cancelling, reconnecting, lost, or orphaned before bounded cleanup and released. Define independent reservation/lease ownership, reusable worker identity, disposable sandbox identity, and optional namespace-owned session identity. Make every transition idempotent by operation ID, revision, and expected prior state; persist reason codes, monotonic event sequence, attempt, profile/transfer digests, cleanup deadline, and reconnect token before acknowledging transitions that can affect retry or ownership. Terminal node success remains distinct from cleanup_warning plus an orphan record.

3. Freeze the smallest typed semantic surface and experimental envelopes. Model capability/profile advertisement from cached provider snapshots; non-blocking TryReserve returning granted, queued, mismatch, or unavailable without provisioning; worker acquisition and exact-profile attestation; sandbox create/materialize; separate session acquire/reset/release for the E06 macOS shape; CAS Has/Get/Put operations with digest, size, media type, and resumable chunk identity; Exec, ordered Logs(cursor), Cancel, Wait/Exit; atomic Publish; Cleanup; Reconnect/Heartbeat; and QueryOrphans. Separate typed core commands/events/errors from transports. Provider extensions are discriminated, versioned capability values owned by an adapter, never map[string]any or project-visible universal options. Secret bytes, ambient SSH config, credentials, host paths, and provider instance options never enter plans, cache keys, durable events, or logs.

4. Freeze numeric thresholds and sample rules before Phase B. Run every correctness/fault case at least five times per adapter shape, retaining failed attempts. For 30 ready-result-hit samples, require p95 below 300 ms and exactly zero reservation, worker-wake/acquire, attestation, sandbox/session, execution, publication, and cleanup events. For 30 warm SSH/Linux cache-miss samples, measure cache miss through sandbox ready and require p95 below 2 seconds excluding declared queue time. Require TryReserve to return a typed immediate disposition within 100 ms while acquisition is deliberately delayed, proving no scheduler-thread provisioning. Require cancellation acknowledgement within 1 second; require execution/process termination, reservation release, and experiment-owned sandbox/session cleanup within 30 seconds or an exact durable orphan. Require log replay after reconnect to be gap-free, duplicate-free by sequence identity, and byte-equivalent from the last acknowledged cursor; require zero stale-success, corrupt-input use, profile rewrite, double publication, cross-attempt output, leaked reservation, unrecorded orphan, or cleanup of unowned resources. Run sample sets serially in a frozen order, distinguish queueing from service time, bind T1 benchmark-v2 records where timing applies, and forbid post-result threshold relaxation.

5. Freeze a fault matrix that maps every injected boundary to expected state, observable events, retry rule, ownership outcome, and raw trace path. Cover ready cache hit before any reservation; capability/profile mismatch before and during attestation; provider unavailable before acquisition; CAS missing blob, corrupted chunk, digest mismatch, partial materialization, and manifest tamper before use; disconnect before exec acknowledgement, during execution/log streaming, during publication, and during cleanup; permanent worker loss; cancel before placement and while running; non-zero exit versus infrastructure loss; duplicate/reordered command delivery; stale reconnect token; output collection failure, digest failure, and atomic publication failure; cleanup timeout; caller loss/lease expiry; orphan query/reconcile; and resume on a second compatible worker without rerunning or republishing the successful W2 build. Each injected case must have a stable typed error distinct from command failure.

6. Add a first mandatory review boundary after Phase A only. The proposed Phase A files are README.md, Taskfile.yml, contract.json, state-machine.json, envelopes.schema.json, thresholds.json, fault-matrix.json, decision-matrix.json, fixture-bindings.json, frozen-artifacts.json, protocol.sha256, scripts/verify_contract.py, and contract mutation tests, all under the claimed experiment subtree. Show the complete diff and verification results and obtain explicit approval before checking any DoD item, committing the frozen contract, or starting implementation. After approval, create the dedicated Phase A contract commit; Phase B must verify the contract from that immutable commit rather than rewriting thresholds or semantics.

7. Implement the approved Phase B as disposable experiment-local Go code with no new production module or public package. Drive all three adapter shapes through the same controller/state-machine harness and typed semantic commands: a deterministic in-process worker with controllable clock/faults; an SSH-backed Linux adapter whose transport is a replaceable newline-framed request/event stream over one separately approved SSH invocation; and a macOS adapter stub that implements the E06 worker/sandbox/session boundaries and profile fields without simctl, Xcode, VM, filesystem, or host lifecycle mutations. Keep transport framing, reconnect token encoding, and wire version explicitly experimental; do not claim gRPC, Connect, HTTP, or SSH as a stabilized transport.

8. Make cache and attestation ordering executable and auditable. Resolve the immutable profile digest and complete W2 result key before placement, perform and verify the ready-result lookup first, and return typed artifact handles immediately on a hit. On a miss only, obtain a reservation, acquire/wake a worker, attest every planned profile field, and reject or quarantine mismatch before sandbox/session creation. A mismatch cannot mutate the planned profile or convert a planned hit into a miss. Treat result cache, mutable tool cache, and warm worker/provider state as different types and event namespaces.

9. Exercise CAS, execution, logs, cancel, publication, and cleanup end to end. Materialize only digest-verified immutable source and dependency manifests into an attempt-owned sandbox; verify every received chunk and final object before use. Execute the W2 build/test-shaped command through a bounded child-process runner, stream ordered structured stdout/stderr chunks with resumable cursors, make cancel idempotent, distinguish exit status from transport loss, collect only declared output paths with symlink/path confinement, and publish manifests atomically with compare-and-swap so partial or losing attempts never become visible. Run cleanup on a cancellation-detached deadline context, release reservation/session exactly once, and record cleanup_warning plus exact orphan identity when the 30-second bound cannot be met.

10. Exercise reconnect, loss, resume, and orphan reconciliation against the frozen W2 expectations. Reconnect with run/node/attempt identity, last durable revision, and last acknowledged log cursor; replay durable events/logs without gaps and reject stale ownership. Query exact experiment-owned reservations, workers, sandboxes, sessions, executions, publications, and cleanup records after controller/transport loss. For permanent worker loss, supersede the lost attempt, reserve a distinct compatible worker, rematerialize and reverify the original artifact, and resume only the affected node while preserving exactly one successful upstream build/publication. Never infer success from a disconnected process or reuse partial outputs.

11. Gate real SSH work behind a fresh ssh-availability.json and explicit user approval. Before any network call, pin endpoint/port and host-key fingerprint, approved user and credential mediator, immutable Linux profile plus runner digest, experiment-owned remote root and process/name prefix, capacity/reservation scope, exact command allowlist, connection and fault timeouts, cleanup allowlist, and orphan-query procedure. Use strict host-key checking and an experiment-owned known-hosts file; disable interactive password/host-key prompts and ambient forwarding; never discover or consume arbitrary SSH config, agent identities, or user shell startup state. No sudo, install, provider provisioning, shared directory, broad pkill/killall, or delete outside the exact manifest. If availability or approval is absent, preserve the in-process and macOS-stub evidence as partial only, report SSH as a blocker, leave AC #1 and all dependent decision gates unpassed, and do not substitute localhost/macOS or a mocked SSH command for Linux evidence.

12. Preserve raw JSONL traces and summarized scorecards for every adapter and fault case, with protocol transition, revision, run/node/attempt, reservation/lease, worker/sandbox/session, planned and attested profile, CAS/object/chunk digest, log cursor, cancellation, publication, cleanup, reconnect, and orphan fields. Sanitize credentials, host details beyond the approved manifest, environment values, source bytes, and remote paths. The evidence verifier must check frozen-contract hashes, all state and fault coverage, threshold calculations, adapter provenance, W2 invariants, no provider option leakage, no macOS lifecycle calls, SSH manifest/approval binding, and decision precedence. Verify with gofmt, go test -race ./... in the experiment module, mise exec -- task --dir experiments/e08-worker-protocol check, repository-root mise exec -- task check, and git diff --check.

13. Produce docs/decisions/0012-e08-worker-protocol.md only from verified evidence, following the existing ADR convention: question/date, options, frozen thresholds, raw evidence, chosen branch, consequences/unsupported cases, revisit triggers, and contracts allowed to stabilize. Decision precedence is: stop/narrow if correctness, ownership, or cleanup gates fail; state-machine-first transport deferral if semantics pass but reconnect/framing/SSH transport dominates or lacks representative evidence; separated worker/sandbox/session protocols if the E06 stub cannot preserve Linux semantics without lifecycle leakage; otherwise one typed core with typed capability extensions if all three shapes and hard gates pass. Even the success branch freezes only the proven semantic core for Gate 1/T6; transport and envelopes stay experimental.

14. At Phase B completion, update implementation notes with exact evidence and limitations but stop before checking AC/DoD, adding the final summary, setting Done, or committing. Show the user the full summary and diff and wait for explicit approval as required. Only separately authorized landing work may then check criteria, add the PR-style final summary, commit, update against current main, rerun all checks, merge/push through the serialized integrator, verify HEAD equals origin/main, and clean up the isolated worktree.

15. Preserve the accepted checkpoints as immutable baselines. Phase A remains commit fe41c6428c4d7d432cdd463c82dd12c3465e1103 and the partial non-SSH evidence remains commit 3cb71951a36a1a56fa957a0ddea641d1c5ef0669. Do not edit any frozen Phase A artifact. Extend only experiments/e08-worker-protocol/**, docs/decisions/0012-e08-worker-protocol.md, and TF-003.16 metadata; retain the existing two-adapter evidence and invalidated benchmark set through Git history and rebind the final three-adapter implementation/evidence manifests.

16. After explicit approval of this revised plan, implement repository code only, without starting infrastructure or opening SSH. Add a taskflow-e08-envelope/v1-experimental newline-JSON codec, a persistent Linux worker daemon, a proxy subcommand, and an SSHLinuxAdapter implementing the existing Adapter/Worker/Sandbox interfaces. The worker must persist operation ID, payload digest, expected revision, events, log chunks, result/publication identity, reservations, attempt ownership, cleanup deadline, and orphans before acknowledgement. Parser/codec unit tests may use local pipes only and must never be reported as SSH success evidence. Keep the transport replaceable and experiment-local.

17. Make the remote process model survive a transport drop. Run two warm worker-daemon identities, worker-a and worker-b, inside one isolated Linux OpenSSH container; each owns an exact Unix socket, PID/start token, journal, CAS root, and attempt subtree beneath /config/taskflow-e08-ssh-linux. Each SSH command runs only the experiment proxy, which forwards the frozen envelopes to a selected daemon. A killed client SSH process therefore drops transport without killing durable worker state; reconnect uses run/node/attempt, revision, cursor, and operation identity. Worker-a may terminate itself only for the declared permanent-worker-loss injection; worker-b provides the distinct compatible identity for W2 resume without claiming a second host.

18. Stop at a mandatory repository diff checkpoint before infrastructure. Show the SSH adapter/worker, provisioning/collector scripts, schema-compatible manifest template, tests, verifier changes, proposed evidence paths, and ADR update strategy. Obtain separate user approval before any registry lookup/download, key generation, Colima/VM/container action, port listener, Docker mutation, SSH connection, fault injection, or cleanup.

19. On separately approved infrastructure materialization only, create the single ownership root /private/tmp/taskflow-e08-ssh-linux with mode 0700. Set COLIMA_HOME=/private/tmp/taskflow-e08-ssh-linux/colima, COLIMA_CACHE_HOME=/private/tmp/taskflow-e08-ssh-linux/cache, and DOCKER_CONFIG=/private/tmp/taskflow-e08-ssh-linux/docker so Colima, Lima metadata, VM/cache bytes, Docker contexts, credentials, and runtime data cannot use or alter the existing ~/.colima/default profile or global Docker configuration. Capture starting df/du and fail closed if the experiment root exceeds 12 GiB actual usage.

20. Create only Colima profile e08-ssh-linux with aarch64, 2 CPUs, 2 GiB memory, 10 GiB data disk, 10 GiB root disk, Docker runtime, no Kubernetes, no host mounts, no SSH-agent forwarding, no ~/.ssh/config update, and no Docker-context activation: --activate=false --ssh-config=false --ssh-agent=false --mount none --arch aarch64 --cpus 2 --memory 2 --disk 10 --root-disk 10 --runtime docker --kubernetes=false. Do not repair, start, stop, delete, inspect SSH configuration for, or otherwise mutate the existing broken default profile.

21. Use one ARM64 LinuxServer OpenSSH Server image only after resolving and recording an immutable registry manifest and platform-child digest; never run a mutable tag as evidence. The materialization approval must cover only the image/Colima HTTPS downloads, the isolated VM/profile, an internal Docker network taskflow-e08-ssh-net, named config volume taskflow-e08-ssh-config, and container taskflow-e08-ssh-linux. Bind container port 2222 only to fixed host endpoint 127.0.0.1:22216; use restart=no, password authentication false, sudo false, no host bind mounts, finite CPU/memory/PID limits, and no package installation. If the port is occupied, image resolution differs, the internal network cannot publish loopback safely, or the image/VM/runtime exceeds the disk cap, stop for a revised manifest rather than auto-selecting a port, tag, network, or broader capability.

22. Generate one experiment-only Ed25519 host key and one client key under the 0700 ownership root with non-interactive explicit filenames and empty passphrases. Never read or modify ~/.ssh, SSH agents, or ambient identities; never commit/log the private keys. Seed the container's isolated /config volume with the host public/private key and client public key before sshd starts, cross-compile the Linux ARM64 worker locally, record its SHA-256, copy only that binary into /config/e08/bin, and start the two exact daemons through the profile-scoped Docker socket. Container/Docker readback must prove uname reports Linux/aarch64, /proc exists, the process is real OpenSSH sshd, the image and runner digests match, forwarding/password/sudo are disabled, and only the declared root/processes exist.

23. Before the first SSH connection, build experiments/e08-worker-protocol/ssh-availability.json against the frozen schema and experiments/e08-worker-protocol/approved/known_hosts from the pre-generated host public key, pinning 127.0.0.1:22216, ssh-ed25519 fingerprint, user e08worker, experiment-key wrapper, full Linux profile and runner digests, /config/taskflow-e08-ssh-linux, taskflow-e08-ssh-linux prefix, one active attempt, expiry, exact proxy commands, fault IDs/timeouts/process targets, cleanup allowlists, evidence destination, and orphan query. Show the complete manifest and infrastructure readback and obtain a second explicit approval for the exact SSH connections, fault mutations, and cleanup. Approval of code or materialization alone is not SSH approval.

24. Invoke only /usr/bin/ssh with no ambient configuration or agent: -F none, BatchMode=yes, IdentitiesOnly=yes, IdentityAgent=none, the explicit experiment client key, StrictHostKeyChecking=yes, the experiment known_hosts file, GlobalKnownHostsFile=/dev/null, HostKeyAlgorithms=ssh-ed25519, password and keyboard-interactive authentication disabled, forwarding/X11/local commands disabled, RequestTTY=no, bounded connect/keepalive timeouts, the fixed user/host/port, and the allowlisted proxy command. Reject prompts, host-key drift, different endpoints/users/keys, shell/login sessions, arbitrary remote commands, paths outside the owned root, and any secret/environment value entering cache keys, events, or evidence.

25. Exercise all frozen semantics through the real SSH adapter. A ready cache hit must open zero SSH connections and reserve zero capacity. TryReserve must use only the attested cached capability snapshot and return within 100 ms while Acquire is delayed. On misses, attest the complete planned profile before sandbox creation; transfer W2 source/dependencies via chunk/final digest-verified CAS; execute only allowlisted W2-shaped commands; persist ordered stdout/stderr cursor digests; collect confined declared outputs; compare-and-swap publish; release reservations exactly once; and query exact orphan identities. Run 30 warm SSH/Linux admission samples at p95 below 2 seconds excluding queueing, 30 ready hits, 30 TryReserve samples, five cancel samples, five cleanup samples, and five repetitions for every applicable fault, retaining every failed/invalidated set.

26. Use real fault boundaries without broad host actions: terminate only the controller-owned local ssh PID for disconnect-before-ack/log/publication/cleanup cases; reconnect through a new pinned OpenSSH process; have worker-a self-terminate from its exact recorded PID/start token for permanent loss; expire only the named attempt lease for caller loss; delay/fail only the named cleanup target; corrupt only copied experiment chunks/manifests; and use worker-b for distinct-compatible W2 resume. Verify no second execution/publication, no stale success, gap or byte drift, no profile rewrite/corrupt use, no cross-attempt output, and each resource released once or durably orphaned. Never stop sshd/Colima or kill/delete broadly as a fault shortcut.

27. Regenerate sanitized three-adapter evidence without overwriting the frozen contracts: 15 rows per fault path (three adapters times five), T1 benchmark-v2 records, SSH/worker/container provenance, connection and mutation counters, implementation/evidence manifests, scorecard, and limitations. The verifier must distinguish actual OpenSSH/Linux evidence from unit/state-machine evidence, validate the strict availability manifest/known-host binding and exact digests, prove cache hits made no connection, recompute thresholds/hard gates, scan for option/secret/provider leakage, and ensure no path/process outside the manifest appears.

28. Recompute ADR 0012 strictly in frozen precedence. Select stop/narrow for any exercised integrity/ownership/idempotency/publication/cleanup/orphan failure; retain state-machine-first deferral if real reconnect/framing semantics or missing representative evidence still dominate; select separated protocols if the macOS session shape distorts stateless Linux; otherwise select one typed core with capability extensions only if the in-process, real SSH/Linux, and macOS-stub shapes pass every hard gate. Even a local SSH success proves actual OpenSSH, host-key enforcement, transport loss/reconnect, Linux process ownership, and cleanup semantics only; it does not prove WAN latency/jitter/loss, NAT/firewall behavior, provider provisioning/quotas, external credential mediation, physical host loss, or cross-host recovery.

29. Perform cancellation-detached cleanup on success, failure, and interruption. Through exact Docker ownership, stop/remove only taskflow-e08-ssh-linux, taskflow-e08-ssh-net, taskflow-e08-ssh-config, worker-a/worker-b processes, and /config/taskflow-e08-ssh-linux; then run colima delete e08-ssh-linux --data --force with the isolated COLIMA_HOME. Verify no named container/network/volume/profile/listener/process remains. After validating the canonical path equals /private/tmp/taskflow-e08-ssh-linux and evidence/private-key separation, delete only that root and record final df/du/reclaimed bytes. The existing approximately 57 GiB ~/.colima tree is explicitly unowned and unchanged; reclaiming it requires a different user-approved task.

30. Run the SSH-specific verifier and fault tests, gofmt, go vet, go test -race ./... in the experiment module, bound W2/integrity/T1/E06 checks, repository-root mise exec -- task check, manifest/evidence validation after infrastructure removal, git diff --check, and a secret/path scan. Append exact evidence and cleanup readback via Backlog CLI, then stop at the mandatory final evidence/diff checkpoint. Do not check AC/DoD, add Final Summary, mark Done, commit, merge, push, or remove the worktree without a further explicit approval.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership (2026-09-04):
- Agent: @codex-tf-003-16
- Branch: tf-003.16-e08-worker-protocol
- Worktree: /private/tmp/taskflow-tf-003.16
- Base: main@098035b
- Initially claimed paths: experiments/e08-worker-protocol/**; docs/decisions/0012-e08-worker-protocol.md; Backlog metadata for TF-003.16 only.
- Planning-only boundary: no experiment implementation, SSH connection, external/shared resource mutation, commit, merge, push, or cleanup before explicit approval.

Dependency and contract research (2026-09-04):
- Backlog CLI confirms declared dependencies TF-003.03 and TF-003.13 are Done. TF-003.03 freezes a reject-all symlink policy and rooted reads for its fixture while explicitly not claiming an atomic production snapshot; E08 must bind and consume immutable fixture bytes without widening that guarantee.
- TF-003.13 freezes the E06 initial shape: reusable attested native host or macOS VM worker; disposable workspace/DerivedData sandbox; namespace-owned custom-simulator session and lease. Profile identity excludes ephemeral worker, workspace, simulator, reservation, namespace, and lease IDs. The macOS leg in E08 is a stub only and must not call simctl or mutate VM/simulator state.
- W2 is specification-only and provider-neutral. It requires pre-placement profile identity, manifest verification before use, compatible-worker resume without repeating successful upstream work, and durable distinct provider-outage, worker-loss, corruption, and cancellation outcomes.
- docs/product-specification.md defines no REM-* requirement IDs. TF-003.16 retains REM-1 through REM-5 as stale ticket provenance only; the live mapping is EXEC-1 through EXEC-5, REP-1, REP-3 through REP-6, AGENT-3, AGENT-5, DUR-1, and DUR-3, with no invented aliases.
- Decision ADR numbering is reserved by experiment order: docs/decisions/0012-e08-worker-protocol.md, leaving 0007 through 0011 available for E03 through E07 convergence records.
- Available local tools for a later approved implementation: backlog, git, mise, task, Go 1.25.12, Python 3, ssh, and ssh-keyscan. Tool presence is not SSH endpoint, credential, Linux-profile, authorization, or execution evidence.

External SSH boundary:
- No SSH endpoint, port, host-key fingerprint, user, credential mediator, Linux profile manifest, remote experiment root, capacity reservation, or cleanup allowlist has been supplied or approved. Phase A is fully local and must not contact SSH resources.
- Before Phase B may open any SSH connection, a fresh read-only availability manifest must pin the endpoint and host key, identity/credential mediator, attested Linux profile and worker runner digest, experiment-owned remote root/name prefix, capacity/exclusivity, allowed commands, timeout/fault-injection scope, evidence destination, and exact cleanup/orphan-query procedure. The user must explicitly approve that manifest and the exact network/resource mutations. Unknown host keys, interactive prompts, ambient SSH config/agent credentials, shared roots, broad process kills, installation, sudo, provider creation/deletion, or cleanup outside the allowlist fail closed.
- If no approved representative Linux endpoint exists, Phase B may implement and validate the in-process core plus macOS stub but must preserve SSH as an explicit blocker; it cannot satisfy AC #1 or select the one-core success branch from synthetic transport evidence.

Phase A contract implemented for review (2026-09-04):
- Added only the approved repository contract under experiments/e08-worker-protocol/**: README and Taskfile; contract, six separated state machines with 90 transitions, closed typed envelope and SSH-manifest schemas, five predeclared timing metrics, twelve hard gates, 26 fault cases, four ordered decision branches, 35 digest-pinned inputs across five binding groups, frozen-artifact manifest/digest, verifier, and nine mutation tests.
- The state model keeps node outcome, reservation, worker attachment, sandbox, optional session, and publication ownership distinct. Ready verified cache hits terminate before all capacity activity; misses require exact-profile attestation before sandbox/session creation, complete CAS verification before use, and atomic publication before visibility.
- The envelope schema fixes eighteen semantic operations plus typed disposition/state/log/result messages, 25 closed reason codes, operation/revision/payload identities, resumable log cursors, and compare-and-swap publication. Provider option bags and project-visible provider fields remain forbidden; transport/framing stays experimental and unselected.
- Thresholds are frozen before Phase B: 30 ready-hit samples at p95 <300 ms with nine zero resource counters; 30 warm SSH/Linux admissions at p95 <2 s excluding queueing; 30 non-blocking TryReserve samples at maximum <=100 ms; five cancellation samples at maximum <=1 s acknowledgement; five cleanup samples at maximum <=30 s or exact orphan; all 26 fault cases repeat five times per adapter with retained failures.
- The strict SSH availability schema requires a pinned endpoint and host key, approved identity mediator, Linux profile and runner digests, experiment-owned remote scope, command/fault/cleanup allowlists, and explicit network/mutation approval. No availability manifest exists and no SSH, provider, VM, simulator, worker, credential, shared-resource, or external operation was performed.
- Frozen Phase A digest: a270d6efa007b4991aacc85843c1558a03385c322c8b7828ccac336c5ddd33ed. No adapter/worker implementation, Go module, raw evidence, scorecard, selected branch, or ADR was added.

Verification:
- Direct isolated-worktree mise exec -- task --dir experiments/e08-worker-protocol check:phase-a stopped at the expected untrusted copied mise.toml guard without running work.
- mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.16/experiments/e08-worker-protocol check:phase-a — PASS: nine mutation tests and frozen contract verifier.
- Bound fixture checks — PASS: integrity-faults check; T1 benchmark-harness check; E06 check:phase-a with twelve tests and verifier. All used the trusted primary checkout only for pinned tooling and executed against this worktree.
- Repository-root check first passed every gate through malicious-planner, where the managed sandbox denied its required throwaway loopback listener. The identical approved outside-sandbox command then PASSed the complete prototype, T1, vet, test, build, and race gates: mise -C /Users/alessandro.rizzo/personal/taskflow exec -- env GOCACHE=/private/tmp/taskflow-tf00316-root-cache task --dir /private/tmp/taskflow-tf-003.16 check.
- All JSON documents parse; whitespace/conflict scan is clean; git diff --check passes.

Mandatory checkpoint:
- Phase A remains uncommitted. AC/DoD are unchecked; Final Summary and E08 ADR are absent; status remains In Progress. Awaiting explicit contract diff approval before any Phase A commit or Phase B implementation.

Approved Phase A contract commit:
- fe41c64190801011359d87ddf3f34819973428aa (experiment(e08): freeze worker protocol contract).
- Commit contains only reviewed TF-003.16 metadata and experiments/e08-worker-protocol Phase A files. It was not pushed.
- Phase B authorization remains limited to the in-process adapter and non-mutating E06-shaped macOS stub; SSH remains forbidden and blocked.

Commit-hash correction:
- The exact Phase A commit is fe41c6428c4d7d432cdd463c82dd12c3465e1103. The abbreviated fe41c64 shown by git commit was expanded incorrectly in the preceding note; this correction is authoritative.

Approved partial Phase B implementation (2026-09-04):
- Added a disposable experiment-local Go controller plus deterministic in-process adapter and non-mutating E06-shaped macOS stub. The same core enforces cache-before-reservation, exact planned-profile attestation before sandbox/session creation, verified CAS bytes, typed execution/log/cancel outcomes, compare-and-swap publication, cancellation-detached cleanup, explicit reservation release, semantic reconnect/revision and log-cursor replay, and exact orphan query/reconciliation. No production or prototype package imports the experiment.
- Added executable and mutation-oriented tests, probe/evidence commands, an immutable-Phase-A verifier, a separate Phase B Taskfile, sanitized raw JSONL, T1 benchmark-v2 records, implementation/evidence manifests, scorecard, limitations, and ADR 0012. The original 14 Phase A files remain byte-identical to commit fe41c6428c4d7d432cdd463c82dd12c3465e1103.
- Retained 260 local fault rows: 200 executable/typed-core rows and 60 explicitly labelled state-machine-analysis-only rows. All exercised rows pass. Analysis-only rows are not claimed as transport, worker-loss, lease-expiry, or SSH evidence.
- Applicable local measurements pass: in-process p95 cache hit 6.203 ms, TryReserve maximum 6.856 ms, cancellation maximum 6.279 ms, cleanup maximum 13.696 ms; macOS-stub p95 cache hit 5.832 ms, TryReserve maximum 6.187 ms, cancellation maximum 5.813 ms, cleanup maximum 6.029 ms. Ready hits record zero reservations/resource work. The full first measurement set with a cleanup boundary error was retained under evidence/failures/pre-boundary-correction and excluded before a complete sample-one rerun; thresholds were unchanged.
- Frozen precedence selects state-machine-first transport deferral. No SSH availability manifest exists; zero SSH/network connections and zero macOS lifecycle/provider/shared-host mutations occurred. Representative SSH/Linux, remote admission, credential/host-key mediation, remote reconnect/process/cleanup behavior, AC #1, and dependent all-three-adapter gates remain blocked/unpassed. Transport and all experiment APIs/envelopes remain experimental; no production contract stabilizes.

Verification at final checkpoint:
- Phase B Taskfile check PASS: Phase A commit snapshot/bound inputs, gofmt, go vet, go test -race ./..., and evidence/decision verifier.
- Bound integrity-faults and T1 benchmark fixture checks PASS using experiment-owned Go caches after their first default-cache attempts were sandbox-denied; E06 check:phase-a PASS.
- Repository-root mise exec -- task check PASS, including prototype, all T1 fixtures, builds, vets, tests, and race suites.
- git diff --check PASS.

Mandatory final checkpoint: AC/DoD remain unchecked; status remains In Progress; no Final Summary, Phase B completion commit, merge, push, SSH work, or cleanup was performed.

Measurement correction (authoritative final records):
- The exact final scorecard values are: in-process p95 cache hit 5.765000 ms, TryReserve maximum 5.745709 ms, cancellation maximum 5.569292 ms, cleanup maximum 13.823042 ms; macOS-stub p95 cache hit 5.654417 ms, TryReserve maximum 5.684542 ms, cancellation maximum 6.089750 ms, cleanup maximum 5.566167 ms. The prior Phase B note copied transient pre-final-run values; this correction is authoritative.

User-approved partial evidence checkpoint (2026-09-04):
- The user accepted the reviewed in-process/macOS-stub implementation, retained evidence, ADR 0012, and frozen-precedence state-machine-first transport-deferral decision for a checkpoint commit.
- This approval does not authorize SSH work or ticket completion. No approved SSH availability manifest or representative SSH/Linux evidence exists; AC #1 and every dependent all-three-adapter gate remain blocked and unchecked.
- Keep TF-003.16 In Progress with all AC/DoD unchecked and no Final Summary. Preserve the branch and worktree after the ticket-scoped checkpoint commit; do not push, merge, or clean up.

Local real-Linux SSH extension research (2026-09-05):
- Checkpoint readback is clean on tf-003.16-e08-worker-protocol at 3cb71951a36a1a56fa957a0ddea641d1c5ef0669. Dependencies TF-003.03 and TF-003.13 remain Done.
- Read-only local probes confirm Colima 0.10.1 is installed; its default profile is Broken/not running and advertises aarch64, 2 CPUs, 8 GiB memory, and 100 GiB disk. Docker client 29.4.1 cannot reach the default socket; limactl reports no instance. /Users/alessandro.rizzo/.colima currently consumes about 57 GiB, while the data volume has about 1.4 TiB available.
- Recommendation: do not repair or reuse the existing default profile. Set isolated COLIMA_HOME, COLIMA_CACHE_HOME, and DOCKER_CONFIG roots under /private/tmp/taskflow-e08-ssh-linux, create one small named profile, and delete that exact root after evidence. This prevents the experiment from claiming or mutating the existing 57 GiB.
- Colima 0.10.1 exposes the required no-activation, no-host-mount, no-agent, and no-user-SSH-config flags. Its upstream defaults warn that host-home mounting and SSH config generation otherwise occur, which is why every isolation flag is explicit.
- LinuxServer's current OpenSSH Server documentation/source confirms ARM64 support, a real OpenSSH daemon on container port 2222, persistent /config host keys, public-key provisioning, disabled password and sudo modes, and container-scoped filesystem/process access. Execution must pin a resolved image digest; a mutable latest tag is not evidence.
- A Colima Linux VM plus a digest-pinned Linux OpenSSH container reached at loopback by /usr/bin/ssh is valid for E08's transport/host-key/framing/reconnect/Linux-process ownership/cleanup questions if the frozen manifest and fault gates pass. It is not localhost macOS or mocked SSH. It remains explicitly non-evidence for WAN/provider behavior, remote quotas/provisioning, external credential stores, physical host loss, or cross-host recovery.
- Required approvals are deliberately split: (1) approve this plan for repository-only SSH adapter/worker implementation; (2) after reviewing that diff, approve exact isolated registry/key/Colima/container materialization with no SSH; (3) after reviewing the populated frozen-schema availability manifest and host/profile/runner digests, approve only its exact OpenSSH connections, named fault injections, and exact cleanup. No current approval authorizes any of these mutations.

Foreground execution approval (2026-09-05):
- The user explicitly approved the recorded repository implementation and isolated local real-Linux SSH experiment, requested direct foreground execution without delegation, and asked not to pause for optional approvals.
- Authorization covers the plan-scoped repository edits, isolated /private/tmp/taskflow-e08-ssh-linux ownership root, image resolution/download, experiment-only keys, named Colima profile/container/network/volume, fixed loopback port, strict SSH connections, named fault injection, evidence collection, and exact cleanup/disk reclamation.
- The mandatory pre-commit/Done summary-and-diff checkpoint remains required by repository policy.

Foreground local Linux/OpenSSH extension completed (2026-09-05):
- Implemented an experiment-local newline-JSON codec, strict /usr/bin/ssh transport runner, SSHLinuxAdapter, persistent Linux worker daemon/proxy, profile/manifest tools, fault collector, verifier extensions, and tests. The existing typed controller drives in-process, SSH/Linux, and macOS-stub shapes; no production/prototype package imports the experiment.
- Materialized a real ARM64 Linux/OpenSSH worker in an isolated local Colima VM and digest-pinned container. Manifest pins 127.0.0.1:22216, Ed25519 host fingerprint SHA256:jijUrPcVoEr8vHLFOAjJR3I9eLnhS6HT/RwJPB55/Ck, experiment-only client identity, image sha256:2a48f9ce01f61c1d7b376b7be99bd12801a3ecd9f339a4c7e7698d529e8d0b47, runner sha256:c1e08fb5df6d87c5628ac75dd7f8e25f2eeb072d26417a8a692293d9722319da, and profile sha256:e6da1e31f41cade64d1f9c32818b59fa56f9771b90c4ff340dd841949fe2146e. Password auth, sudo, forwarding, ambient SSH config/agent use, host mounts, and arbitrary commands were disabled.
- The first strict adapter probe failed closed before sandbox creation and exposed cross-process reuse of reservation operation IDs. Reservation identity was corrected with a per-adapter execution namespace; the retained final evidence uses unique attempt identities so durable replay cannot mask execution or cleanup.
- Final evidence: 390 rows, all passing; 300 executable/typed and 90 explicitly analysis-only. SSH/Linux contributes 130 rows and 125 manifest-bound connections. Cache hits and TryReserve open zero SSH connections. Actual SSH evidence covers strict attestation, digest rejection, missing inputs, allowlist rejection, durable replay across fresh connections, conflicting duplicate rejection, exact orphan query, cleanup, and two compatible worker identities.
- Frozen SSH metrics pass without threshold changes: ready-hit p95 12.066 ms <300 ms; warm admission p95 51.178 ms <2000 ms; TryReserve max 8.129 ms <=100 ms; cancellation acknowledgement max 7.563 ms <=1000 ms; cleanup max 55.801 ms <=30000 ms.
- Frozen precedence remains state-machine-first transport deferral. Thirty Linux rows remain analysis-only and disconnect evidence is at acknowledged boundaries, so this does not prove precisely timed mid-flight cuts, WAN/provider behavior, external credential brokers, physical host loss, or cross-host recovery.
- Cleanup complete: removed only taskflow-e08-ssh-linux container/network/volume/profile, the exact temporary root/private keys, the run-created Colima cache image, listener, and worker processes. Direct cleanup reclaimed about 2.02 GiB; fstrim safely released unused sparse blocks, leaving 10.855 GiB more filesystem space than before the experiment. The larger trim includes older unused blocks and is not attributed solely to E08. The pre-existing default Colima profile was not deleted.
- Verification passes: immutable Phase A snapshot; experiment gofmt/go vet/go test -race; offline three-shape evidence/decision verifier; full repository mise/task check including malicious-planner loopback and all race gates; git diff --check; private-key/provider-option scan.
- Mandatory final checkpoint remains: AC/DoD unchecked, no Final Summary, status In Progress, and no completion commit/merge/push/worktree cleanup.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Completed the E08 worker-protocol experiment across all three required adapter shapes while retaining state-machine-first transport deferral.

Changes:
- Added an experiment-local typed SSH/Linux adapter, persistent newline-JSON worker daemon/proxy, strict OpenSSH runner, manifest/profile tooling, fault collector, and tests.
- Bound a real ARM64 Linux/OpenSSH run to an approved endpoint, host key, experiment-only identity, image/runner/profile digests, command allowlist, and exact ownership/cleanup scope.
- Preserved 390 passing fault rows and 13 passing benchmark sets; cache hits and TryReserve open zero SSH connections.
- Fixed cross-process reservation identity after the first strict probe exposed stale durable-operation replay.
- Updated ADR 0012 and the offline evidence verifier. The decision remains state-machine-first transport deferral because local-VM evidence does not prove WAN/provider, precise mid-flight failure, external credential-broker, physical-host-loss, or cross-host recovery behavior.

Verification:
- E08 immutable Phase A snapshot, gofmt, go vet, go test -race, and three-shape evidence verifier passed.
- Full repository mise/task check and git diff --check passed.
- No private key or provider option bag is retained.

Cleanup:
- Removed the experiment container, network, volume, Colima profile, keys, run-created cache image, listener, worker processes, and exact temporary root.
- Direct cleanup reclaimed about 2.02 GiB; safe sparse-filesystem trim left 10.855 GiB more available than before the experiment, with older freed blocks explicitly excluded from E08 attribution.

Risk/follow-up:
- Transport and all experiment APIs remain disposable and must not stabilize before representative external-host evidence closes the recorded limitations.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 Protocol state machine, thresholds, and fault matrix are committed before implementation
- [x] #2 Raw traces for all three adapter shapes and fault cases are preserved
- [x] #3 Task-specific integration checks and root verification pass
- [x] #4 The E08 decision and Backlog final summary are current
<!-- DOD:END -->
