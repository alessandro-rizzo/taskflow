---
id: TF-003.13
title: 'Inventory E06 macOS VM, Xcode, and simulator mechanisms'
status: Done
assignee:
  - '@codex-tf-003-13'
created_date: '2026-09-03 13:55'
updated_date: '2026-09-04 15:28'
labels:
  - risk-lab
  - e06
  - macos
  - mobile
  - parallel-wave-r1
dependencies:
  - TF-003.01
  - TF-003.05
references:
  - docs/roadmap.md#e06-macos-vm-xcode-and-simulator-feasibility
  - fixtures/w3/
parent_task_id: TF-003
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Roadmap experiment: E06 setup; risks R4, R8, R9; requirements MAC-1 through MAC-5.
Outcome: establish the available macOS hardware/tooling inventory, candidate mechanisms, safe measurement plan, and initial worker/sandbox/session shape before costly feasibility runs.
Candidates: immutable warm VM restore; warm clean VM with APFS-cloned workspaces; VM per run/namespace; trusted native host with strict reset; simulator clone/reset versus fresh boot.
Stable assumptions: this ticket does not claim representative hardware exists or select a feasibility branch.
Parallelism: begins early on separate macOS infrastructure. Own experiments/e06-macos-feasibility inventory/setup artifacts only; do not mutate shared host/VM state without a fresh manifest and explicit approval.
Test or fault scenario: inventory image/profile identity, reset controls, concurrency constraints, failure-recovery hooks, and contamination probes before executing them.
Observability: available hardware, macOS/Xcode/SDK/runtime builds, VM/simulator tools, licensing/automation constraints, capacity, and evidence destinations.
Rollback or removal: setup steps have explicit cleanup and preserve base images; no destructive host mutation.
Versioned formats: machine profiles and image manifests are experimental.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Current available macOS hardware, VM/snapshot tooling, Xcode/SDK/simulator runtimes, and automation constraints are evidenced without assumption
- [x] #2 Each viable candidate has a concrete setup/reset/cleanup procedure and an immutable profile identity plan
- [x] #3 Cold/warm timing, contamination, concurrency, image-update, and failure-recovery measurements are predeclared against W3
- [x] #4 Representative infrastructure is either reserved with a read-only manifest or recorded as an explicit external blocker with the next safe action
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create a Phase-A-only E06 inventory contract under experiments/e06-macos-feasibility at base revision 21a55f3e. Bind the W3 v1 specification, its two namespace examples and six fault scenarios, plus the T1 benchmark-v2 record/validator by path and SHA-256. Map only canonical product requirements (EXEC-3/4/5, REP-2, AGENT-4/5 where applicable) and retain ticket-only MAC-1..5 as noncanonical provenance. Freeze the complete inventory/contract artifact set through frozen-artifacts.json and protocol.sha256; forbid measurement results and any selected E06 branch in this ticket.
2. Add a dependency-free, explicitly read-only inventory collector and commit its sanitized snapshot. Capture system_profiler, sw_vers/uname, Xcode version/build/path/SDK output, first-launch status, framework/tool presence and versions, filesystem capacity, simulator image metadata, and default-device counts/states from plist files. Never call simctl, start a service, reveal host serial/UUID/device UDIDs, inspect credentials, use the network, install/download, or modify host state. Preserve raw command transcripts alongside normalized host/tool/simulator JSON and record every sandbox-denied probe as unknown rather than inferring success.
3. Define the candidate matrix for immutable warm Tart VM restore, one warm clean VM with APFS-cloned workspaces, VM per namespace/run, trusted native host, and coarse external runner; separately compare simulator clone-from-golden, erase/reset, and fresh-create/boot. Give every candidate an evidenced availability state, pinned prerequisites, capacity assumptions, setup/reset/cleanup procedure, exact experiment-owned path/name prefix, forbidden shared resources, rollback, and rejection reason. Installed Linux VM/container CLIs do not count as macOS candidates; command presence never counts as exercised evidence.
4. Specify identity and lifecycle boundaries consumed later by E08: a profile digest covers mechanism/version, immutable image digest, macOS/Xcode/SDK/runtime builds and architectures, runner digest, and sandbox/reset-policy digests; worker/VM instance, workspace sandbox, and simulator session/lease identities remain separate and ephemeral. Require attestation before build/install/test, reject mismatches, and never allow an unexpected worker identity to rewrite the planned profile.
5. Predeclare TF-003.14 timing records against W3 using benchmark schema taskflow-t1-benchmark/v2: 15 descriptive cold-boot samples; 30 warm-acquire/workspace-ready samples with warm workspace p95 strictly below 3 seconds; 30 simulator-ready-to-install samples with p95 strictly below 15 seconds; and 15 samples each for build, install, test, reset, cleanup, and image import/update where applicable. Freeze start/end boundaries, per-sample preparation, cache dimensions, lease/reservation counts, raw trace destinations, serial execution order, failure-retention rules, and exclusions before any candidate runs.
6. Predeclare correctness and fault probes: twenty alternating namespace handoffs with zero workspace/DerivedData/simulator-state residue; ten two-namespace repetitions with zero identity, writable-path, simulator, or lease collision; a bounded concurrency ramp whose exact levels and memory/thermal stop limits are sealed in the approved execution manifest; five each of VM-loss, simulator-loss, cancellation, and caller-loss recovery; immutable-base checksum before/after; and one measured image update/rollback. E06 does not claim Linux endpoint authorization or routing, which remains E07-owned. A candidate that cannot safely reach concurrency two is classified as serialized rather than silently passing.
7. Record infrastructure-status.json as an explicit external blocker, not a reservation: current shared native host is not exclusive, one default simulator is already booted, no macOS VM tool/image or external runner credential is available, and managed CoreSimulator access is blocked. Provide an execution-manifest schema that must resolve exact host, candidate/version, base-image digest, custom device-set root, VM/workspace names, resource limits, commands, cleanup allowlist, evidence paths, and operator before TF-003.14. User approval of this implementation plan does not authorize that later external or destructive execution.
8. Add a dependency-free verifier plus mutation tests proving fixture/hash bindings, normalized/sanitized inventory, complete candidate procedures, immutable identity fields, exact metric counts/thresholds/boundaries, fault matrix, decision-branch coverage, blocker status, manifest safety allowlist, and absence of Phase-B evidence. Expose mise exec -- task --dir experiments/e06-macos-feasibility check:phase-a; then run that command, mise exec -- task check, and git diff --check from the repository root. Update notes with exact results and limitations, but stop for the mandatory user diff/summary approval before checking AC/DoD, marking Done, committing, merging, pushing, or performing cleanup.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Planning ownership (2026-09-04):
- Agent: @codex-tf-003-13
- Branch: tf-003.13-e06-inventory
- Worktree: /private/tmp/taskflow-tf-003.13
- Initially claimed paths: experiments/e06-macos-feasibility/**; Backlog metadata for TF-003.13 only.
- Planning phase only: inventory uses non-mutating local checks; no GUI, VM/simulator boot/create/reset/delete, installation/download, or shared host-state mutation. No implementation/evidence/docs edits before explicit user approval.

Planning research and local inventory (2026-09-04):
- Dependencies verified through Backlog CLI: TF-003.01 and TF-003.05 are Done. Branch HEAD, main, and merge-base are all 21a55f3ea9eac0016d55b7827e80c01c237c9020.
- Current host evidence: MacBook Pro Mac17,7 / Apple M5 Max / arm64 / 18 cores / 64 GiB; macOS 26.5.2 build 25F84; Xcode 26.6 build 17F113 at /Applications/Xcode.app; iOS and iOS Simulator SDK 26.5 build 23F81a; first-launch status exits 0.
- Installed simulator image metadata identifies iOS 26.5 runtime build 23F77, arm64. Default device plists expose eleven devices; an iPhone 17 is currently state 3 (booted) and was last booted at 2026-09-04T14:44:18Z. It is shared host state and is explicitly out of scope.
- Simulator service queries fail in the managed sandbox because CoreSimulatorService and its log path are inaccessible. Hardware sysctl, diskutil framework access, DevToolsSecurity, and ps are likewise sandbox-blocked. The retained collector must use system_profiler, Xcode metadata, filesystem reads, and sanitized CoreSimulator plist reads only; no simctl call belongs in the non-mutating inventory command.
- Virtualization.framework and Hypervisor.framework exist. Tart, Orchard, vfkit, UTM, Parallels, VirtualBox, and a macOS restore/image tool are absent. Lima 2.1.1, Colima 0.10.1, and Docker CLI 29.4.1 are installed but are Linux-container/VM tooling and do not establish a macOS-guest candidate. Approximately 1.4 TiB is free on the data volume.
- Official mechanism research confirms Apple Virtualization requires a supported restore image and virtualization entitlement; Tart uses Virtualization.framework and OCI-distributed images; Orchard adds host orchestration but introduces privileged/local-network setup choices. None was installed, authenticated, downloaded, or run.
- Product truth defines no MAC-1 through MAC-5 requirement IDs. Keep those ticket labels as stale provenance only; map evidence to R4/R8/R9 and current EXEC/REP/AGENT requirements without inventing aliases.
- Candidate status: trusted native host plus experiment-owned workspace/device set is conditionally viable but unreserved; warm immutable VM, warm VM plus APFS workspaces, VM-per-namespace, and coarse external runner are externally blocked by absent tooling/images/credentials or capacity reservation.
- External blocker and next safe action: no representative exclusive infrastructure or mutation window is reserved. After this inventory contract is accepted, request a separate approval against a fresh exact manifest for either (a) a dedicated native-host custom device set and workspace or (b) a dedicated Apple-silicon host with pinned Tart/Orchard tooling and a digest-pinned macOS/Xcode image. No default simulator device, existing VM, base image, or user workspace may be touched.

Refined proposed file claims (all new under experiments/e06-macos-feasibility):
- README.md; Taskfile.yml; contract.json; protocol.sha256; frozen-artifacts.json; fixture-bindings.json.
- candidate-matrix.json; candidate-procedures.md; measurement-plan.json; infrastructure-status.json; execution-manifest.schema.json.
- inventory/host-profile.json; inventory/tooling.json; inventory/simulator.json.
- inventory/raw/system-profiler.txt; inventory/raw/xcodebuild-version.txt; inventory/raw/xcodebuild-sdks.txt; inventory/raw/xcode-first-launch.txt; inventory/raw/local-tool-presence.txt; inventory/raw/simulator-plists.txt.
- scripts/collect_inventory.py; scripts/verify_contract.py; tests/test_verify_contract.py.
- No edits outside this new experiment subtree and TF-003.13 Backlog metadata.

Implementation checkpoint (2026-09-04)

- Implemented the approved Phase-A-only frozen inventory and measurement contract under `experiments/e06-macos-feasibility/**` (23 new files). No Phase B execution, candidate selection, lifecycle action, or production integration was performed.
- Frozen the W3 fixture bindings, benchmark helper hashes, candidate matrix/procedures, measurement thresholds and repetition counts, execution-manifest safeguards, point-in-time sanitized host/tooling/simulator inventory, blocker state, and artifact-integrity manifest.
- Current outcome is inventory/contract evidence only: no candidate has runtime proof and no branch is selected. Candidate blockers remain external approval/reservation/provisioning constraints; the trusted native-host candidate is conditional-unreserved, while VM-backed candidates require missing/provisioned tooling or images.
- Safety maintained: only read-only local inventory queries were executed. No GUI, `simctl`, VM/simulator boot/create/reset/delete, install/download, provider credential, network-provider action, or shared-host lifecycle mutation was invoked. The collector emits JSON to stdout and writes no files.

Verification

- `mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.13/experiments/e06-macos-feasibility check:phase-a` — PASS: 12 unit/mutation tests; verifier reports `Phase A inventory and measurement contract valid`. The trusted primary checkout supplied the already-installed mise toolchain because the isolated worktree config was untrusted; `mise trust` was not invoked.
- `env PYTHONDONTWRITEBYTECODE=1 python3 experiments/e06-macos-feasibility/scripts/collect_inventory.py >/dev/null` — PASS; read-only collector completed without repository output.
- Root `task check` first needed a ticket-scoped Go cache because the default cache was unavailable in the managed sandbox. The sandboxed rerun passed through the suite until the malicious-planner loopback test hit the expected bind restriction.
- `env GOCACHE=/private/tmp/taskflow-tf00313-go-cache mise -C /Users/alessandro.rizzo/personal/taskflow exec -- task --dir /private/tmp/taskflow-tf-003.13 check` — PASS outside the managed sandbox, including prototype, T1, race, and malicious-planner gates.
- `git diff --check` — PASS before ticket-note recording; rerun at final diff checkpoint.

Awaiting explicit diff approval before any AC/DoD checks, Final Summary, Done transition, commit, merge, push, or cleanup.

Final diff checkpoint (2026-09-04): `git diff --check` PASS after recording the implementation evidence. Worktree contains only the TF-003.13 Backlog metadata change and the claimed new E06 experiment subtree.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Established the frozen Phase-A E06 macOS/Xcode/simulator feasibility inventory and measurement contract without exercising shared infrastructure.

Changes:
- Added sanitized, read-only host, Xcode/SDK/runtime, simulator-image, framework, and local-tool inventory with a dependency-free collector.
- Bound the W3 fixture and T1 benchmark helpers by digest, and froze all Phase-A contract artifacts for reproducible follow-on execution.
- Defined macOS worker and simulator candidate procedures, immutable profile identity, setup/reset/cleanup ownership, contamination and concurrency probes, timing thresholds, recovery cases, and decision branches.
- Added an execution-manifest schema that forbids the default simulator set, broad cleanup, immutable-base deletion, path traversal, and treating this approval as Phase-B execution approval.
- Added a verifier and 12 mutation/unit tests covering fixture drift, privacy, procedure completeness, thresholds, blocker state, lifecycle restrictions, and frozen-artifact integrity.

Outcome and boundary:
- Phase A is complete, but no candidate has runtime feasibility evidence and no E06 branch is selected.
- Representative execution remains an explicit external blocker: the shared native host is unreserved, and VM-backed candidates require separately provisioned tooling/images or an external runner.
- Any Phase-B measurement must receive fresh explicit approval against a fully resolved execution manifest; it must use experiment-owned workspaces and custom simulator device sets and must not modify shared/default resources.

Verification:
- Phase-A Taskfile check: 12 tests PASS; frozen contract verifier PASS.
- Read-only inventory collector PASS.
- Repository root `task check` PASS, including prototype, T1, race, and malicious-planner gates.
- `git diff --check` PASS.

Risk/follow-up:
- TF-003.14 should resolve the external reservation/provisioning blocker and request separate execution authorization; this ticket does not authorize VM/simulator lifecycle operations, downloads, provider access, or shared-host mutation.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 No external or destructive setup occurs without a fresh manifest and explicit user approval
- [x] #2 Inventory and predeclared measurement plan are preserved under the experiment
- [x] #3 Local non-mutating verification and root check pass
- [x] #4 Backlog acceptance criteria, notes, blocker state, and final summary are current
<!-- DOD:END -->
