# Candidate A limitations

This is disposable E01 evidence. It explicitly registers schema metadata, so
metadata duplication and registration calls remain visible authoring costs.
Its typed handles and W1 trace are fake composition evidence, not plan IR or
execution semantics. File auditing is not a hostile-code sandbox.
